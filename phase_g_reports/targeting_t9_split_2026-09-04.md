<!-- Presented with wave 2 per the owner's ruling of 2026-09-04 ("T9: port-only additions with a documented
reason become MOD records behind the modern config; the rest are removed from classic. Present the split
with wave 2."). Line numbers are HEAD 6e041e5's, before wave 1 landed; wave 1 moved lines in Girlfriend,
EntityGammaMetroid, EntityLeon, EntitySpyro, EntityStinky, Pointysaurus, ThePrinceAdult/Teen by a few
lines each (the gates it added), nothing else in the rows below. -->

# T9 split — port-only targeting additions: MOD record (A) or removal from classic (B)

Read-only analysis, 2026-09-04, HEAD `6e041e5`. Input: `phase_g_reports/targeting_survey_2026-09-04.md` §4 T9 (15 rows, :1700-1724) plus the T7 row deferred to this split (MyValentineTarget / Girlfriend, :1666).

**Owner's ruling (verbatim, phase_g_scope_addendum_2026-09-03.md:97-98):** "T9: port-only additions with a documented reason become MOD records behind the modern config; the rest are removed from classic. Present the split with wave 2."

**Method per row.** (1) The port code at the cited lines and the orig code (or the confirmation that orig has nothing there). (2) A documented reason searched in: the code's own javadoc/comments; AUDIT_FINDINGS.md, FIX_LOG.md, MODERNIZATION_NOTES.md, KNOWN_ISSUES.md, CHANGELOG.md, PARITY_NOTES.md, IMPLEMENTATION_PLAN.md, TESTING_CHECKLIST.md (class, goal and predicate names, reason phrases); `git blame` on the cited lines and the introducing commit's message (a commit message that states a reason counts). (3) Classification A/B with the classic restoration and the gametests that touch the site (`src/gametest`). (4) A safety flag (Peaceful players, creative players, owner/allies) that overrides the documentation test where it applies.

**Vanilla cites** are from the in-repo NeoForm decompile `build/neoform/neoFormJoined1.21.1-20240808.144430/steps/decompile/outputs.jar` (the survey's S3 numbering; its T7 row's "(v) LivingEntity.java:899-901" is the gradle-cache jar's numbering of the same method): `LivingEntity.canAttack` :864-866 (`Player && PEACEFUL → false`, else `canBeSeenAsEnemy`); `TargetingConditions.test` :60-90 (:73 — `isCombat`: `canAttack`, `canAttackType`, `isAlliedTo`); `NearestAttackableTargetGoal` :41 (`forCombat()`), :58-71 (`findTarget`); `TargetGoal.canContinueToUse` :38-47 (a held target is dropped when `canAttack` turns false); `Player.canBeSeenAsEnemy` :903-905 (`!abilities.invulnerable`); `Player.hurt` :875-878 (difficulty-scaled damage zeroed in PEACEFUL) with `DamageSource.scalesWithDifficulty` :96-102; `TamableAnimal.canAttack` :181-183 (never the owner), `isAlliedTo` :206-215 (the owner and the owner's team). Config precedent: `OreSpawnConfig.java:431-460` (`[modern]` block: `enabled` master, `mothraWideRootHitbox`, `fireRespectsMobGriefing`), helpers :470-487 (`master && key`); the master's comment :433-443 lists every key and must list any new one; goal registration happens in the `Mob` constructor, so a key that gates a goal is a construction snapshot (BOSS-017 pattern, :422-429), a key that gates a filter can be read live (MOD-031's impact read).

Line numbers are HEAD's. Where the survey's cite has drifted it is noted.

## 1. The table

| row | species | what the port adds | orig cite | port cite | documented reason (source, verbatim) or "none found" | proposed side | safety flag | tests pinning it |
|---|---|---|---|---|---|---|---|---|
| 1 | Cryolophosaurus | A **melee chase goal**: `DinosaurMeleeAttackGoal` @2 on the goal selector with `Presets.cryolophosaurus()` = navigate 1.25, reach √5 + width/2, cadence 1-in-5, swing dice 1-in-12 / 1-in-14, forget 1-in-200, follow 9, knockback 2 (ai/DinosaurMeleeAttackGoal.java:37). It consumes `getTarget()` (ai/BugMeleeAttackGoal.java:83-150: look, chase, bite, `setTarget(null)` on the forget roll), and only `HurtByTargetGoal` @1 (:62) ever fills that slot — the ported 1-in-5 hunt (:96-105) chases and bites its own `findSomethingToAttack` pick without `setTarget`. So the goal is a second, parallel chase/bite path acting on the revenge target only. Plus the no-op `legacySetAttacking` callback (:66). | Cryolophosaurus.java:51-57 — tasks Swim / Panic / MoveThroughVillage / WanderALot / WatchClosest / LookIdle and `EntityAIHurtByTarget(this,false)` @1; **no attack task**; `func_70638_az` (getAttackTarget) is never read in the class, only `func_70604_c(null)` at :148 (the 1-in-200 forgiveness); `isAIEnabled` true (:84). The revenge target was stored and never chased; the chase came only from the 1-in-5 nearest-in-9×2×9 hunt (:150-155). | Cryolophosaurus.java:57-58 (goal), :62 (HurtBy), :66 (callback), :86-105 (the ported hunt; the survey's ":150-152 dice" are orig lines — port :100-101) | **Commit f5cb0ba5** (2026-04-17, "Phase 4C: Dinosaurs & Reptiles - goal-based AI, renderer scales, spawns"): "All dinosaurs now use modern MeleeAttackGoal-style combat via Goal selectors plus HurtByTargetGoal / NearestAttackableTargetGoal for target acquisition." and "Cryolophosaurus keeps its high forgetTargetRoll / low attack probability to preserve its timid 1.7.10 personality." **AUDIT_FINDINGS.md:171** (ENT-A-009, the Alosaurus): "the pick feeds the accepted DinosaurMeleeAttackGoal mapping via setTarget". The port comment :49-52 describes the orig hunt "in addition to retaliating via HurtByTargetGoal" (true of the port, not of 1.7.10). No Cryolophosaurus record (ENT-A-111/112/113) names the goal; ENT-A-112's resolution (:1044) restored the orig hunt beside it and says nothing about it. | **A** (by the letter: commit-stated reason + the ENT-A-009 acceptance) — flagged §4.1: the documentation is generic (Phase 4C) / another species', and the goal duplicates the ported chase. | none — the goal inherits only the revenge target; a creative attacker is dropped by `TargetGoal.canContinueToUse` → `canAttack` → `canBeSeenAsEnemy` (`invulnerable`); Peaceful: a Monster, engine despawn. Removal lowers aggression. | **none.** `EntityLogicTestsA#cryolophosaurus_proactive_hunt` (:1044-1064) bites an unprovoked pig through the ported hunt — the slot stays null, the goal never runs; `CreativeMappingParityTests` :190 (filter by reflection); `TargetScanParityTests` :201 (the species as another hunter's exclusion). |
| 2 | DungeonBeast | **No port-written code.** The refusal is the engine's, reached through the vanilla goal path: `HurtByTargetGoal` @1 (DungeonBeast.java:88) → `TargetGoal.canContinueToUse` :46 → `LivingEntity.canAttack` :864-866. The ENT-S-108 scan (`selectTarget` :151) hands its pick to `setTarget` (:168) without `canAttack`, so only the revenge path carries the gate. | none — `EntityMob` (DungeonBeast.java:40); neither tree gates selection on difficulty (survey V13, :58). | DungeonBeast.java:88; vanilla `LivingEntity.java:864-866` | **Not a port addition** — no commit introduces it; it is the engine. The survey's own V13 (:58): "The vanilla goals additionally refuse PLAYER targets in PEACEFUL (`canAttack`) — inert for Monsters (rows say PORT-ONLY, inert)", and the identical gate is rated "MATCH (engine, P6)" on the S1 Monster rows (:87, :101, :129, :143, :157, :171, :185, :199 "moot", :213, :241, :255, :274) and "N/A (P3)" at :867. | **B-inert: nothing to delete.** "Removal" would be a `canAttack` override on a Monster — a no-op while `Mob.checkDespawn` (survey V13: Mob.java:703-705, `Monster.shouldDespawnInPeaceful` :55-57) discards it in Peaceful. Recommend no code; re-rate the row "MATCH (engine, P6)" like its siblings. | **YES if "removed":** a `canAttack` override would let the mob attack players in Peaceful the moment it persisted there (any future `shouldDespawnInPeaceful` override, a modded persistence rule). Never add one. Lands on neither side — leave the engine gate. | none (the PEACEFUL mentions in CephadromeGateTests, CreativeMappingParityTests :234/:377, ProjectileTypeParityTests :1113, LeonTargetingTests are NORMAL-difficulty preconditions or other species). |
| 3 | EmperorScorpion | as row 2 — `HurtByTargetGoal` @1 (EntityEmperorScorpion.java:97); ENT-S-108 `selectTarget` :247 / `setTarget` :264 bypass `canAttack`. | none — `EntityMob` (EmperorScorpion.java:50) | EntityEmperorScorpion.java:97 | as row 2 (engine; V13) | **B-inert: nothing to delete** — no code, re-rate. | as row 2 | none (`HitboxDimsParityTests#s095_emperor_scorpion_dims_both_modes`, `EntityLogicTestsA#emperor_scorpion_baby_flood`, IgnoreScreen/TargetScan rows — not the gate). |
| 4 | EnderKnight | as row 2 — `HurtByTargetGoal` @1 (EnderKnight.java:46) and `NearestAttackableTargetGoal<Player>` @2 (:47, `forCombat` :41 → `test` :73 → `canAttack`). | none — `EntityMob` (EnderKnight.java:27) | EnderKnight.java:46-47 | as row 2 (engine; V13) | **B-inert: nothing to delete** — no code, re-rate. | as row 2 | none (DsbOutcomeTests, HitboxDims, RecipeTests, StructureTestsC — not targeting). |
| 5 | EnderReaper | as row 4 — `HurtByTargetGoal` @1 (EnderReaper.java:47) and the predicate NAT<Player> @2 (:50-51). | none — `EntityMob` (EnderReaper.java:27) | EnderReaper.java:47, :50-51 | as row 2 (engine; V13) | **B-inert: nothing to delete** — no code, re-rate. | as row 2 | none (PitchBlackAllyTests names it as an ally species; DsbOutcome/HitboxDims/Recipe/StructureTestsB — not the gate). |
| 6 | HerculesBeetle | as row 2 — `HurtByTargetGoal` @1 (EntityHerculesBeetle.java:78); ENT-S-108 `selectTarget` :202 / `setTarget` :219. | none — `EntityMob` (HerculesBeetle.java:38) | EntityHerculesBeetle.java:78 | as row 2 (engine; V13) | **B-inert: nothing to delete** — no code, re-rate. | as row 2 | none (`HitboxDimsParityTests#s095_hercules_beetle_dims_both_modes`, IgnoreScreen/TargetScan rows — filters). |
| 7 | GammaMetroid | Three **owner / tame target goals**: `OwnerHurtByTargetGoal` @1, `OwnerHurtTargetGoal` @2, `NearestAttackableTargetGoal<Monster>(this, Monster.class, 10, true, false, e -> this.isTame())` @4 (EntityGammaMetroid.java:82, :83, :85). `HurtByTargetGoal` @3 (:84) is orig :67. **Inert:** the class has no melee goal and never reads `getTarget()`; `customServerAiStep` (:108-124, orig :235-251) looks at, chases and bites its own `findSomethingToAttack` pick, which a tamed metroid never produces (port `isSuitableTarget` :216 ← orig :278). A target these goals set sits in the slot and does nothing. | GammaMetroid.java:60-67 — tasks Swim / Mate / FollowOwner / Tempt / WanderALot / WatchClosest / LookIdle; targetTasks `EntityAIHurtByTarget(this,false)` @1 only (:67); :278 a tamed metroid targets nothing; :241-250 the pick is never stored. | EntityGammaMetroid.java:82-85 | **Commit 27b66a39** (2026-04-17, "Phase 4E: Companions & Tameables — defense AI, riding, dim-aware spawns"): "Stinky/Spyro/Leon/Camarasaurus/VelocityRaptor/Hydrolisc/GammaMetroid/ThePrince/ThePrinceTeen/ThePrinceAdult/ThePrincess now wire up OwnerHurtByTargetGoal + OwnerHurtTargetGoal + HurtByTargetGoal so they retaliate alongside their owner." and "Stinky/Spyro/Leon/GammaMetroid/Prince(es) get a tame-gated NearestAttackableTargetGoal<Monster> so they autonomously hunt hostiles once tamed (Leon flips to wild aggression when untamed)." No code comment; nothing in AUDIT_FINDINGS (ENT-D-034/035/036 are stats/drops/spawns), FIX_LOG, MODERNIZATION_NOTES, KNOWN_ISSUES or CHANGELOG (:284-287 lists "taming, moods, jealousy, dances" — no owner defence). | **B (recommended)** — the commit states a reason, but the stated behaviour does not exist at HEAD (the slot is unread): a MOD record would record a non-feature. Flagged §4.4 with the other Phase 4E rows. Classic = delete :82, :83, :85; keep :84. | none — removal makes the metroid no less safe (nothing changes); the owner is never a target (`TamableAnimal.canAttack` :181-183). | **none.** `CreativeMappingParityTests` :194/:244 and `IgnoreScreenParityTests` row 9 gammametroid_266 reach `isSuitableTarget` by reflection on a frozen, untamed metroid; `EntityLogicTestsA` :875-916 is the Cephadrome's filter against an untamed metroid; LootTests / RecipeTests / LeonTargetingTests (species list) — none touch the selector. |
| 8 | Godzilla | Two **prey exclusion lines** in `isSuitableTarget`: `if (target instanceof Mothra) return false;` (:597) and `if (MyUtils.isBigBoss(target) \|\| MyUtils.isRoyalty(target)) return false;` (:598), comment :594-596. **Effective additions:** PitchBlack and Kraken (`isBigBoss`, util/MyUtils.java:75-80 — its Godzilla / GodzillaHead members duplicate :589-590) and the nine royals (`isRoyalty`, MyUtils.java:9-19: TheKing, TheQueen, KingHead, QueenHead, ThePrince, ThePrinceAdult, ThePrincess, ThePrinceTeen, PurplePower). **The Mothra line is redundant in both trees:** Mothra extends EntityButterfly (orig Mothra.java:50-51, port :46) and the shared ignore screen refuses EntityButterfly (orig MyUtils.java:124, port :42) at :588 (orig :442-444), ahead of these lines — the survey's "Mothra … were prey in 1.7.10" does not hold for Mothra. | Godzilla.java:448-471 — the eight names (Godzilla, GodzillaHead, Creeper, Zombie, Spider, Skeleton, Ghost, GhostSkelly), then the creative player test; the 64×40×64 scan (:534) sees every living thing. | Godzilla.java:594-598 (scan :612-613) | **Code comment Godzilla.java:594-596:** "Don't pick fights with peers — Mothra, the royal couple, and other OreSpawn bosses are tracked separately so Mobzilla doesn't grief boss arenas and so co-existing bosses don't cancel each other out." **Commit a87c0649** (2026-04-17, "Phase 4F: The Boss Tier — Titan AI, spawn gating & 1.7.10 spawn-rule fidelity"): "Added Mothra, MyUtils.isBigBoss(), and MyUtils.isRoyalty() to the isSuitableTarget ignore list so co-existing bosses don't grief each other and Mobzilla doesn't tear up royal arenas." (:594's dash was re-encoded by 9158f397, a loot commit — no content change.) No AUDIT / FIX / MOD entry (the survey's "unrecorded" is right). | **A.** Key `[modern] godzillaSparesBossPeers`; classic = delete :594-598 (the orig eight remain; Mothra stays refused by :588). | none — no player, owner or Peaceful term (the creative gate :599-601 is separate). | **none.** `IgnoreScreenParityTests` row 11 godzilla_442 runs `isSuitableTarget` by reflection with an ignore-list species and a pig control — unaffected; CrashReproTests (doJumpDamage), BossDims / ConfigGate / KrakenPlayNicely / HitboxDims / HitboxPart / LootTests / SpawnGate / ProjectileTypeParity — not the filter. |
| 9 | Leon | **Owner goals + a tame predicate:** `OwnerHurtByTargetGoal` @1 and `OwnerHurtTargetGoal` @2 (EntityLeon.java:157-158 — the survey's :155-156), and the predicate `e -> !this.isTame() \|\| this.getTarget() == null` on the `NearestAttackableTargetGoal<Monster>` @4 (:160 — the survey's :158). **Live:** `flyWithRider` (:493-506) reads `getTarget()` first and bites it within 9 + width/2 on its 1-in-7 roll, so a tamed Leon avenges and defends its owner through these goals; the predicate stops the tamed monster hunt from overwriting a held target (e.g. the `hurt`-set one, :412). | Leon.java:92-95 — `if (PlayNicely == 0)` `EntityAINearestAttackableTarget(this, EntityLiving.class, 0, true, false, IMob.mobSelector)` @1; `EntityAIHurtByTarget(this,false)` @2; **no owner goals, no tame predicate** (1.7.10's `EntityAITarget` only excludes the owner and tamed pets when the task owner is tamed). | EntityLeon.java:157-158, :160; consumer :493-506 | **Commit 27b66a39** (row 7's quotes, incl. "(Leon flips to wild aggression when untamed)"). ENT-S-110 (AUDIT_FINDINGS.md:7087-7105) rewrote `isSuitableTarget` only and observed "EntityLeon.registerGoals adds its NearestAttackableTargetGoal<Monster> unconditionally where orig :92-94 added the target task only with PlayNicely off" (T1's row) — it does not mention the owner goals or the predicate. Nothing else (ENT-K-016..021 are stats/flight/damage/taming/drops/spawns). | **A** (live, commit-stated). Key `[modern] leonDefendsOwner`, a construction snapshot gating :157-158 and the predicate; classic = `HurtByTargetGoal` + `NearestAttackableTargetGoal<>(this, Monster.class, 10, true, false)` with no predicate (the PlayNicely registration gate is T1's; the scan geometry is T3c's). | none — the owner goals target the owner's enemies, never the owner (`TamableAnimal.canAttack` :181-183; `wantsToAttack` :189); dropping the predicate lets a tamed Leon's monster hunt replace a held target — monsters only. | **none.** `LeonTargetingTests` (6 tests, `spawnWithNoFreeWill` + `setNoAi` → goals stripped, `isSuitableTarget` by reflection), `CreativeMappingParityTests#s107_leon_*` (same), `IgnoreScreenParityTests` row 15 leon_403 (same), `KrakenTargetingParityTests#s100_kt_b1_leon_*` (the Kraken's filter), CoreStatTests / RenderInfoParityTests / `EntityLogicTestsA#leon_ducky_tame_untame_tempt` (stats, render, taming). Adjacent, out of scope (survey): port `hurt` :406 skips a player's damage to a tamed Leon where orig :321 applied it and skipped only the retaliation. |
| 10 | Mantis | Two **inert target goals**: `HurtByTargetGoal` @1 and `NearestAttackableTargetGoal<>(this, Player.class, true)` @2 (EntityMantis.java:67-68), comment :60-66. Nothing reads `getTarget()`: the flight/attack step reads the private `retaliationTarget` (:171, set by `hurt` :221) and its own `findSomethingToAttack` (:173); no melee goal, no `setTarget` override. | Mantis.java — **no tasks or targetTasks at all** (`isAIEnabled` true, :152); the custom scan only. | EntityMantis.java:60-68 | **Code comment EntityMantis.java:62-66:** "We still wire up modern target acquisition here so vanilla "hurt-by" retaliation and proximity aggression use getTarget() — the legacy retaliationTarget field can now be backed by the proper target slot, letting it appear correctly in the boss pathfinding HUD." **Commit b5aeb82a** (2026-04-17, "Phase 4B: Giant Bugs & Crawlers — combat AI, spawning, and Spit Bug acid"): "EntityMantis keeps its bespoke flight AI (aerial pathing doesn't fit the melee framework) but gains modern target-acquisition goals so retaliation/proximity aggression use getTarget() rather than its private retaliationTarget field." **Both describe an effect that does not exist at HEAD:** `getTarget()` is never read in the class, `retaliationTarget` is still the field read (:171), and no "pathfinding HUD" exists anywhere in src/main (the comment is the only hit). | **B** — a stale reason for an inert addition; classic = delete :60-68 (orig registers nothing; `registerGoals` becomes empty). Flagged §4.3 only as "documented-but-false". | none — nothing observable changes; the Mantis keeps its own scan (a Monster: engine despawn; its creative token is the ENT-S-109/T8 business). | **none.** `EntityLogicTestsA#mantis_claw_silent_drain` (the item), `HitboxDimsParityTests#s095_mantis_dims_both_modes`, DsbOutcome / StructureTestsC / CoreStatTests — not the selector; `TargetScanParityTests`' "no NearestAttackableTargetGoal remains" runs only over its nine ENT-S-108 hunters (Mantis is not one). |
| 11 | Pointysaurus | A **stare goal**: `PointysaurusStareGoal` @2 on the target selector (Pointysaurus.java:60-65; ai/PointysaurusStareGoal.java:25-97) — `canUse` takes `level.getNearestPlayer(TargetingConditions.forCombat().range(32).selector(isAngryAt))`: the nearest player within 32 that is not creative / spectator (:48), whose view vector's dot with the player→mob-eye vector exceeds 0.97 (:49-57), with mob→player line of sight (:58); ticks the look control at the starer and after a 5-tick delay `setTarget(player)` (:69, :90-95); continues while alive, within 32 and still staring (:78-83). The `DinosaurMeleeAttackGoal` @1 (:54-55) then chases and bites the slot. (The :48 `isCreative()` is the T8 row: "correct" per AUDIT_FINDINGS.md:7063.) | Pointysaurus.java:50-55 — **no such rule**; aggression only from the 1-in-6 player scan over 12×5×12 (:183, :253) and revenge (:55). | Pointysaurus.java:60-65; ai/PointysaurusStareGoal.java:32-95 | **Javadoc PointysaurusStareGoal.java:20-23:** "The 1.7.10 Pointysaurus had no such mechanic — its aggression was purely proximity / damage-based — so this is a modern enhancement layered on top to give it the "you angered the predator by making eye contact" feel the Phase 10 brief asked for." **Comment Pointysaurus.java:60-64:** "Phase 10 — Enderman-style eye-contact aggression. Runs at priority 2 so it overrides the regular proximity targeting; if you stare at the Pointysaurus it locks onto you. Wider-radius proximity aggro still exists at priority 3 as a fallback so it isn't completely passive when you mind your business but get too close." **Commit 21b8d0e8** (2026-04-18, "Phase 10 - The Gadgets, Gems, & Genetics"): "Pointysaurus: - New PointysaurusStareGoal modelled on EndermanLookForPlayerGoal: scans 32-block radius, 0.97 dot-product cone, line-of-sight gated. Locks the staring player as the combat target after a 5-tick aggro delay; downstream NearestAttackableTargetGoal keeps the pursuit going". The "Phase 10 brief" is not in the repository; Phase 10's other inventions (hoverboard, kyanite / tourmaline, Ancient Dried Egg) were removed under the no-procedural-fabrication ruling (AUDIT_FINDINGS.md:1194; MOD-009, MOD-013, MOD-020; PN-009). | **A** by the letter (three documented statements) — flagged §4.2: the Phase 10 precedent is removal, and the javadoc itself calls it "a modern enhancement". Key `[modern] pointysaurusStareAggro`, construction snapshot; classic = :60-65 not registered (the class may stay unregistered or be deleted). | none in either direction — the goal refuses creative / spectator (:48) and Peaceful players (`forCombat` → `canAttack`; a Monster, engine despawn); removal only lowers aggression against survival players. | **none.** `IgnoreScreenParityTests` row 19 pointysaurus_227 reads only the `NearestAttackableTargetGoal`s off the selector (`nearestAttackableTargetGoals`, :501-509) on a `spawnWithGoals` + noAi hunter — the stare goal is a plain `Goal`, skipped; removing it leaves the NAT @3 (:69-70) and the row unchanged. RecipeTests — no. |
| 12 | Spyro | **Four target goals with no orig counterpart:** `OwnerHurtByTargetGoal` @1, `OwnerHurtTargetGoal` @2, `HurtByTargetGoal` @3, `NearestAttackableTargetGoal<>(this, Monster.class, 10, true, false, e -> this.isTame())` @4 (EntitySpyro.java:101-104). **Inert:** the attack step reads its own `findSomethingToAttack` (:381-399) and never `getTarget()`; the only slot access is the 1-in-200 `setTarget(null)` (:334). | Spyro.java:73-81 — tasks only (Swim, AvoidEntity, FollowOwner, Tempt, Panic, WatchClosest, Wander, LookIdle, MoveIndoors); **no targetTasks**. | EntitySpyro.java:101-104 | **Commit 27b66a39** (row 7's quotes). Nothing else (ENT-S-027/028/029 are evolution / drops / spawns). | **B (recommended)** — inert; the documented "retaliate alongside their owner" does not happen. Classic = delete :101-104. Flagged §4.4. | none. | **none.** `EntityLogicTestsA` :1153-1163 (a diamond turns a tamed Dragon into a tamed Spyro), `KrakenTargetingParityTests#s100_kt_b1_spyro_rejected` (the Kraken's filter), LeonTargetingTests (species list), LootTests, RecipeTests — none touch the selector. |
| 13 | Stinky | as row 12 (EntityStinky.java:102-105); inert — attack step :330-344 reads its own pick; slot only cleared at :250. | Stinky.java:67-77 — tasks only; **no targetTasks**. | EntityStinky.java:102-105 | **Commit 27b66a39.** Nothing else (ENT-S-033/034/035 are economy / drops / spawns). | **B (recommended)** — classic = delete :102-105. Flagged §4.4. | none. | **none.** `LootTests#i059_d4_pet_drop_tables_and_stinky_kills` rolls loot tables and kills the Stinky with a player (`killByPlayer`), `StructureTestsB#i160_stinky_house_content_and_stats`, DsbOutcome / LeonTargetingTests / RecipeTests — none touch the selector. |
| 14 | ThePrince | as row 12 (ThePrince.java:114-117); inert — the hunt reads `findSomethingToAttack` (:352, bites :373); no `getTarget()` read in the class; the 1-in-200 forgiveness clears `lastHurtByMob` (:240 ← orig :513-515), not the slot. | ThePrince.java:86-92 — tasks only; **no targetTasks**. | ThePrince.java:114-117 | **Commit 27b66a39.** Nothing else (MOD-003 is the flight; BOSS-024 the prey list). | **B (recommended)** — classic = delete :114-117. Flagged §4.4. ThePrinceAdult :142-143 / ThePrinceTeen :153-154 carry the same two owner goals from the same commit (the survey parks them in T3c) — the same disposition should follow them. | none. | **none.** `EntityLogicTestsB#i092_baby_prince_prey_targeting` (:1476) drives a `TestPrince` subclass's `ai()` — the custom scan — against frozen prey, no goal involved; `KrakenTargetingParityTests` teen/adult rows (the Kraken's filter, other classes); LootTests i060; SpawnGateTests; ProjectileTypeParityTests (the `isRoyalty` exemption); CrashReproTests (the tame-transformation crash). |
| 15 | ThePrincess | as row 14 (ThePrincess.java:112-115); hunt :460/:481; forgiveness :227 ← orig :512-514. | ThePrincess.java:86-92 — tasks only; **no targetTasks**. | ThePrincess.java:112-115 | **Commit 27b66a39.** Nothing else. | **B (recommended)** — classic = delete :112-115. Flagged §4.4. | none. | **none.** LootTests i060, ProjectileTypeParityTests, SpawnGateTests — none touch the selector. |
| 16 | MyValentineTarget (Girlfriend) — the deferred T7 row | **The vanilla combat gates on `ValentineTargetGoal`** (Girlfriend.java:226-248, a `NearestAttackableTargetGoal` subclass registered @1 for Player and @2 for Boyfriend, :204-205): `TargetingConditions.forCombat()` (NAT :41) → `test` :73 → `LivingEntity.canAttack` :864-866 (**a Player in PEACEFUL is refused**) and `Player.canBeSeenAsEnemy` :903-905 (**`abilities.invulnerable` — creative and spectator — refused**; the T8 row), `canAttackType`, `isAlliedTo` :206-215; and `TargetGoal.canContinueToUse` :38-47 drops a held player when `canAttack` turns false (difficulty flipped mid-rampage). The port's own selector (:233-235) is orig's owner / tamed-pet rule (MyEntityAITarget.java:88-95) — parity, not part of this row. | MyValentineTarget.java:47-70 — `valentines_day != 0`, `feelingBetter == 0`, chance, box 16×4×16, `MyValentineTargetSorter`, `isSuitableTarget(e,false)` = MyEntityAITarget.java:78-129: null / self / dead; tamed owner: tamed target → false, owner → false; **Player → `valentines_day != 0`** (:96-98) — **no difficulty and no creative term.** The Girlfriend is an EntityTameable (Girlfriend.java:58-59) that persists in Peaceful: on Feb 14, 1.7.10's angry Girlfriend took players in Peaceful and creative players. | Girlfriend.java:204-205, :226-248; consumers :316-337 (melee via `doHurtTarget`), :385-400 (UltimateArrow) | **none found** — the goal's javadoc (:219-225) cites only the owner rule; FIX_LOG.md:975-979 ("MyValentineTarget goal (players + Boyfriends while angry, owner/tamed-pet filtered)"), AUDIT_FINDINGS.md:5191 and commit d65b9b11 (2026-07-02, Phase D4) say nothing about Peaceful or creative. The survey names it "a port-only gate" (T7 :1666) and "the Girlfriend's port-only creative refusal" (T8). | **By the letter B; recommended A on safety** (§4.5). Keep the refusal; record it as a MOD entry. If the owner wants it switchable: key `[modern] girlfriendValentineSafetyGates` (default true), classic = a `TargetingConditions.forNonCombat().range(16).selector(...)` override of `findTarget` and `canContinueToUse` that restores :96-98 (players taken in Peaceful and creative alike). My recommendation is to keep it unconditional (a recorded deviation, no key) — T7 wave 1 restored Peaceful gates "safety first" in the other direction. | **YES:** removal makes a TamableAnimal that persists in Peaceful hunt players there on Feb 14, and hunt creative players. The engine zeroes her melee damage in Peaceful (`Player.hurt` :875-878; `Mob.doHurtTarget` is `mobAttack`, which scales — `DamageSource` :96-102), but the chase, the aggression and the UltimateArrow path (:385-400; its damage type unverified here) are not covered, and nothing covers creative players. Recommend the gate stays regardless of documentation. | **none.** `DateGateTests#valentine_girlfriend_state` (:241) checks 800 HP / 2.5×8.0 under a Feb-14 clock only; EntityLogicTestsA (Bertha, cage), DsbOutcome / StructureTestsB / SpawnGate / Recipe — no. A pin: the `CephadromeGateTests` shape under `withClock(VALENTINES, …)` with a survival mock player, difficulty flipped inside the test and restored in a finally, plus the CreativeMappingParityTests triple. |

## 2. List A — proposed MOD records behind the modern config

Each key registers under `[modern]` (OreSpawnConfig.java:431), is listed in the master's comment (:433-443) and javadoc (:191-198), and is read only through a `master && key` helper beside `mothraWideRootHitbox()` / `fireRespectsMobGriefing()` (:470-487). Defaults follow the MOD-029 / MOD-031 precedent (on) unless the owner rules otherwise; classic (`enabled = false`, or the key off) is the orig transcription in every case.

| # | record | key | reason (source) | gated code | classic restoration | user-visible | read shape | proposed pin |
|---|---|---|---|---|---|---|---|---|
| A1 | Godzilla spares boss peers (row 8) | `godzillaSparesBossPeers` | Godzilla.java:594-596 comment; commit a87c0649 | Godzilla.java:594-598 | delete the two lines (the orig eight :589-593 remain; Mothra stays refused by the ignore screen :588 in both modes) | yes — with the key off / classic, Mobzilla hunts the Nightmare, the Kraken, the King / Queen (and heads), the Prince family and the Purple Power inside its 64×40×64 box, as 1.7.10 did | live (a filter; MOD-031's impact-read shape) | `isSuitableTarget` by reflection (the IgnoreScreenParityTests row-11 rig, `empty_tall`) with a PitchBlack and a TheKing: refused in modern, accepted in classic; a pig control both ways |
| A2 | Leon defends its owner (row 9) | `leonDefendsOwner` | commit 27b66a39 | EntityLeon.java:157-158 and the :160 predicate | `HurtByTargetGoal` + `NearestAttackableTargetGoal<>(this, Monster.class, 10, true, false)` — no owner goals, no predicate (orig Leon.java:92-95; the PlayNicely registration gate is T1's, the geometry T3c's) | yes — a tamed Leon avenges / defends its owner (modern) or does not (classic) | construction snapshot (goals register in the ctor; BOSS-017) | a tamed Leon whose owner is hit by a zombie: `getTarget()` == the zombie in modern, null in classic (the `OwnerHurtByTargetGoal` shape); own batch |
| A3 | Pointysaurus stare aggression (row 11) | `pointysaurusStareAggro` | PointysaurusStareGoal.java:20-23 javadoc; Pointysaurus.java:60-64 comment; commit 21b8d0e8 | Pointysaurus.java:60-65 (+ the class) | the goal is not registered (orig Pointysaurus.java:50-55 has none) | yes — a survival player who looks at a Pointysaurus within 32 blocks is attacked (modern) vs. only within the 12×5×12 scan or on hit (classic) | construction snapshot | the stare goal's `canUse` on a spawned-with-goals, noAi Pointysaurus with a survival mock player facing it at 10 blocks: true in modern; the goal absent from the selector in classic |
| A4 | Cryolophosaurus revenge chase (row 1) | `cryolophosaurusRevengeChase` | commit f5cb0ba5; ENT-A-009 (AUDIT_FINDINGS.md:171) "accepted … mapping" | Cryolophosaurus.java:57-58 (+ the :66 no-op callback) | the goal is not registered (orig :51-57 has no attack task; the revenge target is stored and never chased); the ported hunt :86-105 is untouched | marginal — after a hit from outside its 9×2×9 hunt box the port chases and bites the attacker (reach √5 + width/2, dice 1-in-12 / 1-in-14 at cadence 1-in-5, forgets 1-in-200); 1.7.10 ignored such an attacker | construction snapshot | a Cryolophosaurus hit by a mock survival player 12 blocks off: navigation target set within a few ticks in modern, none in classic |
| A5 | Girlfriend's Valentine rampage respects Peaceful and creative (row 16) — **on safety, not documentation** | `girlfriendValentineSafetyGates` — recommended **no key**: keep in both modes, record as a deviation (PARITY_NOTES / MOD) | none found (safety ruling requested, §4.5) | Girlfriend.java:226-248 (the inherited `forCombat` conditions) | if keyed: a `forNonCombat()` conditions override of `findTarget` / `canContinueToUse` restoring MyEntityAITarget.java:96-98 | yes, Feb 14 only — a Peaceful or creative player near an angry Girlfriend is left alone (port) vs. hunted (1.7.10) | live if keyed | the CephadromeGateTests shape under `withClock(VALENTINES, …)`; the CreativeMappingParityTests triple |

## 3. List B — removed from classic

No gametest pins any of these additions (§1, last column), so the deletions need no test updates; each can carry a presence assertion on the target selector (the `IgnoreScreenParityTests#nearestAttackableTargetGoals` enumeration shape, :501-509 — for the owner goals, enumerate `WrappedGoal.getGoal()` classes) if the owner wants a pin per removal. Being inert, none of B1–B6 changes anything a player sees.

| # | species (row) | delete | classic becomes | orig cite | note |
|---|---|---|---|---|---|
| B1 | GammaMetroid (7) | EntityGammaMetroid.java:82, :83, :85 | `HurtByTargetGoal` @1 only (renumber :84 to priority 1) | GammaMetroid.java:67 | inert at HEAD; commit-documented (27b66a39) but the feature does not exist — §4.4 |
| B2 | Mantis (10) | EntityMantis.java:60-68 | an empty `registerGoals` (or none) | Mantis.java — no tasks | the comment's reason is false at HEAD (no `getTarget()` read, no HUD) — §4.3 |
| B3 | Spyro (12) | EntitySpyro.java:101-104 | no target goals | Spyro.java:73-81 | inert; §4.4 |
| B4 | Stinky (13) | EntityStinky.java:102-105 | no target goals | Stinky.java:67-77 | inert; §4.4 |
| B5 | ThePrince (14) | ThePrince.java:114-117 | no target goals | ThePrince.java:86-92 | inert; §4.4; ThePrinceAdult :142-143 / ThePrinceTeen :153-154 (T3c) ride with it |
| B6 | ThePrincess (15) | ThePrincess.java:112-115 | no target goals | ThePrincess.java:86-92 | inert; §4.4 |
| B7 | DungeonBeast, EmperorScorpion, EnderKnight, EnderReaper, HerculesBeetle (2–6) | **nothing** — no port-written code exists | unchanged | `EntityMob` in all five | the engine's `canAttack`, rated "MATCH (engine, P6)" on every other Monster row (V13); recommend the five rows are re-rated, not coded. **Never** add a `canAttack` override (§1 safety flag). |

Counts: A = 5 rows (A1–A4 by documentation, A5 by safety); B = 11 rows (6 deletions, B1–B6; 5 engine rows with nothing to delete, B7).

## 4. Needs the owner's ruling (documentation ambiguous)

1. **Cryolophosaurus (row 1, A4).** The only documentation is generic (commit f5cb0ba5: the Phase 4C goal-based mapping for all eight dinosaurs) and another species' acceptance (ENT-A-009, the Alosaurus, where the scan hands its pick to the goal); here the goal duplicates the ported orig chase and only ever acts on a revenge target. A record for a marginal, duplicated chase versus a one-line deletion — the owner's call; presented as A by the letter.
2. **Pointysaurus (row 11, A3).** Documented three times, but by the "Phase 10 brief", whose other inventions were removed (hoverboard AUDIT_FINDINGS.md:1194, kyanite MOD-009, Ancient Dried Egg MOD-013, extractor MOD-020, precedent PN-009), and whose javadoc calls it "a modern enhancement". A record (default on / off?) or the Phase 10 precedent (removal) — presented as A by the letter, default undecided.
3. **Mantis (row 10, B2).** Documented (comment + commit) but the documented effect does not exist and never did at HEAD (the slot is unread; no HUD). Presented as B; if a stale comment counts as documentation it would be a record for a non-feature.
4. **The Phase 4E six — GammaMetroid, Leon, Spyro, Stinky, ThePrince, ThePrincess (rows 7, 9, 12–15; + Adult / Teen in T3c).** One commit (27b66a39) documents them together; five are inert (the slot is unread) and one (Leon) is live. Presented as a split — Leon A2, the five B1/B3–B6. The alternative is one record "pets defend their owner" (`petsDefendOwner`) over all six with the five noted as registered-but-unconsumed, so a future consumer (a melee goal reading the slot) could light them up under the same key. Either way the classic side is the same deletion.
5. **Girlfriend's Valentine gates (row 16, A5).** Undocumented, so B by the letter; parity says remove, safety says keep — the one row where the two pull apart, and a TamableAnimal that persists in Peaceful. Recommended: keep the refusal in both modes as a recorded deviation (no key); second choice: A with the key default on. Please rule.
6. **The five engine PEACEFUL rows (rows 2–6, B7).** Not port additions; nothing to delete; the survey rates the same gate "MATCH (engine, P6)" on 13 other Monster rows. Please confirm no-action and the re-rating.

## 5. Corrections to the survey noticed on the way

- **Godzilla (row 8):** Mothra was never Mobzilla's prey in 1.7.10 — she extends EntityButterfly (orig Mothra.java:50-51) and `isIgnoreable` refuses EntityButterfly (orig MyUtils.java:124) at orig :442-444, ahead of the species chain; the port's :597 is redundant with its own :588. The row's effective additions are PitchBlack, the Kraken and the nine royals.
- **Leon (row 9):** the port cites :155-156 / :158 are :157-158 / :160 at HEAD.
- **Cryolophosaurus (row 1):** ":150-152 dice" are orig lines (port :100-101).
- **Rows 2–6:** the same vanilla `canAttack` refusal is "MATCH (engine, P6)" on the S1 Monster rows (:87, :101, :129, :143, :157, :171, :185, :199, :213, :241, :255, :274) and "N/A (P3)" at :867 — the five PORT-ONLY ratings are an S-lane inconsistency, not five port additions.

## 6. Effort

B1–B6: six deletions (S), no test updates, optional presence pins. B7: a ledger edit. A1–A4: four keys + helpers + master-comment / javadoc entries, four construction-snapshot or live reads, four MOD records, four pins (M). A5: a record (S) or a key with a conditions override (M).
