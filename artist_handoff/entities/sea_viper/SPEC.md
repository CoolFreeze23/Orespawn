# Sea Viper — `sea_viper` — artist SPEC

**This rig is NOT yet in-game: the geometry is the converter's output over the port's compiled model, proven part for part against the 1.7.10 source by the reference-geometry leg; it lands through the seam in a later slice. Bone names are FINAL — the seam, the hitbox profiles and the transcriptions find bones by name.**

Generated by `tools/artist_package.py` 0.2.9 (the full folder, 2026-09-13, fourth set, items 5, 7 and 8: every artist-tier species packaged - a rig not yet in-game from the reference leg's converter output, bone names final; the Tier-1 bosses' intended locked bones from the design's section 6 rendered in section 7 and validated against the geo; the README's priority table the full list, bosses first, closing with the deliverable count) from the repository's own sources; the paragraphs marked AUTHORED come from `tools/artist_specs/sea_viper.json` (DRAFT — authored by a seed lane from the entity code, 2026-09-13; the owner's edit pending). Every contract decision this sheet rests on was ruled by the mod's owner on 2026-09-06 (`phase_g_reports/animation_contract/open_questions.md` records each ruling); a line that cites one ("ruled 2026-09-06, Q14 (a)") states that ruling — nothing here invents an answer. The one open item is the lock reject mode (§7, README rule 6).

## 1. What this mob is (AUTHORED — draft for the owner's edit)
> **DRAFT** — DRAFT — authored by a seed lane from the entity code, 2026-09-13; the owner's edit pending. Nothing in this section is ruled: it was drafted from the entity code and the model, every claim citing its line, for the owner's edit.


A hostile sea serpent: a 12x8x6 head with an upper mouth, a hinged lower jaw, two eyes, two fangs and a three-piece forked tongue, a neck, and a body of twenty-two segments that taper from 8x8 to 2x5 over 150 units, ending in a tail tip (ModelSeaViper.java:93-228, 34 parts). 160 HP, 22 damage, 12 armour; speed 0.75 in water and 0.25 on land (SeaViper.java:59-60, :136-144, :201-207), a water-bound navigator that breathes underwater (:148-159). On a 1-in-5 tick it picks the nearest living thing in an 18-block box - a non-creative player, any monster, or the attackable animals, never another viper (:301-319, :336-357) - chases at 1.5 and, within 4.5 blocks plus half the target's width, rolls its bite on a 1-in-5 cadence (:119; DinosaurMeleeAttackGoal.Presets.seaViper; BugMeleeAttackGoal.java:159-174), landing 22 with a knockback (:209-226) and poison one time in two (SeaViperBiteGoal.java:22-31); the synched DATA_ATTACKING int (:56-57) is held at 1 while it stands in reach (BugMeleeAttackGoal.java:168). Stranded, it hunts up to twelve blocks for water at 1.33 and dries out (:277-296); in water it heals with a splash (:321-324). It spawns above y 50 in daylight, no other within 16 (:411-419; ModEntities.java:159-160). Read it as a serpent: a travelling S-wave down the whole body that stiffens into a fixed S at rest, the jaw chattering and the tongue flicking when it closes in.

## 2. Size and scale in-game

- Hitbox (EntityType dims, `ModEntities.java`): 1.5 x 2.5 blocks (wide x tall).
- Renderer scale: x1; shadow radius 1 (reference pin status: pin, `tools/reference_renderer_pins.json`).
- Rig units: 16 units = 1 block before the renderer scale, so a 2.5-block-tall hitbox is 40 rig units tall at x1.
- Texture canvas: 128 x 128 (keep it; the canvas size is checked on return).
- Notes (AUTHORED): Drawn at x1.0 of the rig (SeaViperRenderer.java:26; shadow 1.0, :28) on a 128x128 sheet (ModelSeaViper.java:229), seaviper.png (SeaViperRenderer.java:20); the hitbox is 1.5 x 2.5 (ModEntities.java:159-160). The original's ModelSeaViper(0.5f) argument was the wingspeed (ModelSeaViper.java:14), not a scale. The body is a position chain: the code places each segment 9 px along the one before it every tick (doseg, :331-343) - key the joints, never the positions.

## 3. Bone glossary (locked legacy names — readable labels beside them)

Never rename, delete or re-parent a bone: code and hitboxes reference them by name. Left/right in the labels follow the model author's own naming (the side the legacy name calls left); Blockbench mirrors X for display, so the author's left appears on your right when the mob faces you. `gait bone` = its motion is scaled by walking speed in-game (P3). `locked` = it carries or parents a hitbox part (contract §8.1). Keying a locked bone is allowed; the checker warns, and the hitbox part follows the bone in-game (the consequence, so keep such keys deliberate). Renaming, re-parenting or deleting a locked bone is refused.

This rig is NOT yet in-game: the geometry is the converter's output over the port's compiled model, proven part for part against the 1.7.10 source by the reference-geometry leg; it lands through the seam in a later slice. Bone names are FINAL — the seam, the hitbox profiles and the transcriptions find bones by name.

| bone | readable label | classic part | parent | pivot (x, y, z) | group | gait bone | locked | cubes |
|---|---|---|---|---|---|---|---|---|
| `EyeLeft` | left eye (rides the head) | EyeLeft (ModelPart of the same name) | - | (0, 18, -8) | - | no | no | 1 |
| `EyeRight` | right eye (rides the head) | EyeRight (ModelPart of the same name) | - | (0, 18, -8) | - | no | no | 1 |
| `FangLeft` | left fang (rides the head) | FangLeft (ModelPart of the same name) | - | (0, 18, -8) | - | no | no | 1 |
| `FangRight` | right fang (rides the head) | FangRight (ModelPart of the same name) | - | (0, 18, -8) | - | no | no | 1 |
| `ForkLeft` | tongue, left fork (splayed -0.436; the tongue group) | ForkLeft (ModelPart of the same name) | - | (0, 18, -8) | tongue | no | no | 1 |
| `ForkRight` | tongue, right fork (splayed +0.436; the tongue group) | ForkRight (ModelPart of the same name) | - | (0, 18, -8) | tongue | no | no | 1 |
| `Head` | head (12x8x6; yaws with the look at half the head yaw; the head group's root) | Head (ModelPart of the same name) | - | (0, 18, -8) | - | no | no | 1 |
| `MiddleTounge` | tongue, middle (the tongue group) | MiddleTounge (ModelPart of the same name) | - | (0, 18, -8) | tongue | no | no | 1 |
| `MouthBottom` | lower jaw (hinged 2 px behind the head's pivot; the jaw group) | MouthBottom (ModelPart of the same name) | - | (0, 20, -10) | jaw | no | no | 1 |
| `MouthTop` | upper mouth (rides the head) | MouthTop (ModelPart of the same name) | - | (0, 18, -8) | - | no | no | 1 |
| `Neck` | neck (8x8, pitched up 15 deg behind the head; static) | Neck (ModelPart of the same name) | - | (0, 19.5, -1) | - | no | no | 1 |
| `TailTip` | tail tip (2x5x10; the wave, lag 21 x pi/4) | TailTip (ModelPart of the same name) | - | (-1, 4, 152) | body_wave | yes | no | 1 |
| `ToungBase` | tongue, base (the tongue group; slides fore-aft with the flick) | ToungBase (ModelPart of the same name) | - | (0, 18, -8) | tongue | no | no | 1 |
| `t10` | body segment 10 (the wave, lag 9 x pi/4) | t10 (ModelPart of the same name) | - | (-10, 4, 60) | body_wave | yes | no | 1 |
| `t11` | body segment 11 (the wave, lag 10 x pi/4) | t11 (ModelPart of the same name) | - | (-8, 4, 67) | body_wave | yes | no | 1 |
| `t12` | body segment 12 (the wave, lag 11 x pi/4) | t12 (ModelPart of the same name) | - | (-2, 4, 74) | body_wave | yes | no | 1 |
| `t13` | body segment 13 (the wave, lag 12 x pi/4) | t13 (ModelPart of the same name) | - | (4, 4, 80) | body_wave | yes | no | 1 |
| `t14` | body segment 14 (6x7x10; the wave, lag 13 x pi/4) | t14 (ModelPart of the same name) | - | (8, 4, 88) | body_wave | yes | no | 1 |
| `t15` | body segment 15 (6x7x10; the wave, lag 14 x pi/4) | t15 (ModelPart of the same name) | - | (8, 4, 97) | body_wave | yes | no | 1 |
| `t16` | body segment 16 (6x7x10; the wave, lag 15 x pi/4) | t16 (ModelPart of the same name) | - | (5, 4, 105) | body_wave | yes | no | 1 |
| `t17` | body segment 17 (4-wide; the wave, lag 16 x pi/4) | t17 (ModelPart of the same name) | - | (-1, 4, 112) | body_wave | yes | no | 1 |
| `t18` | body segment 18 (the wave, lag 17 x pi/4) | t18 (ModelPart of the same name) | - | (-7, 4, 119) | body_wave | yes | no | 1 |
| `t19` | body segment 19 (the wave, lag 18 x pi/4) | t19 (ModelPart of the same name) | - | (-10, 4, 127) | body_wave | yes | no | 1 |
| `t2` | body segment 2 (8x8x10; the wave, lag 2 x pi/4) | t2 (ModelPart of the same name) | - | (0, 17, 5) | body_wave | yes | no | 1 |
| `t20` | body segment 20 (2x5x10; the wave, lag 19 x pi/4) | t20 (ModelPart of the same name) | - | (-10, 4, 136) | body_wave | yes | no | 1 |
| `t21` | body segment 21 (2x5x10; the wave, lag 20 x pi/4) | t21 (ModelPart of the same name) | - | (-7, 4, 145) | body_wave | yes | no | 1 |
| `t3` | body segment 3 (the wave, lag 2 x pi/4 - the code repeats the index) | t3 (ModelPart of the same name) | - | (0, 10, 8) | body_wave | yes | no | 1 |
| `t4` | body segment 4 (the wave, lag 3 x pi/4) | t4 (ModelPart of the same name) | - | (0, 5, 15) | body_wave | yes | no | 1 |
| `t5` | body segment 5 (the wave, lag 4 x pi/4) | t5 (ModelPart of the same name) | - | (0, 4, 23) | body_wave | yes | no | 1 |
| `t6` | body segment 6 (the wave, lag 5 x pi/4) | t6 (ModelPart of the same name) | - | (0, 4, 31) | body_wave | yes | no | 1 |
| `t7` | body segment 7 (the wave, lag 6 x pi/4) | t7 (ModelPart of the same name) | - | (-2, 4, 38) | body_wave | yes | no | 1 |
| `t8` | body segment 8 (the wave, lag 7 x pi/4) | t8 (ModelPart of the same name) | - | (-7, 4, 44) | body_wave | yes | no | 1 |
| `t9` | body segment 9 (the wave, lag 8 x pi/4) | t9 (ModelPart of the same name) | - | (-10, 4, 52) | body_wave | yes | no | 1 |
| `tBase` | body segment 1, the base (8x8x10, pitched up 30 deg; the wave's root, yawing 18 deg x the speed) | tBase (ModelPart of the same name) | - | (0, 20, -2) | body_wave | yes | no | 1 |

## 4. Current animation behaviour — plain English and the source formulas

- One gait-scaled wave with no threshold (ModelSeaViper.java:249-272; doseg :331-343): the base segment yaws 18 degrees x the walking speed at 0.65 rad/tick and every segment behind it yaws 36 degrees x the speed with a pi/4 lag per index - a wave eight segments long travelling toward the tail - each segment placed 9 px along the one before; as the speed drops the wave blends into a fixed S-curve (cos of -pi/4 x index, up to 1 rad), so a resting viper lies coiled, not straight.
- Two branches on the synched DATA_ATTACKING int (SeaViper.java:56-57; held at 1 while it stands within 4.5 blocks plus half the target's width, BugMeleeAttackGoal.java:168, dropped at :132 / :145 / :176): attacking, the lower jaw chatters 31 degrees about 0.65 rad at 0.85 rad/tick and the tongue flicks 13 degrees at 2.35 rad/tick, sliding 2.5 px in and out (:280-291); calm, the jaw breathes 4 degrees about 0.45 rad at 0.1 rad/tick and the tongue flicks 5 degrees at 0.85 rad/tick (:294-303).
- The head, upper mouth, eyes, fangs, lower jaw and tongue yaw together at half the head yaw (:312-324), the lower jaw's pivot trailing the head's by 2 px along that yaw (:318-319) and the forks splayed 0.436 rad; nothing pitches. The neck is static.
- The same wave draws it in water (0.75) and on land (0.25, SeaViper.java:59-60): setupAnim has no water branch. The bite (doHurtTarget :209-226 from the goal's swing roll, BugMeleeAttackGoal.java:170-173) and the heal splash (:321-324) are not animation events.

### 4.1 Tempo table (contract §7.1)

| group | bones | axis | omega (rad/tick) | natural period | amplitude | speed-scaled | Blockbench preview rate | source |
|---|---|---|---|---|---|---|---|---|
| body_wave | tBase, t2, t3, t4, t5, t6, t7, t8, t9, t10, t11, t12, t13, t14, t15, t16, t17, t18, t19, t20, t21, TailTip | y (and each segment's x / z position) | 0.65 | 9.666 ticks (0.483 s); VISIBLE: 4.833 ticks (0.242 s) — a rectified shape (\|cos\|) repeats every half period | the base: pi x 0.1 = 0.314 rad (18 deg) x the walking speed; every segment behind: pi x 0.2 = 0.628 rad (36 deg) x the speed, lagged pi/4 per segment index (2, 2, 3 ... 21, so eight segments per wavelength), blended toward a static S-curve of cos(-pi/4 x index) - up to 1 rad - as the speed drops to 0; each segment is placed 9 px along the previous one's yaw, foreshortened by \|cos(pitch)\|; no threshold | yes | 2.07 clip-ticks per game tick at a 1.0 s authoring length (author one visible pump: 4.14 clip-ticks per game tick) | ModelSeaViper.java:249-272 (the chain), :331-343 (doseg); wingspeed 0.5 (:14); a negative limbSwingAmount is clamped to 0 (:244-246) |
| jaw | MouthBottom | x | 0.1 | 62.832 ticks (3.142 s) | calm: pi x 0.02 = 0.063 rad (4 deg) about a 0.45 rad rest at 0.2 x 0.5 = 0.1 rad/tick; attacking: pi x 0.17 = 0.534 rad (31 deg) about a 0.65 rad rest at 1.7 x 0.5 = 0.85 rad/tick | no | 0.32 clip-ticks per game tick at a 1.0 s authoring length | ModelSeaViper.java:280-283, :292-295 |
| tongue | ToungBase, MiddleTounge, ForkLeft, ForkRight | x (and a z slide) | 0.85 | 7.392 ticks (0.37 s) | calm: pi x 0.03 = 0.094 rad (5 deg) about the rests 0.261 / 0.174 / 0.087 / 0.087 at 1.7 x 0.5 = 0.85 rad/tick, with a fore-aft slide of cos(t x 0.25) x pi x 0.05 x 16 = 2.5 px; attacking: pi x 0.07 = 0.22 rad (13 deg) at 4.7 x 0.5 = 2.35 rad/tick, the slide at 0.75 rad/tick | no | 2.71 clip-ticks per game tick at a 1.0 s authoring length | ModelSeaViper.java:285-291, :297-303, :306-309 |

The bare `walk` clip is the gait group's (`body_wave`, scaled by walking speed in-game); every other group keys `walk_<group>` / `idle_<group>` (contract §2.1).

A rectified group (`|cos|`, `|sin|`) has TWO periods: the natural 2π/ω of the underlying wave, and the visible one — half of it — at which the pump you see actually repeats; author the loop to the visible period.

Author every loop at 1.0 s (README rule 5, the phase-locked kind; ruled 2026-09-06, Q14 (a)). In-game each loop plays once per natural period whatever its length; your timeline's length is the SHAPE's timeline and does not change that (P6). Blockbench previews at the declared length, so a 1.0 s preview of a fast group shows the shape slowed down (contract §9); a `_preview` copy stretched to the natural periods is a Blockbench-only aid, never in the jar and never in a delivery (ruled 2026-09-06, Q15 (a)).

### 4.2 The math

- **body_wave**: a travelling wave runs down the whole body from the neck to the tail tip while it moves, and stiffens into a fixed S-shape as it comes to rest — `tBase.yRot = cos(t * 1.3 * 0.5) * pi * 0.1 * amount; for each segment (index f = 2, 2, 3, ..., 21): seg.z = prev.z + cos(prev.yRot) * 9 * |cos(prev.xRot)|; seg.x = prev.x + sin(prev.yRot) * 9 * |cos(prev.xRot)|; seg.yRot = cos(t * 1.3 * 0.5 - pi/4 * f) * pi * 0.2 * amount + cos(-pi/4 * f) * (1 - amount)` (ModelSeaViper.java:249-272 (the chain), :331-343 (doseg); wingspeed 0.5 (:14); a negative limbSwingAmount is clamped to 0 (:244-246))
- **jaw**: the lower jaw breathes slowly nearly shut at rest and chatters wide and fast while the attacking flag is up — `MouthBottom.xRot = attacking ? 0.65 + cos(t * 1.7 * 0.5) * pi * 0.17 : 0.45 + cos(t * 0.2 * 0.5) * pi * 0.02` (ModelSeaViper.java:280-283, :292-295)
- **tongue**: the forked tongue flicks and slides in and out - slowly at rest, fast while attacking — `f = attacking ? cos(t * 4.7 * 0.5) * pi * 0.07 : cos(t * 1.7 * 0.5) * pi * 0.03; ToungBase.xRot = 0.261 + f; MiddleTounge.xRot = 0.174 + f; ForkLeft.xRot = ForkRight.xRot = 0.087 + f; z = (attacking ? cos(t * 1.5 * 0.5) : cos(t * 0.5 * 0.5)) * pi * 0.05 * 16 added to each tongue part's initial z` (ModelSeaViper.java:285-291, :297-303, :306-309)

### 4.3 Reference clip (reference-only)

_no reference clip yet: it is sampled from the classic hook when the rig lands through the seam; until then §4.1 / §4.2 and the plain-language formulas (where the seed carries them) are the motion's description._

This creature has NO exact keyframe transcription and is not yet on its hook (the rig is not yet in-game: §3). The plain-language transcription of its source formulas (migration design section 5 — the source method and line quoted, each formula and constant in words) is authored when the rig lands on its hook; until then the seed's description, clip verdicts and wishlist (§1, §4, §5) describe the motion.

## 5. Clips: what to improve, what to leave (AUTHORED verdicts on generated rows)
> **DRAFT** — DRAFT — authored by a seed lane from the entity code, 2026-09-13; the owner's edit pending. Nothing in this section is ruled: it was drafted from the entity code and the model, every claim citing its line, for the owner's edit.


| clip | loop | layer | weight / trigger | bones it may animate | length | code-triggered | verdict | notes |
|---|---|---|---|---|---|---|---|---|
| `idle` | true | base | w_idle = (1 - w_move)(1 - w_swim)(1 - w_fly) | any of the 34 bones — gait group (speed-scaled): TailTip, t10, t11, t12, t13, t14, t15, t16, t17, t18, t19, t2, t20, t21, t3, t4, t5, t6, t7, t8, t9, tBase; free: EyeLeft, EyeRight, FangLeft, FangRight, Head, MouthTop, Neck; other groups' bones: ForkLeft (better left to `idle_tongue`), ForkRight (better left to `idle_tongue`), MiddleTounge (better left to `idle_tongue`), MouthBottom (better left to `idle_jaw`), ToungBase (better left to `idle_tongue`) | 1 s | no | author | REQUIRED with walk: the gate opens on the pair (today nothing ships; the classic code plays) — the rest: the body settled into its S-curve (ModelSeaViper.java:342, the (1 - amount) term), the jaw's slow breath (:294-295), the tongue's slow flick (:297-303) |
| `idle_jaw` | true | parallel layer | w_idle = (1 - w_move)(1 - w_swim)(1 - w_fly) (group jaw) | MouthBottom | 1 s | no | author |  |
| `idle_tongue` | true | parallel layer | w_idle = (1 - w_move)(1 - w_swim)(1 - w_fly) (group tongue) | ToungBase, MiddleTounge, ForkLeft, ForkRight | 1 s | no | author |  |
| `walk` | true | base | w_walk = w_move (1 - w_swim)(1 - w_fly); the gait group additionally x limbSwingAmount (P3) | any of the 34 bones — gait group (speed-scaled): TailTip, t10, t11, t12, t13, t14, t15, t16, t17, t18, t19, t2, t20, t21, t3, t4, t5, t6, t7, t8, t9, tBase; free: EyeLeft, EyeRight, FangLeft, FangRight, Head, MouthTop, Neck; other groups' bones: ForkLeft (better left to `walk_tongue`), ForkRight (better left to `walk_tongue`), MiddleTounge (better left to `walk_tongue`), MouthBottom (better left to `walk_jaw`), ToungBase (better left to `walk_tongue`) | 1 s | no | author | REQUIRED with idle: the locomotion loop — the travelling body wave at 0.65 rad/tick, 36 degrees x the walking speed with a pi/4 lag per segment (:249-272, :331-343); on land this is the 0.25 crawl toward water (SeaViper.java:277-296) |
| `walk_jaw` | true | parallel layer | w_walk = w_move (1 - w_swim)(1 - w_fly); the gait group additionally x limbSwingAmount (P3) (group jaw) | MouthBottom | 1 s | no | author |  |
| `walk_tongue` | true | parallel layer | w_walk = w_move (1 - w_swim)(1 - w_fly); the gait group additionally x limbSwingAmount (P3) (group tongue) | ToungBase, MiddleTounge, ForkLeft, ForkRight | 1 s | no | author |  |
| `swim` | true | base | w_swim from isInWater() | any of the 34 bones — gait group (speed-scaled): TailTip, t10, t11, t12, t13, t14, t15, t16, t17, t18, t19, t2, t20, t21, t3, t4, t5, t6, t7, t8, t9, tBase; free: EyeLeft, EyeRight, FangLeft, FangRight, Head, MouthTop, Neck; other groups' bones: ForkLeft (better left to `swim_tongue`), ForkRight (better left to `swim_tongue`), MiddleTounge (better left to `swim_tongue`), MouthBottom (better left to `swim_jaw`), ToungBase (better left to `swim_tongue`) | 1 s | no | author | its element: a water-bound navigator (SeaViper.java:148-151) with a swimming move control (:100-101), 0.75 speed in water (:59, :205-206) and a RandomSwimmingGoal (:120); the code draws the same wave in water (no water branch) — author the swim as the full-speed serpentine and leave walk to the land crawl; optional: falls back to walk then idle when absent (section 2.4) |
| `calm_idle` | true | base | = idle until this creature's SPEC adds a synched attacking byte (ruled 2026-09-06, Q12 (a)) | (as idle) | 1 s | no | author | the flag at 0 (BugMeleeAttackGoal.java:132, :145, :176): the jaw's 4-degree breath and the slow tongue (ModelSeaViper.java:294-303) — the same rest as idle |
| `attack` | false | triggered controller | event: a strike (the transport per species by the trigger inventory — ruled 2026-09-06, Q11 (a); §6 says which applies here) | (any unlocked) | 0.5 s | yes | author | the bite: doHurtTarget (SeaViper.java:209-226) for 22 with a 0.8 / 0.14 knockback and poison one time in two for 6 s (8 on Easy; SeaViperBiteGoal.java:22-31), rolled within 4.5 blocks plus half the target's width on a 1-in-5 cadence tick (BugMeleeAttackGoal.java:159-174; Presets.seaViper: nextInt(2) == 0 or nextInt(4) == 1); the flag is held while in reach, not pulsed at the strike, so the strike is transport 1 (the server LivingDamageEvent.Post) — a strike of the head with the jaw snapping shut |
| `hurt` | false | triggered controller | event: hurtTime rising edge (client-observed); the red overlay stays (ruled 2026-09-06, Q4 (a)) | (any unlocked) | 0.4 s | yes | author | a flinch: hurt() (SeaViper.java:228-250) refuses cactus and other vipers, takes one hit per 5 ticks (:235-238) and turns on a mob attacker (:245-248); the seaviper_hit sound (:398-401); the hurtTime edge is client-observed |
| `death` | hold_on_last_frame | triggered controller | event: deathTime > 0; the vanilla death flip stays — the pilot ships no death clip (ruled 2026-09-06, Q3 (a)); a creature whose JSON ships one gets the clip-replaces-flip mode when the first such clip arrives, so a `death` delivered here plays under the flip until then | (any unlocked) | 1.5 s | yes | author | ruled 2026-09-06, Q3 (a): the vanilla death flip stays; a creature whose JSON ships a `death` clip gets the clip-replaces-flip mode, designed when the first such clip arrives (the Queen's own death clip the precedent) - a `death` delivered here plays under the flip until then |
| `hiss` | false | overlay | getAmbientSound (SeaViper.java:389-396): the vanilla ambient roll answers seaviper_living one time in two; a sound the server plays, no animation hook today — a named site (transport 3) when the rig lands | Head, MouthTop, MouthBottom, ToungBase, MiddleTounge, ForkLeft, ForkRight, FangLeft, FangRight | 0.8 s | no | author | a rear of the head with the jaw wide and the tongue out |
| `splash` | false | overlay | customServerAiStep (SeaViper.java:321-324): in water, hurt, on a 1-in-100 tick GENERIC_SPLASH at 1.5 volume and a heal of 1; a server-side roll with no packet today — a named site (transport 3) when the rig lands | tBase, t2, t3, t4, t5, t6, t7, t8, Head | 0.5 s | no | author | a roll of the forebody on the splash |

Loop values are the JSON `loop` field exactly: `true` for cycles, `false` for one-shots, `hold_on_last_frame` only where written. Every name in this table is fixed (contract §2.4); `idle_alt_1`, `idle_alt_2`, ... may be added freely. A sheet may carry up to four extra clips of this creature's own beyond the contract's names (§2.3; ruled 2026-09-06, Q6 (a)) — more needs the owner's ruling; the extras rows above, if any, are those.

### 5.1 Wishlist (AUTHORED — only what `check` accepts today; anything else is marked)
> **DRAFT** — DRAFT — authored by a seed lane from the entity code, 2026-09-13; the owner's edit pending. Nothing in this section is ruled: it was drafted from the entity code and the model, every claim citing its line, for the owner's edit.


- in `swim`: the code's wave is uniform down all 22 segments (ModelSeaViper.java:331-343) — let the amplitude grow toward the tail and add a vertical undulation; the segments are yaw-only today
- in `walk` (the land crawl at 0.25, SeaViper.java:59-60): a heavier, slower serpentine with the head raised; the code plays the swim wave scaled by speed
- in `attack`: a strike — the head is look-only (:312-324) and the neck static (:97-100); a coil-and-lunge with the jaw snapping on the bite (SeaViper.java:209-226)
- in `aggro_idle`: the coiled S-curve the code holds at rest (:342) with the head raised and weaving; keep the 31-degree chatter (:282-283) and the fast tongue (:285-291) — NOT accepted by `check` today: `aggro_idle` is not in this creature's clip set

## 6. Trigger inventory (generated from the entity's AI goals and state flags)

Source: `src/main/java/danger/orespawn/entity/SeaViper.java` (Monster)

| selector | prio | goal | guard | category | what it does | bears on |
|---|---|---|---|---|---|---|
| goalSelector | 0 | `FloatGoal` | - | locomotion | bobs up to the surface in water (vanilla) | swim state (client reads isInWater) |
| goalSelector | 1 | `SeaViperBiteGoal` | - | UNCLASSIFIED | no entry in the generator's goal dictionary | unknown |
| goalSelector | 2 | `RandomSwimmingGoal` | - | UNCLASSIFIED | no entry in the generator's goal dictionary | unknown |
| goalSelector | 3 | `MyEntityAIWanderALot` | - | locomotion | OreSpawn's restless wander: picks a new spot often | walk |
| goalSelector | 4 | `LookAtPlayerGoal` | - | look | turns the head toward a nearby player (vanilla; head yaw/pitch only) | none: head look, not a clip |
| goalSelector | 5 | `LookAtPlayerGoal` | - | look | turns the head toward a nearby player (vanilla; head yaw/pitch only) | none: head look, not a clip |
| goalSelector | 6 | `RandomLookAroundGoal` | - | look | looks around idly (vanilla; head yaw/pitch only) | none: head look, not a clip |
| targetSelector | 1 | `HurtByTargetGoal` | - | targeting | retaliates against whoever hurt it (vanilla target selector) | aggro state (through the attacking flag where one exists) |

A `[modern: key]` guard means the goal is registered only under the modern config key named (read once, at construction); `UNPARSED` means the registration's shape is one the generator does not read (a local variable or a computed priority) — the owner reads that line.

**Synched state flags** (what the client can see): `DATA_ATTACKING` (Integer, line 56)

**Attacking flag `DATA_ATTACKING`** — classified **NONE** (no setAttacking sites) — a mechanical reading; the owner confirms it against the sites:

| line | method | sets | guard (the enclosing block) | within | comment |
|---|---|---|---|---|---|

The guard is the header of the block that actually encloses the write (found by brace depth, comments and strings blanked); an `else` branch is written as the negated `if` chain it closes; a header the parser cannot read is `(unparsed: ...)`, never dropped.

**Locomotion facts:** a walker. Overrides: hurt, doHurtTarget, aiStep, customServerAiStep.

**Strike and launch sites:**

- melee `doHurtTarget(target)` — SeaViper.java:215 in `doHurtTarget`

**What fires each contract clip:**

| clip | signal | verdict |
|---|---|---|
| `walk` | limbSwingAmount from AnimationState (the seam's input; 0 at rest, 1 at full stride) | the gait group's weight and speed scale (P3) |
| `idle` | w_idle = (1 - w_move)(1 - w_swim)(1 - w_fly) | standing still |
| `swim` | Entity.isInWater() (client-evaluated); FloatGoal keeps it at the surface | falls back to walk then idle when absent (§2.4) |
| `aggro_idle / calm_idle / attack` | DATA_ATTACKING classified NONE: no setAttacking sites | OWNER READS THE SITES (listed below) — a mechanical reading; the owner confirms which of the ruled transports applies (ruled 2026-09-06, Q11 (a) / Q12 (a)) |
| `hurt` | rising edge of LivingEntity.hurtTime (0 -> 10), client-observed | always available; the red overlay stays (ruled 2026-09-06, Q4 (a)) |
| `death` | LivingEntity.deathTime > 0, client-observed | hold_on_last_frame; the vanilla death flip stays and the pilot ships no death clip (ruled 2026-09-06, Q3 (a)); a creature whose JSON ships a `death` clip gets the clip-replaces-flip mode, designed when the first such clip arrives (the Queen's own death clip the precedent) — until then a delivered `death` plays under the flip |
| `idle_alt_N` | a roll at each idle loop boundary (p = 0.15) while w_idle > 0.9 | optional; one roll per idle loop boundary, p = 0.15, a uniform choice, only while the idle weight is above 0.9 and no triggered clip plays, keyed on the clip-clock cycle index (ruled 2026-09-06, Q5 (a)) |

## 7. Hitbox bones that must keep their names

No MultiHitboxLib profile: no locked bones. Every bone name is still immutable (the code poses bones by name).

## 8. Textures

| canonical file (edit this one) | canvas | also shipped as (fan-out on import) | referenced by the code as | note |
|---|---|---|---|---|
| `seaviper.png` | 128x128 | seavipertexture.png | seaviper.png |  |

## 9. Reference screenshots

`reference/` holds the slots listed in `reference/SLOTS.md`; the owner fills them from the dev client (fixed camera, light and state per slot). Empty until then.

## 10. Effort and priority

- Artist scope: full contract (Tier 2, the state-branching class: geckolib_migration_design.md section 5); the rig is not yet in-game (it lands through the seam in a later slice).
- Estimated effort: 23.8 h (generator: 4 h + 0.2 h/bone x 34 + 1 h/clip x 13 to author or improve (owner adjusts)).
- Tier 2 (state-branching — setupAnim branches on entity state).
- Density statement (the harness's, not the artist's; ruled 2026-09-06, Q9 (a) and Q10): the classic transcription is verified at 2.5e-3 rad — Beaver reference leg 15 / 13 / 8 catmullrom keys per bone with spline arguments repaired at load; wrap sample T−ε vs 0+ε included; the key counts per bone are an output of the harness, re-derived on every re-transcription.

## 11. What 'done' looks like for this entity

_This rig is NOT yet in-game: the geometry is the converter's output over the port's compiled model, proven part for part against the 1.7.10 source by the reference-geometry leg; it lands through the seam in a later slice. Bone names are FINAL — the seam, the hitbox profiles and the transcriptions find bones by name._

1. `reference_seaviper.geo.json` returned UNCHANGED (or not at all): every bone name, parent, pivot, rotation and cube as listed in §3 and the packaged file (the reference leg's geo; the rule is the shipped rig's) (a `locked` bone renamed, re-parented or deleted is refused by name).
2. `sea_viper.animation.json` — that exact file name, one file — (format 1.8.0) holding the clips of §5 with the loop values shown; keys on `locked` bones: Keying a locked bone is allowed; the checker warns, and the hitbox part follows the bone in-game (the consequence, so keep such keys deliberate). Renaming, re-parenting or deleting a locked bone is refused.
3. Textures at their canvas sizes under the canonical names of §8 (aliases are fanned out on import).
4. Optionally the `.bbmodel` working file. `tools/artist_package.py check <your folder>` must pass before hand-in. It REJECTS: a missing or empty folder, or a missing manifest; no `.animation.json`, one under the wrong name (the sheet names the file), or a second one; a clip not in the sheet (renamed or added; `idle_alt_N` only where the sheet allows it), or a clip delivered for a Tier-3 creature; a wrong `loop` value; a missing `idle` or `walk` (they open the game's switch only TOGETHER — one without the other leaves the creature on its code-driven motion); a missing clip the game already triggers by name; a key on a bone the rig does not have (renamed), or a channel other than rotation / position / scale; a Molang string, a non-numeric or non-finite value, a custom-instruction (`timeline`) key; an event keyframe (`sound_effects` / `particle_effects`) on a LOOPING clip of a phase-locked species — no event keyframes on loops; code-fired events come from the trigger inventory (one-shot clips may carry them; a creature whose controllers are its own GeckoLib controllers — the SPEC's controller kind `native`, the Queen — is exempt: native controllers are not phase-locked, event keys fire per loop); |rotation| > 3600 degrees or |position| > 1024 units; a key later than the clip's `animation_length` (a declared length shorter than the last key); a returned `.geo.json` whose bones, parents, pivots, rotations, cubes, UVs or canvas differ from the shipped rig; a `locked` bone renamed, re-parented or deleted in a returned `.geo.json` (the finding names the bone); a texture that is not this creature's, or whose canvas size changed; a `*_reference.animation.json` returned that is not the package's own untouched copy (the reference-only clip the package carries beside the sheet: the classic code sampled at fixed inputs, never edited, never delivered, never shipped — the finding names the file; the untouched copy coming back is warned, not a delivery). It WARNS on: a key on a `locked` bone (the clip and the bones are named) — Keying a locked bone is allowed; the checker warns, and the hitbox part follows the bone in-game (the consequence, so keep such keys deliberate). Renaming, re-parenting or deleting a locked bone is refused; a `_preview` file delivered (a Blockbench-only aid, never in the jar — ruled 2026-09-06, Q15 (a); leave it out of the delivery); a returned `.geo.json` at all (the shipped rig is used regardless — do not re-export it); a clip length far from the one the sheet states (rule 5: a phase-locked creature's loops at 1.0 s — ruled 2026-09-06, Q14 (a); a native creature's clips at their shipped length; a one-shot at the length its row states); an optional clip not delivered; an alias-named texture (the canonical name is preferred); a `format_version` other than 1.8.0; a number written as a string; an unknown keyframe key; a clip without `animation_length`; an unreadable `.bbmodel`.

