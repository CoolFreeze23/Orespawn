# Ender Knight — `ender_knight` — artist SPEC

**This rig is NOT yet in-game: the geometry is the converter's output over the port's compiled model, proven part for part against the 1.7.10 source by the reference-geometry leg; it lands through the seam in a later slice. Bone names are FINAL — the seam, the hitbox profiles and the transcriptions find bones by name.**

Generated by `tools/artist_package.py` 0.4.0 (the reference clips revised, 2026-09-14, second set revised, items 2 to 6: every state a hook reads sampled and named by the seed - reference_walk, reference_idle, reference_attack, reference_fly, reference_swim, the species' own states, an unnamed one flagged - keyed by the exact transcriptions' density search (catmullrom, 1 degree, 1/32 block), embedded in every .bbmodel within the round-trip tolerance with the files beside the sheet the pinned source; a reference clip's keys as the starting point of idle / walk / aggro_idle / fly / swim, a species state offered as a SPEC extra; only the reference file itself under its own name refused; one tracked copy, the folder's copies ignored by git) from the repository's own sources; the paragraphs marked AUTHORED come from `tools/artist_specs/ender_knight.json` (DRAFT — authored by a seed lane from the entity code, 2026-09-13; the owner's edit pending). Every contract decision this sheet rests on was ruled by the mod's owner on 2026-09-06 (`phase_g_reports/animation_contract/open_questions.md` records each ruling); a line that cites one ("ruled 2026-09-06, Q14 (a)") states that ruling — nothing here invents an answer. The one open item is the lock reject mode (§7, README rule 6).

## 1. What this mob is (AUTHORED — draft for the owner's edit)
> **DRAFT** — DRAFT — authored by a seed lane from the entity code, 2026-09-13; the owner's edit pending. Nothing in this section is ruled: it was drafted from the entity code and the model, every claim citing its line, for the owner's edit.


A towering armoured enderman: a pelvis, three spine pieces and four ribs up to a neck, collars and shoulders, a head, two three-piece arms - the right one holding a sword of handle and blade - two legs of thigh, two shin pieces and a hip, four foot pieces each, and a two-piece cape (ModelEnderKnight.java:64-103; 40 parts on a 512x512 sheet; 2.9 blocks tall). It is a hostile enderman in mail: 60 HP, 12 damage, 6 armour, speed 0.32, a 64-block follow range (MobStats.ENDER_KNIGHT, EnderKnight.java:202-210). It attacks a player who looks at it - the nearest player within 64 blocks whose gaze lines up with its chest and who has no pumpkin on (:90-144, :187-200), the enderman stare rule - or whoever hurts it (:63-74), closing with a vanilla melee goal (:56). While it has a target it SCREAMS (a synched flag, :40-41, :119, :342) and teleports: away when the starer comes within 4 blocks, toward a target more than 16 blocks away (:264-286), at random when hit by an arrow or magic (:339-349), in daylight (:243-255) and when wet or burning (:257-262, :228-230); portal particles stream off it (:232-241). It spawns in darkness at night above y 30 (:366-374). Read it as an enderman knight: a long-legged stride, the cape stirring, arms folded and the sword low at rest - then, screaming, the arms thrown up and the sword whirling.

## 2. Size and scale in-game

- Hitbox (EntityType dims, `ModEntities.java`): 0.6 x 2.9 blocks (wide x tall).
- Renderer scale: x1; shadow radius 0.3 (reference pin status: pin, `tools/reference_renderer_pins.json`).
- Rig units: 16 units = 1 block before the renderer scale, so a 2.9-block-tall hitbox is 46.4 rig units tall at x1.
- Texture canvas: 512 x 512 (keep it; the canvas size is checked on return).
- Notes (AUTHORED): Drawn at x1.0 of the rig (EnderKnightRenderer.java:25; no scale override) on a 512x512 sheet (ModelEnderKnight.java:270); the hitbox is 0.6 x 2.9 (ModEntities.java:87-89); shadow 0.3 (EnderKnightRenderer.java:27).

## 3. Bone glossary (locked legacy names — readable labels beside them)

Never rename, delete or re-parent a bone: code and hitboxes reference them by name. Left/right in the labels follow the model author's own naming (the side the legacy name calls left); Blockbench mirrors X for display, so the author's left appears on your right when the mob faces you. `gait bone` = its motion is scaled by walking speed in-game (P3). `locked` = it carries or parents a hitbox part (contract §8.1). Keying a locked bone is allowed; the checker warns, and the hitbox part follows the bone in-game (the consequence, so keep such keys deliberate). Renaming, re-parenting or deleting a locked bone is refused.

This rig is NOT yet in-game: the geometry is the converter's output over the port's compiled model, proven part for part against the 1.7.10 source by the reference-geometry leg; it lands through the seam in a later slice. Bone names are FINAL — the seam, the hitbox profiles and the transcriptions find bones by name.

| bone | readable label | classic part | parent | pivot (x, y, z) | group | gait bone | locked | cubes |
|---|---|---|---|---|---|---|---|---|
| `blade` | sword blade (moves with the handle) | blade (ModelPart of the same name) | - | (4, 26, -8) | arms | no | no | 1 |
| `cape1` | cape, upper piece (static) | cape1 (ModelPart of the same name) | - | (8, 44, 3) | - | no | no | 1 |
| `cape2` | cape, lower piece (stirs with the stride and a slow sway) | cape2 (ModelPart of the same name) | - | (4, 44, 4) | cape_sway | no | no | 1 |
| `handle` | sword handle (placed 7 units along the right forearm's pitch) | handle (ModelPart of the same name) | - | (4, 26, -8) | arms | no | no | 1 |
| `head` | head (turns 0.45 of the look yaw, clamped to +-0.45 rad) | head (ModelPart of the same name) | - | (3, 42, 3) | - | no | no | 1 |
| `larm1` | left forearm (placed 10 units along the upper arm's pitch by the code; folded across at rest) | larm1 (ModelPart of the same name) | - | (-4, 42, 1) | arms | no | no | 1 |
| `larm2` | left upper arm, piece a (the shoulder hinge) | larm2 (ModelPart of the same name) | - | (-5, 42, 1) | arms | no | no | 1 |
| `larm3` | left upper arm, piece b (moves with piece a) | larm3 (ModelPart of the same name) | - | (-3, 42, 1) | arms | no | no | 1 |
| `lcollar` | left collar | lcollar (ModelPart of the same name) | - | (1, 43, 1) | - | no | no | 1 |
| `lfoot1` | left foot, piece 1 | lfoot1 (ModelPart of the same name) | - | (2, 29, -2) | gait | yes | no | 1 |
| `lfoot2` | left foot, piece 2 (rest +0.6 rad) | lfoot2 (ModelPart of the same name) | - | (2, 29, -2) | gait | yes | no | 1 |
| `lfoot3` | left foot, piece 3 | lfoot3 (ModelPart of the same name) | - | (2, 29, -2) | gait | yes | no | 1 |
| `lfoot4` | left foot, piece 4 | lfoot4 (ModelPart of the same name) | - | (2, 29, -2) | gait | yes | no | 1 |
| `lhip` | left hip (static) | lhip (ModelPart of the same name) | - | (1, 28, -2) | - | no | no | 1 |
| `lleg1` | left thigh | lleg1 (ModelPart of the same name) | - | (1, 29, -2) | gait | yes | no | 1 |
| `lleg2` | left shin, piece a (rest -0.1 rad) | lleg2 (ModelPart of the same name) | - | (0, 29, -2) | gait | yes | no | 1 |
| `lleg3` | left shin, piece b (rest -0.1 rad) | lleg3 (ModelPart of the same name) | - | (2, 29, -2) | gait | yes | no | 1 |
| `lshoulder` | left shoulder | lshoulder (ModelPart of the same name) | - | (-2, 45, 0) | - | no | no | 1 |
| `neck` | neck | neck (ModelPart of the same name) | - | (6, 44, 0) | - | no | no | 1 |
| `pelvis` | pelvis | pelvis (ModelPart of the same name) | - | (5, 30, -2) | - | no | no | 1 |
| `rarm1` | right forearm (placed 10 units along the upper arm's pitch; holds the sword) | rarm1 (ModelPart of the same name) | - | (12, 42, 2) | arms | no | no | 1 |
| `rarm2` | right upper arm, piece a (the shoulder hinge) | rarm2 (ModelPart of the same name) | - | (13, 42, 1) | arms | no | no | 1 |
| `rarm3` | right upper arm, piece b (moves with piece a) | rarm3 (ModelPart of the same name) | - | (11, 42, 1) | arms | no | no | 1 |
| `rcollar` | right collar | rcollar (ModelPart of the same name) | - | (11, 43, 1) | - | no | no | 1 |
| `rfoot1` | right foot, piece 1 | rfoot1 (ModelPart of the same name) | - | (8, 29, -2) | gait | yes | no | 1 |
| `rfoot2` | right foot, piece 2 (rest +0.6 rad) | rfoot2 (ModelPart of the same name) | - | (8, 29, -2) | gait | yes | no | 1 |
| `rfoot3` | right foot, piece 3 | rfoot3 (ModelPart of the same name) | - | (8, 29, -2) | gait | yes | no | 1 |
| `rfoot4` | right foot, piece 4 | rfoot4 (ModelPart of the same name) | - | (8, 29, -2) | gait | yes | no | 1 |
| `rhip` | right hip (static) | rhip (ModelPart of the same name) | - | (7, 28, -2) | - | no | no | 1 |
| `rib1` | rib 1 (top) | rib1 (ModelPart of the same name) | - | (6, 40, 0) | - | no | no | 1 |
| `rib2` | rib 2 | rib2 (ModelPart of the same name) | - | (6, 37, 1) | - | no | no | 1 |
| `rib3` | rib 3 | rib3 (ModelPart of the same name) | - | (5, 35, 1) | - | no | no | 1 |
| `rib4` | rib 4 (bottom) | rib4 (ModelPart of the same name) | - | (5, 33, 1) | - | no | no | 1 |
| `rleg1` | right thigh | rleg1 (ModelPart of the same name) | - | (7, 29, -2) | gait | yes | no | 1 |
| `rleg2` | right shin, piece a (rest -0.1 rad) | rleg2 (ModelPart of the same name) | - | (8, 29, -2) | gait | yes | no | 1 |
| `rleg3` | right shin, piece b (rest -0.1 rad) | rleg3 (ModelPart of the same name) | - | (6, 29, -2) | gait | yes | no | 1 |
| `rshoulder` | right shoulder | rshoulder (ModelPart of the same name) | - | (14, 45, 0) | - | no | no | 1 |
| `spine1` | spine, lower piece (-17 deg) | spine1 (ModelPart of the same name) | - | (4, 33, 1) | - | no | no | 1 |
| `spine2` | spine, middle piece | spine2 (ModelPart of the same name) | - | (4, 37, 1) | - | no | no | 1 |
| `spine3` | spine, upper piece (11 deg) | spine3 (ModelPart of the same name) | - | (4, 41, 0) | - | no | no | 1 |

## 4. Current animation behaviour — plain English and the source formulas

- One gait rhythm on the legs (ModelEnderKnight.java:276-291): above a walking speed of 0.1 the cosine of the age x 1.3 x 0.21 (wingspeed, :21), x pi x 0.25, x the walking speed is added to every piece of the left leg (thigh, two shin pieces around -0.1 rad, four foot pieces, the second around +0.6) and subtracted from the right - a 45-degree stride at full speed every 23 ticks, each leg one rigid unit; the cape's lower piece rolls by a quarter of it. Below the threshold the legs snap to their rests.
- The cape's lower piece also stirs at all times (:292): the cosine of the age x 0.7 x 0.21, x pi x 0.02 - 3.6 degrees every 42.7 ticks; the upper piece is static.
- The head turns 0.45 of the look yaw, clamped to +-0.45 rad (:293-299), no pitch.
- The arms and sword branch on the synched screaming flag (:300-322). Calm: the upper arms at -0.5 rad, the forearms at -1.0 and yawed +1.0 (left) and -1.0 (right) - folded across the chest - the sword handle and blade at 0.35; nothing moves. Screaming: the cosine of the age x 2.7 x 0.21, x pi x 0.3 (54 degrees every 11.1 ticks) is added to -1.2 on the four upper-arm pieces and to -1.8 on the forearms, both arms together, and one and a half times it to 0.5 on the sword - the arms thrown high and pumping, the sword whirling 81 degrees. Each forearm is placed 10 units along its upper arm's pitch and the sword 7 units along the right forearm's (:323-328): a hand-written FK chain on flat bones (when the rig lands a keyframed swing must key the positions too, or the arms must be parented).
- The pelvis, spine, ribs, neck, collars, shoulders, hips and upper cape never move. The screaming flag is a held state (EnderKnight.java:119 on a stare, :342 on a hit; cleared at :124, :252, :260, :283) under its own name, which the generator's inventory reads as the held aggro state (TEST-011 (c), owner 2026-09-14, item 5: no attacking flag, a vanilla MeleeAttackGoal, the flag raised in the target goal's findTarget and cleared on target loss); the strike is vanilla's MeleeAttackGoal (:56), whose doHurtTarget call is in vanilla code - the inventory records it as the strike site now.
- The teleports (EnderKnight.java:290-337) are instant position changes with portal particles and the enderman teleport sound at both ends; the body itself does not animate them.

### 4.1 Tempo table (contract §7.1)

| group | bones | axis | omega (rad/tick) | natural period | amplitude | speed-scaled | Blockbench preview rate | source |
|---|---|---|---|---|---|---|---|---|
| gait | lleg1, lleg2, lleg3, lfoot1, lfoot2, lfoot3, lfoot4, rleg1, rleg2, rleg3, rfoot1, rfoot2, rfoot3, rfoot4, cape2 | x (the legs) / z (the cape's quarter-swing) | 0.273 | 23.015 ticks (1.151 s) | rests 0 (thigh, foot pieces 1, 3, 4), -0.1 rad (shin pieces), +0.6 (foot piece 2), then pi x 0.25 = 0.785 rad (45 deg) x the walking speed at 1.3 x 0.21 - still below the 0.1 threshold; the cape's lower piece rolls by a quarter of it | yes | 0.87 clip-ticks per game tick at a 1.0 s authoring length | ModelEnderKnight.java:276-291 |
| cape_sway | cape2 | x | 0.147 | 42.743 ticks (2.137 s) | pi x 0.02 = 0.063 rad (3.6 deg), at all times | no | 0.47 clip-ticks per game tick at a 1.0 s authoring length | ModelEnderKnight.java:292 |
| arms | larm1, larm2, larm3, rarm1, rarm2, rarm3, handle, blade | x | 0.567 | 11.081 ticks (0.554 s) | screaming: -1.2 rad rest (upper arms), -1.8 (forearms), +0.5 (sword), then pi x 0.3 = 0.942 rad (54 deg) on the arms and 1.5 x that (81 deg) on the sword; calm: held still - upper arms -0.5, forearms -1.0 with the left yawed +1.0 and the right -1.0 (folded across), the sword at 0.35; each forearm placed 10 units along its upper arm's pitch, the sword 7 units along the right forearm's | no | 1.8 clip-ticks per game tick at a 1.0 s authoring length | ModelEnderKnight.java:300-328 |

The bare `walk` clip is the gait group's (`gait`, scaled by walking speed in-game); every other group keys `walk_<group>` / `idle_<group>` (contract §2.1).

Author every loop at 1.0 s (README rule 5, the phase-locked kind; ruled 2026-09-06, Q14 (a)). In-game each loop plays once per natural period whatever its length; your timeline's length is the SHAPE's timeline and does not change that (P6). Blockbench previews at the declared length, so a 1.0 s preview of a fast group shows the shape slowed down (contract §9); a `_preview` copy stretched to the natural periods is a Blockbench-only aid, never in the jar and never in a delivery (ruled 2026-09-06, Q15 (a)).

### 4.2 The math

- **gait**: the long legs stride in opposition, every piece of a leg together, and the cape swings a little with the step — `a = limbSwingAmount > 0.1 ? cos(ageInTicks * 1.3 * 0.21) * pi * 0.25 * limbSwingAmount : 0; lfoot1/3/4.xRot = a; lfoot2.xRot = 0.6 + a; lleg1.xRot = a; lleg2/lleg3.xRot = -0.1 + a; the right pieces the same rests - a; cape2.zRot = a / 4` (ModelEnderKnight.java:276-291)
- **cape_sway**: the cape's lower piece stirs slowly fore and aft — `cape2.xRot = cos(ageInTicks * 0.7 * 0.21) * pi * 0.02` (ModelEnderKnight.java:292)
- **arms**: at rest the arms are folded across the chest with the sword held low; screaming, both arms rise high and pump together with the sword whirling wider still — `s = cos(ageInTicks * 2.7 * 0.21) * pi * 0.3; screaming: larm2/larm3/rarm2/rarm3.xRot = -1.2 + s; larm1/rarm1.xRot = -1.8 + s; handle/blade.xRot = 0.5 + s * 1.5; calm: larm2/larm3/rarm2/rarm3.xRot = -0.5; larm1.xRot = rarm1.xRot = -1.0, larm1.yRot = 1.0, rarm1.yRot = -1.0; handle/blade.xRot = 0.35; larm1/rarm1 y/z placed 10 units along larm2/rarm2's pitch; handle/blade placed 7 units along rarm1's pitch (+1 on y)` (ModelEnderKnight.java:300-328)

### 4.3 Reference clips (reference-only)

`ender_knight_reference_walk.animation.json`, `ender_knight_reference_idle.animation.json`, `ender_knight_reference_scream.animation.json` (beside this sheet, and embedded in `ender_knight.bbmodel`'s Animation tab) are NOT clips to return or ship under their own names. Each is the creature's classic code — the motion the game draws today — SAMPLED by the harness at fixed inputs so you can see that motion beside the rig in Blockbench. The `.bbmodel` opens with every clip listed (the shipped clips and these); the files beside the sheet are the PINNED SOURCE (their bytes are what the checker knows; the generator's round trip proves the embedded copies within 5e-5 s on key times and 1e-6 on values). If you rebuild the `.bbmodel` and they are gone, Animation → Import Animations... on the files brings them back. One clip per state the code reads, each value alone: `_reference_walk` — full walking speed (limbSwingAmount 1, the walk position and the age advancing one tick per key), not attacking, looking straight ahead, every state flag at rest, full health; `_reference_idle` — standing still (limbSwingAmount 0, the walk position 0; the age advancing one tick per key), not attacking, looking straight ahead, every state flag at rest, full health; `_reference_scream` — the `scream` state: the code's `isScreaming` answering true while standing still, every other state at rest; no `_reference_attack` — this creature's code reads no attack state. Keys: the classic code is sampled once per tick, and the samples are reduced by the density search the exact transcriptions use — the fewest Catmull-Rom keys per bone and channel whose curve stays within 1 degree of rotation and 1/32 block of position of every sample, the closing key always kept — so each clip carries as few keys as that tolerance allows (each entry below states its key counts and the measured maximum error). Rotations are deltas from the bind pose under the same sign rule as the shipped clips (X as the classic degrees, Y and Z negated), positions only where the code moves a bone.

This rig is not yet in-game: these clips are sampled from the hook's transcription of the classic `setupAnim` into the descriptor a landing slice will use (owner 2026-09-14, addendum item 10) — the transcription is proven when the rig lands; until then the clips show the code as the port carries it.

**Starting point (owner 2026-09-14, addendum item 13; extended by the second set revised, item 4):** a reference clip's KEYS may be the starting point of the delivered clip its state corresponds to — copy them into `walk` (from `_reference_walk`), `idle` (from `_reference_idle`), `aggro_idle` (from `_reference_attack`), `fly` (from `_reference_fly`) or `swim` (from `_reference_swim`) and improve from there; a delivered clip carrying those keys is a valid delivery (no rule compares them). A state with no contract clip (sit, sleep, display, ...) is OFFERED as an extra of this creature's own under §2.3 — §5 lists it as "offered from reference_<state>" beside the seed's extras, and a delivered clip under that name carrying the reference keys is a valid delivery. Only the reference file itself coming back under its own name is refused.

- `ender_knight_reference_walk.animation.json` (sha256 `5906327e13091b4f07ef601ee78873e65732dbc9996b42ba5c41dbee3bddd8ea`; clip `reference_walk`; a starting point for `walk`): two seconds (40 ticks): this motion does not close within 6 s (its slowest rhythm is 42.743 ticks, and no multiple of it under 6 s brings every rhythm back within 5 degrees) — a two-second window, not a loop; the closing key differs from the first by 48.4008 degrees; 2 to 34 keys per bone chosen by the density search from 41 per-tick samples, Catmull-Rom; max error 0.976 degrees / 0 block against the samples (the tolerance 1 degree / 1/32 block).
  - Sampled from: `danger.orespawn.entity.client.EnderKnightGeoReplacement.applyCustomAnimations(AnimationProcessor, PoseInputs) through OreSpawnGeoReplacement.pose (the S4 doctrine: the unlanded hook of owner 2026-09-14, addendum item 10 - the classic setupAnim transcribed into the descriptor's PoseInputs form, proven when the rig lands - registry-free, on explicit PoseInputs, over the reference leg's converter output reference_enderknight.geo.json baked without the face-order strictness the landing slice's TEST-007 adds)`.
  - Rule applied: wingspeed 0.21: the slowest rhythm is the cape cos(ageInTicks * 0.7 * WINGSPEED) * PI * 0.02 (EnderKnightGeoReplacement.applyCustomAnimations:79, every state) at 0.147 rad/tick, 42.74 ticks; the threshold gait on fourteen leg parts cos(ageInTicks * 1.3 * WINGSPEED) * PI * 0.25 * limbSwingAmount (:63, walk) at 0.273; the screaming branch cos(ageInTicks * 2.7 * WINGSPEED) * PI * 0.3 (:88, isScreaming false on the probe); the head look clamped from netHeadYaw = 0 (:80); the forearm and blade position writes follow the arms (:128-136); no multiple of the slowest group's period 42.742757 ticks up to the 6 s cap (120 ticks) brings every group back within 5 degrees of its start (k = 1: 16.945368 deg, k = 2: 55.015247 deg): a two-second window (40 ticks), not a loop - the sheet states the closing key's seam.
  - Bones that move in it: `cape2`, `lfoot1`, `lfoot2`, `lfoot3`, `lfoot4`, `lleg1`, `lleg2`, `lleg3`, `rfoot1`, `rfoot2`, `rfoot3`, `rfoot4`, `rleg1`, `rleg2`, `rleg3`; bones the code also MOVES (position keys): `blade`, `handle`, `larm1`, `rarm1`.
  - Loop seam: the closing key differs from the first by at most 48.4008 degrees (mod 360).
- `ender_knight_reference_idle.animation.json` (sha256 `e2023c9659ff0c3a291eb51746f97edeb73a64c4ded32bb05224584e86217475`; clip `reference_idle`; a starting point for `idle`): one period of its slowest rhythm — 42.743 ticks (2.137 s): every moving bone is back within 5 degrees of its start there, so the last key closes the loop; 2 to 3 keys per bone chosen by the density search from 44 per-tick samples, Catmull-Rom; max error 0.267 degrees / 0 block against the samples (the tolerance 1 degree / 1/32 block).
  - Sampled from: `danger.orespawn.entity.client.EnderKnightGeoReplacement.applyCustomAnimations(AnimationProcessor, PoseInputs) through OreSpawnGeoReplacement.pose (the S4 doctrine: the unlanded hook of owner 2026-09-14, addendum item 10 - the classic setupAnim transcribed into the descriptor's PoseInputs form, proven when the rig lands - registry-free, on explicit PoseInputs, over the reference leg's converter output reference_enderknight.geo.json baked without the face-order strictness the landing slice's TEST-007 adds)`.
  - Rule applied: wingspeed 0.21: the slowest rhythm is the cape cos(ageInTicks * 0.7 * WINGSPEED) * PI * 0.02 (EnderKnightGeoReplacement.applyCustomAnimations:79, every state) at 0.147 rad/tick, 42.74 ticks; the threshold gait on fourteen leg parts cos(ageInTicks * 1.3 * WINGSPEED) * PI * 0.25 * limbSwingAmount (:63, walk) at 0.273; the screaming branch cos(ageInTicks * 2.7 * WINGSPEED) * PI * 0.3 (:88, isScreaming false on the probe); the head look clamped from netHeadYaw = 0 (:80); the forearm and blade position writes follow the arms (:128-136); closes at k = 1 (42.742757 ticks): every bone returns within 0 degrees of its start at k x T (the 5-degree test under the 6 s cap; k = 1: 0 deg).
  - Bones that move in it: `cape2`; bones the code also MOVES (position keys): `blade`, `handle`, `larm1`, `rarm1`.
  - Loop seam: the closing key differs from the first by at most 0 degrees (mod 360).
- `ender_knight_reference_scream.animation.json` (sha256 `c9c611854417a3ec980b409234688a42e254a08bfbe93bd7642aaee6ae5a5c8c`; clip `reference_scream`; offered as the extra `scream` (§2.3; §5)): two seconds (40 ticks): this motion does not close within 6 s (its slowest rhythm is 42.743 ticks, and no multiple of it under 6 s brings every rhythm back within 5 degrees) — a two-second window, not a loop; the closing key differs from the first by 143.535 degrees; 2 to 41 keys per bone chosen by the density search from 41 per-tick samples, Catmull-Rom; max error 0.946 degrees / 0 block against the samples (the tolerance 1 degree / 1/32 block).
  - State: `isScreaming` answers true, every other getter at rest (named by ender_knight.json reference_states.scream = {isScreaming: true}).
  - Sampled from: `danger.orespawn.entity.client.EnderKnightGeoReplacement.applyCustomAnimations(AnimationProcessor, PoseInputs) through OreSpawnGeoReplacement.pose (the S4 doctrine: the unlanded hook of owner 2026-09-14, addendum item 10 - the classic setupAnim transcribed into the descriptor's PoseInputs form, proven when the rig lands - registry-free, on explicit PoseInputs, over the reference leg's converter output reference_enderknight.geo.json baked without the face-order strictness the landing slice's TEST-007 adds)`.
  - Rule applied: wingspeed 0.21: the slowest rhythm is the cape cos(ageInTicks * 0.7 * WINGSPEED) * PI * 0.02 (EnderKnightGeoReplacement.applyCustomAnimations:79, every state) at 0.147 rad/tick, 42.74 ticks; the threshold gait on fourteen leg parts cos(ageInTicks * 1.3 * WINGSPEED) * PI * 0.25 * limbSwingAmount (:63, walk) at 0.273; the screaming branch cos(ageInTicks * 2.7 * WINGSPEED) * PI * 0.3 (:88, isScreaming false on the probe); the head look clamped from netHeadYaw = 0 (:80); the forearm and blade position writes follow the arms (:128-136); no multiple of the slowest group's period 42.742757 ticks up to the 6 s cap (120 ticks) brings every group back within 5 degrees of its start (k = 1: 30.501665 deg / 4.463404 units, k = 2: 99.027446 deg / 15.622012 units): a two-second window (40 ticks), not a loop - the sheet states the closing key's seam.
  - Bones that move in it: `blade`, `cape2`, `handle`, `larm1`, `larm2`, `larm3`, `rarm1`, `rarm2`, `rarm3`; bones the code also MOVES (position keys): `blade`, `handle`, `larm1`, `rarm1`.
  - Loop seam: the closing key differs from the first by at most 143.535 degrees (mod 360).

`check` REJECTS a `*_reference_<state>.animation.json` returned under its own name (the package's own untouched copy is warned, not a delivery), and the game's jar never carries one (the asset audit refuses it).

This creature has NO exact keyframe transcription (owner 2026-09-13, Amendment 2: its motion does not fit the current transcription form, and the game runs its classic code until you deliver `idle` and `walk`). Its source formulas in plain language (migration design section 5 — the source method and line quoted, each formula and constant in words):

- _(no `formulas` in the seed yet — the lane authors them from the model sources, the orig line cited)_

## 5. Clips: what to improve, what to leave (AUTHORED verdicts on generated rows)
> **DRAFT** — DRAFT — authored by a seed lane from the entity code, 2026-09-13; the owner's edit pending. Nothing in this section is ruled: it was drafted from the entity code and the model, every claim citing its line, for the owner's edit.


| clip | loop | layer | weight / trigger | bones it may animate | length | code-triggered | verdict | notes |
|---|---|---|---|---|---|---|---|---|
| `idle` | true | base | w_idle = (1 - w_move)(1 - w_swim)(1 - w_fly) | any of the 40 bones — gait group (speed-scaled): cape2, lfoot1, lfoot2, lfoot3, lfoot4, lleg1, lleg2, lleg3, rfoot1, rfoot2, rfoot3, rfoot4, rleg1, rleg2, rleg3; free: cape1, head, lcollar, lhip, lshoulder, neck, pelvis, rcollar, rhip, rib1, rib2, rib3, rib4, rshoulder, spine1, spine2, spine3; other groups' bones: blade (better left to `idle_arms`), handle (better left to `idle_arms`), larm1 (better left to `idle_arms`), larm2 (better left to `idle_arms`), larm3 (better left to `idle_arms`), rarm1 (better left to `idle_arms`), rarm2 (better left to `idle_arms`), rarm3 (better left to `idle_arms`) | 1 s | no | author | REQUIRED with walk: the gate opens on the pair (today nothing ships; the classic code plays) - the standing loop of the calm branch: arms folded, sword low, the cape stirring |
| `idle_cape_sway` | true | parallel layer | w_idle = (1 - w_move)(1 - w_swim)(1 - w_fly) (group cape_sway) | cape2 | 1 s | no | author |  |
| `idle_arms` | true | parallel layer | w_idle = (1 - w_move)(1 - w_swim)(1 - w_fly) (group arms) | larm1, larm2, larm3, rarm1, rarm2, rarm3, handle, blade | 1 s | no | author |  |
| `walk` | true | base | w_walk = w_move (1 - w_swim)(1 - w_fly); the gait group additionally x limbSwingAmount (P3) | any of the 40 bones — gait group (speed-scaled): cape2, lfoot1, lfoot2, lfoot3, lfoot4, lleg1, lleg2, lleg3, rfoot1, rfoot2, rfoot3, rfoot4, rleg1, rleg2, rleg3; free: cape1, head, lcollar, lhip, lshoulder, neck, pelvis, rcollar, rhip, rib1, rib2, rib3, rib4, rshoulder, spine1, spine2, spine3; other groups' bones: blade (better left to `walk_arms`), handle (better left to `walk_arms`), larm1 (better left to `walk_arms`), larm2 (better left to `walk_arms`), larm3 (better left to `walk_arms`), rarm1 (better left to `walk_arms`), rarm2 (better left to `walk_arms`), rarm3 (better left to `walk_arms`) | 1 s | no | author | REQUIRED with idle: the locomotion loop - the gait group's 45-degree long stride with the cape swinging (ModelEnderKnight.java:276-291), scaled by walking speed in-game |
| `walk_cape_sway` | true | parallel layer | w_walk = w_move (1 - w_swim)(1 - w_fly); the gait group additionally x limbSwingAmount (P3) (group cape_sway) | cape2 | 1 s | no | author |  |
| `walk_arms` | true | parallel layer | w_walk = w_move (1 - w_swim)(1 - w_fly); the gait group additionally x limbSwingAmount (P3) (group arms) | larm1, larm2, larm3, rarm1, rarm2, rarm3, handle, blade | 1 s | no | author |  |
| `swim` | true | base | w_swim from isInWater() | any of the 40 bones — gait group (speed-scaled): cape2, lfoot1, lfoot2, lfoot3, lfoot4, lleg1, lleg2, lleg3, rfoot1, rfoot2, rfoot3, rfoot4, rleg1, rleg2, rleg3; free: cape1, head, lcollar, lhip, lshoulder, neck, pelvis, rcollar, rhip, rib1, rib2, rib3, rib4, rshoulder, spine1, spine2, spine3; other groups' bones: blade (better left to `swim_arms`), handle (better left to `swim_arms`), larm1 (better left to `swim_arms`), larm2 (better left to `swim_arms`), larm3 (better left to `swim_arms`), rarm1 (better left to `swim_arms`), rarm2 (better left to `swim_arms`), rarm3 (better left to `swim_arms`) | 1 s | no | author | it has the state - a FloatGoal keeps it at the surface (EnderKnight.java:55) - but water hurts it and it teleports out (:228-230, :259-262), so author a brief thrashing paddle; optional: falls back to walk then idle when absent (section 2.4) |
| `aggro_idle` | true | base | w_idle x w_aggro (DATA_SCREAMING held) | any of the 40 bones — gait group (speed-scaled): cape2, lfoot1, lfoot2, lfoot3, lfoot4, lleg1, lleg2, lleg3, rfoot1, rfoot2, rfoot3, rfoot4, rleg1, rleg2, rleg3; free: cape1, head, lcollar, lhip, lshoulder, neck, pelvis, rcollar, rhip, rib1, rib2, rib3, rib4, rshoulder, spine1, spine2, spine3; other groups' bones: blade (better left to `idle_arms`), handle (better left to `idle_arms`), larm1 (better left to `idle_arms`), larm2 (better left to `idle_arms`), larm3 (better left to `idle_arms`), rarm1 (better left to `idle_arms`), rarm2 (better left to `idle_arms`), rarm3 (better left to `idle_arms`) | 1 s | no | author | the screaming branch at rest: arms thrown high and pumping 54 deg every 11.1 ticks, the sword whirling 81 deg (ModelEnderKnight.java:301-308); the synched screaming flag is held while it has a target (EnderKnight.java:119, :342; cleared :124, :252, :260, :283). Offered now: the generator's inventory reads DATA_SCREAMING as the held aggro state under the entity's own name (TEST-011 (c), owner 2026-09-14, item 5: raised in the target goal's findTarget at :119, cleared on target loss at :124 / :283 - classified STATE), so w_aggro drives this pair |
| `calm_idle` | true | base | w_idle x (1 - w_aggro) | any of the 40 bones — gait group (speed-scaled): cape2, lfoot1, lfoot2, lfoot3, lfoot4, lleg1, lleg2, lleg3, rfoot1, rfoot2, rfoot3, rfoot4, rleg1, rleg2, rleg3; free: cape1, head, lcollar, lhip, lshoulder, neck, pelvis, rcollar, rhip, rib1, rib2, rib3, rib4, rshoulder, spine1, spine2, spine3; other groups' bones: blade (better left to `idle_arms`), handle (better left to `idle_arms`), larm1 (better left to `idle_arms`), larm2 (better left to `idle_arms`), larm3 (better left to `idle_arms`), rarm1 (better left to `idle_arms`), rarm2 (better left to `idle_arms`), rarm3 (better left to `idle_arms`) | 1 s | no | author | the calm branch: arms folded across the chest, sword at 0.35 rad (ModelEnderKnight.java:309-322) - the same as idle; may be delivered as a copy of idle |
| `attack` | false | triggered controller | event: a strike (the transport per species by the trigger inventory — ruled 2026-09-06, Q11 (a); §6 says which applies here) | (any unlocked) | 0.5 s | yes | author | a sword strike: the vanilla MeleeAttackGoal (EnderKnight.java:56) closes and calls doHurtTarget for 12 inside vanilla code - offered now: the generator's inventory records the vanilla goal's strike as the site (TEST-011 (c): MeleeAttackGoal.checkAndPerformAttack calls the entity's doHurtTarget, no override here); the held screaming state is not pulsed at the strike, so the strike is transport 1 (the server LivingDamageEvent.Post) - an overhead sword blow landing on the impact frame |
| `hurt` | false | triggered controller | event: hurtTime rising edge (client-observed); the red overlay stays (ruled 2026-09-06, Q4 (a)) | (any unlocked) | 0.3 s | yes | author | a flinch: hurt() at EnderKnight.java:339-349 sets the scream and, for an arrow or magic, teleports away instead of taking the hit; the hurtTime edge is client-observed |
| `death` | hold_on_last_frame | triggered controller | event: deathTime > 0; the vanilla death flip stays — the pilot ships no death clip (ruled 2026-09-06, Q3 (a)); a creature whose JSON ships one gets the clip-replaces-flip mode when the first such clip arrives, so a `death` delivered here plays under the flip until then | (any unlocked) | 1 s | yes | author | ruled 2026-09-06, Q3 (a): the vanilla death flip stays; a creature whose JSON ships a `death` clip gets the clip-replaces-flip mode, designed when the first such clip arrives (the Queen's own death clip the precedent) - a `death` delivered here plays under the flip until then |
| `teleport` | false | overlay | enderTeleportTo (EnderKnight.java:308-318): every teleport - away from a starer within 4 blocks or toward a target beyond 16 (:271-286), at random on an arrow or magic hit (:339-349), in daylight (:243-255), when wet or burning (:257-262) - a position jump with portal particles and the enderman teleport sound at both ends; a server-side event with no trigger by name today (the seam would need a named site) | cape1, cape2, larm1, larm2, larm3, rarm1, rarm2, rarm3, handle, blade | 0.3 s | no | author | the vanish: a snap of the cape and arms inward on the tick it goes - it will read at the landing, not the origin, since the entity is already there |
| `scream` | true | base (a state loop, offered) | the code's `isScreaming` answering true (the state `reference_scream` samples; its transport is the landing slice's) | (any unlocked) | 1 s | no | author | offered from `reference_scream`: the code's scream pose (`isScreaming` true) — copy the reference clip's keys and improve from there (a valid delivery) |

Loop values are the JSON `loop` field exactly: `true` for cycles, `false` for one-shots, `hold_on_last_frame` only where written. Every name in this table is fixed (contract §2.4); `idle_alt_1`, `idle_alt_2`, ... may be added freely. A sheet may carry up to four extra clips of this creature's own beyond the contract's names (§2.3; ruled 2026-09-06, Q6 (a)) — more needs the owner's ruling; the extras rows above, if any, are those.

### 5.1 Wishlist (AUTHORED — only what `check` accepts today; anything else is marked)
> **DRAFT** — DRAFT — authored by a seed lane from the entity code, 2026-09-13; the owner's edit pending. Nothing in this section is ruled: it was drafted from the entity code and the model, every claim citing its line, for the owner's edit.


- in `walk`: bend the legs - thigh, shin pieces and foot pieces swing as one rigid unit (ModelEnderKnight.java:277-290); a knee that lags the thigh makes the enderman's long, deliberate stride, and the ribs and spine (static) should sway
- the arms and sword (ModelEnderKnight.java:300-328): the rig's bones are flat and the code slides the forearm along the upper arm and the sword along the forearm - author the fold, the rise and the swing as rotations and flag to the owner that the seam must parent the chains or key the positions when the rig lands
- in `attack`: a real sword blow - the screaming branch only pumps the arms (:301-308); an overhead cut landing on the impact frame with the cape following
- in `aggro_idle`: keep the arms-up scream (:301-308) but let the head follow the stare - the code clamps the look to 0.45 rad (:293-299)
- the `teleport` extra: the code snaps the position (EnderKnight.java:308-318); a half-second wind-up would have to play BEFORE the jump, which the seam cannot know - keep it to a landing settle

## 6. Trigger inventory (generated from the entity's AI goals and state flags)

Source: `src/main/java/danger/orespawn/entity/EnderKnight.java` (Monster, danger.orespawn.entity.pose.EnderKnightPose)

| selector | prio | goal | guard | category | what it does | bears on |
|---|---|---|---|---|---|---|
| goalSelector | 0 | `FloatGoal` | - | locomotion | bobs up to the surface in water (vanilla) | swim state (client reads isInWater) |
| goalSelector | 1 | `MeleeAttackGoal` | - | attack | closes in and strikes (vanilla melee) | attack (event) |
| goalSelector | 2 | `WaterAvoidingRandomStrollGoal` | - | locomotion | strolls to random spots, avoiding water (vanilla) | walk |
| goalSelector | 3 | `LookAtPlayerGoal` | - | look | turns the head toward a nearby player (vanilla; head yaw/pitch only) | none: head look, not a clip |
| goalSelector | 4 | `RandomLookAroundGoal` | - | look | looks around idly (vanilla; head yaw/pitch only) | none: head look, not a clip |
| targetSelector | 1 | `HurtByTargetGoal` | - | targeting | retaliates against whoever hurt it (vanilla target selector) | aggro state (through the attacking flag where one exists) |
| targetSelector | 2 | `NearestAttackableTargetGoal` | - | targeting | hunts the nearest attackable target (vanilla) | aggro state |

A `[modern: key]` guard means the goal is registered only under the modern config key named (read once, at construction); `UNPARSED` means the registration's shape is one the generator does not read (a local variable or a computed priority) — the owner reads that line.

**Synched state flags** (what the client can see): `DATA_SCREAMING` (Boolean, line 40)

**Attacking flag `DATA_SCREAMING`** — classified **STATE** (cleared when the target is lost at line(s) [124, 252, 283]; the flag is also RAISED in hurt() at line(s) [342]: a client-observed rising edge would fire `attack` when the mob is HIT, not only when it strikes — transport 2 needs that edge masked (e.g. by hurtTime); other clears at line(s) [260] (guards: if (this.isInWaterRainOrBubble() || this.isOnFire())) — the owner reads them) — a mechanical reading; the owner confirms it against the sites:

`DATA_SCREAMING` is read as the held aggro state under the entity's own name (TEST-011 (c)): no attacking flag is declared, the strike is vanilla MeleeAttackGoal (registered at line 56), and `setScreaming` is raised inside the target-selection code (line(s) [119]: the target goal's body) and cleared on target loss (line(s) [124, 252, 283]) - the STATE pattern; every other write is listed below.

| line | method | sets | guard (the enclosing block) | within | comment |
|---|---|---|---|---|---|
| 119 | findTarget | 1 | if (this.targetConditions.test(this.mob, nearest)) | if (nearest != null) | orig :74 |
| 124 | findTarget | 0 | if (nearest != null) | - | orig :78 |
| 252 | aiStep | 0 | if (brightness > 0.5f && this.level().canSeeSky(this.blockPosition()) && this.random.nextFloat() * 30.0f < (brightness - 0.4f) * 2.0f) | if (this.level().isDay() && !this.level().isClientSide) |  |
| 260 | aiStep | 0 | if (this.isInWaterRainOrBubble() \|\| this.isOnFire()) | - |  |
| 283 | aiStep | 0 | NOT(if (target != null)) | if (!this.level().isClientSide && this.isAlive()) |  |
| 342 | hurt | 1 | (unguarded: the method body) | - |  |

The guard is the header of the block that actually encloses the write (found by brace depth, comments and strings blanked); an `else` branch is written as the negated `if` chain it closes; a header the parser cannot read is `(unparsed: ...)`, never dropped.

**Locomotion facts:** a walker. Overrides: hurt, aiStep, customServerAiStep.

**Strike and launch sites:**

- melee (vanilla `MeleeAttackGoal`, registered at EnderKnight.java:56) `doHurtTarget(target)` inside vanilla code — net.minecraft.world.entity.ai.goal.MeleeAttackGoal.checkAndPerformAttack: canPerformAttack (within melee reach - isWithinMeleeAttackRange - with line of sight and the attack cooldown elapsed), resetAttackCooldown (20 ticks), swing, then the entity's doHurtTarget - NeoForge 21.1.223 bytecode (checkAndPerformAttack offsets 2-27, resetAttackCooldown bipush 20); no doHurtTarget override in the entity or its parents, so the strike is the vanilla goal's (TEST-011 (c))

**What fires each contract clip:**

| clip | signal | verdict |
|---|---|---|
| `walk` | limbSwingAmount from AnimationState (the seam's input; 0 at rest, 1 at full stride) | the gait group's weight and speed scale (P3) |
| `idle` | w_idle = (1 - w_move)(1 - w_swim)(1 - w_fly) | standing still |
| `swim` | Entity.isInWater() (client-evaluated); FloatGoal keeps it at the surface | falls back to walk then idle when absent (§2.4) |
| `aggro_idle / calm_idle` | DATA_SCREAMING held while engaged (cleared when the target is lost at line(s) [124, 252, 283]; the flag is also RAISED in hurt() at line(s) [342]: a client-observed rising edge would fire `attack` when the mob is HIT, not only when it strikes — transport 2 needs that edge masked (e.g. by hurtTime); other clears at line(s) [260] (guards: if (this.isInWaterRainOrBubble() \|\| this.isOnFire())) — the owner reads them); `DATA_SCREAMING` is read as the held aggro state under the entity's own name (TEST-011 (c)): no attacking flag is declared, the strike is vanilla MeleeAttackGoal (registered at line 56), and `setScreaming` is raised inside the target-selection code (line(s) [119]: the target goal's body) and cleared on target loss (line(s) [124, 252, 283]) - the STATE pattern; every other write is listed below | STATE flag drives w_aggro (§4.3) |
| `attack` | melee (vanilla MeleeAttackGoal registered at EnderKnight.java:56; the strike is vanilla code reached through the entity's doHurtTarget): LivingDamageEvent.Post -> triggerAnim packet (transport 1); ranged: named launch sites (transport 3) | the transport per species by the trigger inventory (ruled 2026-09-06, Q11 (a)): this flag is held, not pulsed at the strike, so the signal column's transport applies — the server LivingDamageEvent.Post -> triggerAnim packet for melee, a named launch site for ranged |
| `hurt` | rising edge of LivingEntity.hurtTime (0 -> 10), client-observed | always available; the red overlay stays (ruled 2026-09-06, Q4 (a)) |
| `death` | LivingEntity.deathTime > 0, client-observed | hold_on_last_frame; the vanilla death flip stays and the pilot ships no death clip (ruled 2026-09-06, Q3 (a)); a creature whose JSON ships a `death` clip gets the clip-replaces-flip mode, designed when the first such clip arrives (the Queen's own death clip the precedent) — until then a delivered `death` plays under the flip |
| `idle_alt_N` | a roll at each idle loop boundary (p = 0.15) while w_idle > 0.9 | optional; one roll per idle loop boundary, p = 0.15, a uniform choice, only while the idle weight is above 0.9 and no triggered clip plays, keyed on the clip-clock cycle index (ruled 2026-09-06, Q5 (a)) |

## 7. Hitbox bones that must keep their names

No MultiHitboxLib profile: no locked bones. Every bone name is still immutable (the code poses bones by name).

## 8. Textures

| canonical file (edit this one) | canvas | also shipped as (fan-out on import) | referenced by the code as | note |
|---|---|---|---|---|
| `enderknight.png` | 512x512 | enderknighttexture.png | enderknight.png |  |

## 9. Reference screenshots

`reference/` holds the slots listed in `reference/SLOTS.md`; the owner fills them from the dev client (fixed camera, light and state per slot). Empty until then.

## 10. Effort and priority

- Artist scope: full contract (Tier 2, the state-branching class (a screaming branch on the arms and sword): geckolib_migration_design.md section 5); the rig is not yet in-game (it lands through the seam in a later slice).
- Estimated effort: 26 h (generator: 4 h + 0.2 h/bone x 40 + 1 h/clip x 14 to author or improve (owner adjusts)).
- Tier 2 (state-branching — setupAnim branches on entity state).
- Density statement (the harness's, not the artist's; ruled 2026-09-06, Q9 (a) and Q10): the classic transcription is verified at 2.5e-3 rad — Beaver reference leg 15 / 13 / 8 catmullrom keys per bone with spline arguments repaired at load; wrap sample T−ε vs 0+ε included; the key counts per bone are an output of the harness, re-derived on every re-transcription.

## 11. What 'done' looks like for this entity

_This rig is NOT yet in-game: the geometry is the converter's output over the port's compiled model, proven part for part against the 1.7.10 source by the reference-geometry leg; it lands through the seam in a later slice. Bone names are FINAL — the seam, the hitbox profiles and the transcriptions find bones by name._

1. `reference_enderknight.geo.json` returned UNCHANGED (or not at all): every bone name, parent, pivot, rotation and cube as listed in §3 and the packaged file (the reference leg's geo; the rule is the shipped rig's) (a `locked` bone renamed, re-parented or deleted is refused by name).
2. `ender_knight.animation.json` — that exact file name, one file — (format 1.8.0) holding the clips of §5 with the loop values shown; keys on `locked` bones: Keying a locked bone is allowed; the checker warns, and the hitbox part follows the bone in-game (the consequence, so keep such keys deliberate). Renaming, re-parenting or deleting a locked bone is refused.
3. Textures at their canvas sizes under the canonical names of §8 (aliases are fanned out on import).
4. Optionally the `.bbmodel` working file. `tools/artist_package.py check <your folder>` must pass before hand-in. It REJECTS: a missing or empty folder, or a missing manifest; no `.animation.json`, one under the wrong name (the sheet names the file), or a second one; a clip not in the sheet (renamed or added; `idle_alt_N` only where the sheet allows it), or a clip delivered for a Tier-3 creature; a wrong `loop` value; a missing `idle` or `walk` (they open the game's switch only TOGETHER — one without the other leaves the creature on its code-driven motion); a missing clip the game already triggers by name; a key on a bone the rig does not have (renamed), or a channel other than rotation / position / scale; a Molang string, a non-numeric or non-finite value, a custom-instruction (`timeline`) key; an event keyframe (`sound_effects` / `particle_effects`) on a LOOPING clip of a phase-locked species — no event keyframes on loops; code-fired events come from the trigger inventory (one-shot clips may carry them; a creature whose controllers are its own GeckoLib controllers — the SPEC's controller kind `native`, the Queen — is exempt: native controllers are not phase-locked, event keys fire per loop); |rotation| > 3600 degrees or |position| > 1024 units; a key later than the clip's `animation_length` (a declared length shorter than the last key); a returned `.geo.json` whose bones, parents, pivots, rotations, cubes, UVs or canvas differ from the shipped rig; a `locked` bone renamed, re-parented or deleted in a returned `.geo.json` (the finding names the bone); a texture that is not this creature's, or whose canvas size changed; a `*_reference_<state>.animation.json` returned under its own name that is not the package's own untouched copy (the reference-only clips the package carries beside the sheet: the classic code sampled at fixed inputs, never shipped — the finding names the file; the untouched copy coming back is warned, not a delivery; copying a reference clip's KEYS into `idle` / `walk` / `aggro_idle` / `fly` / `swim` or into an offered extra is a delivery and passes); a returned `.bbmodel` that lists fewer clips than the folder's animation files hold, or whose embedded keys leave the round-trip tolerance (key times within 5e-5 s, values within 1e-6) of those files (the files beside the sheet are the pinned source; the `.bbmodel` must open with every clip listed — owner 2026-09-14). It WARNS on: a key on a `locked` bone (the clip and the bones are named) — Keying a locked bone is allowed; the checker warns, and the hitbox part follows the bone in-game (the consequence, so keep such keys deliberate). Renaming, re-parenting or deleting a locked bone is refused; a `_preview` file delivered (a Blockbench-only aid, never in the jar — ruled 2026-09-06, Q15 (a); leave it out of the delivery); a returned `.geo.json` at all (the shipped rig is used regardless — do not re-export it); a clip length far from the one the sheet states (rule 5: a phase-locked creature's loops at 1.0 s — ruled 2026-09-06, Q14 (a); a native creature's clips at their shipped length; a one-shot at the length its row states); an optional clip not delivered; an alias-named texture (the canonical name is preferred); a `format_version` other than 1.8.0; a number written as a string; an unknown keyframe key; a clip without `animation_length`; an unreadable `.bbmodel`.

