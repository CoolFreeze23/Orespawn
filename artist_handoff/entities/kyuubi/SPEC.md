# Kyuubi — `kyuubi` — artist SPEC

**This rig is NOT yet in-game: the geometry is the converter's output over the port's compiled model, proven part for part against the 1.7.10 source by the reference-geometry leg; it lands through the seam in a later slice. Bone names are FINAL — the seam, the hitbox profiles and the transcriptions find bones by name.**

Generated by `tools/artist_package.py` 0.2.9 (the full folder, 2026-09-13, fourth set, items 5, 7 and 8: every artist-tier species packaged - a rig not yet in-game from the reference leg's converter output, bone names final; the Tier-1 bosses' intended locked bones from the design's section 6 rendered in section 7 and validated against the geo; the README's priority table the full list, bosses first, closing with the deliverable count) from the repository's own sources; the paragraphs marked AUTHORED come from `tools/artist_specs/kyuubi.json` (DRAFT — authored by a seed lane from the entity code, 2026-09-13; the owner's edit pending). Every contract decision this sheet rests on was ruled by the mod's owner on 2026-09-06 (`phase_g_reports/animation_contract/open_questions.md` records each ruling); a line that cites one ("ruled 2026-09-06, Q14 (a)") states that ruling — nothing here invents an answer. The one open item is the lock reject mode (§7, README rule 6).

## 1. What this mob is (AUTHORED — draft for the owner's edit)
> **DRAFT** — DRAFT — authored by a seed lane from the entity code, 2026-09-13; the owner's edit pending. Nothing in this section is ruled: it was drafted from the entity code and the model, every claim citing its line, for the owner's edit.


A burning fox demon that walks upright: a chest and body, a head (built turned 180 degrees and turned back by the code) with two five-piece flame horns sweeping back from it, two-part arms and legs, and a nine-segment tail curling up and over its back - and every body part has a 'Fire' twin drawn over it, 21 pairs on a 512x256 sheet (KyuubiModel.java createBodyLayer). It is hostile: 125 HP, 10 attack, 10 armour (EntityKyuubi.java:65-72, MobStats.KYUUBI), fire-immune (ModEntities.java:254) and sets itself alight every few ticks (:82-89); water burns it (:90-91). Every tenth think tick it picks the nearest living thing that is not a monster within 12 blocks and in sight (:110-114, :131-154), walks at it and, within 8 blocks, hurls a small fireball from 1.25 blocks up on a roll (:115-125); it never bites. It floats (:57), panics when hurt (:58) and spawns anywhere (:180-184). Read it as a flaming, loping biped: arms and legs pumping with the stride, the tail and the two horns rippling in slow travelling waves, the head following the player.

## 2. Size and scale in-game

- Hitbox (EntityType dims, `ModEntities.java`): 0.5 x 1.25 blocks (wide x tall).
- Renderer scale: x1; shadow radius 0.1 (reference pin status: pin, `tools/reference_renderer_pins.json`).
- Rig units: 16 units = 1 block before the renderer scale, so a 1.25-block-tall hitbox is 20 rig units tall at x1.
- Texture canvas: 512 x 256 (keep it; the canvas size is checked on return).
- Notes (AUTHORED): Drawn at x1.0 of the rig (KyuubiRenderer.java:24; no scale override) on a 512x256 sheet (KyuubiModel.java:317); the hitbox is 0.5 x 1.25 (ModEntities.java:252-254); shadow 0.1 (KyuubiRenderer.java:26).

## 3. Bone glossary (locked legacy names — readable labels beside them)

Never rename, delete or re-parent a bone: code and hitboxes reference them by name. Left/right in the labels follow the model author's own naming (the side the legacy name calls left); Blockbench mirrors X for display, so the author's left appears on your right when the mob faces you. `gait bone` = its motion is scaled by walking speed in-game (P3). `locked` = it carries or parents a hitbox part (contract §8.1). Keying a locked bone is allowed; the checker warns, and the hitbox part follows the bone in-game (the consequence, so keep such keys deliberate). Renaming, re-parenting or deleting a locked bone is refused.

This rig is NOT yet in-game: the geometry is the converter's output over the port's compiled model, proven part for part against the 1.7.10 source by the reference-geometry leg; it lands through the seam in a later slice. Bone names are FINAL — the seam, the hitbox profiles and the transcriptions find bones by name.

| bone | readable label | classic part | parent | pivot (x, y, z) | group | gait bone | locked | cubes |
|---|---|---|---|---|---|---|---|---|
| `body` | body (lower torso, pitched -17 deg) | body (ModelPart of the same name) | - | (0, 19, 1) | - | no | no | 1 |
| `bodyFire` | body, fire twin | bodyFire (ModelPart of the same name) | - | (0, 20, 1) | - | no | no | 1 |
| `chest` | chest (upper torso, pitched -49 deg) | chest (ModelPart of the same name) | - | (0, 24, 5) | - | no | no | 1 |
| `chestFire` | chest, fire twin | chestFire (ModelPart of the same name) | - | (0, 25, 6) | - | no | no | 1 |
| `head` | head (built turned 180 deg; the code adds pi to its yaw so it faces forward and follows the look) | head (ModelPart of the same name) | - | (0, 24, 6) | - | no | no | 1 |
| `headFire` | head, fire twin (follows the look without the flip; the horns hang from its yaw) | headFire (ModelPart of the same name) | - | (0, 23, 6) | - | no | no | 1 |
| `lfArmLower` | left forearm (placed 8 units along the upper arm's pitch by the code) | lfArmLower (ModelPart of the same name) | - | (7, 18, 2) | arm_breath | no | no | 1 |
| `lfArmLowerFire` | left forearm, fire twin | lfArmLowerFire (ModelPart of the same name) | - | (7.3333, 19, 1.5) | arm_breath | no | no | 1 |
| `lfArmUpper` | left upper arm | lfArmUpper (ModelPart of the same name) | - | (5, 22, 3) | arm_breath | no | no | 1 |
| `lfArmUpperFire` | left upper arm, fire twin | lfArmUpperFire (ModelPart of the same name) | - | (3, 23, 3) | arm_breath | no | no | 1 |
| `lfHorn1` | left flame horn, piece 1 (base, at the head) | lfHorn1 (ModelPart of the same name) | - | (3, 32.7, 4) | horns | no | no | 1 |
| `lfHorn2` | left flame horn, piece 2 | lfHorn2 (ModelPart of the same name) | - | (3, 34, 2) | horns | no | no | 1 |
| `lfHorn3` | left flame horn, piece 3 | lfHorn3 (ModelPart of the same name) | - | (3, 35, -2) | horns | no | no | 1 |
| `lfHorn4` | left flame horn, piece 4 | lfHorn4 (ModelPart of the same name) | - | (3, 35, -5) | horns | no | no | 1 |
| `lfHorn5` | left flame horn, piece 5 (tip) | lfHorn5 (ModelPart of the same name) | - | (3, 35.5, -7) | horns | no | no | 1 |
| `lfLegLower` | left shin (placed 8 units along the thigh's pitch by the code) | lfLegLower (ModelPart of the same name) | - | (3, 6, 2) | gait | yes | no | 1 |
| `lfLegLowerFire` | left shin, fire twin | lfLegLowerFire (ModelPart of the same name) | - | (3, 8, 2) | gait | yes | no | 1 |
| `lfLegUpper` | left thigh | lfLegUpper (ModelPart of the same name) | - | (2, 12, 0) | gait | yes | no | 1 |
| `lfLegUppperFire` | left thigh, fire twin (the part is spelled with three p's in the code - keep it) | lfLegUppperFire (ModelPart of the same name) | - | (2, 13, 0) | gait | yes | no | 1 |
| `rtArmLower` | right forearm (placed 8 units along the upper arm's pitch by the code) | rtArmLower (ModelPart of the same name) | - | (-7, 18, 2) | arm_breath | no | no | 1 |
| `rtArmLowerFire` | right forearm, fire twin | rtArmLowerFire (ModelPart of the same name) | - | (-7, 19, 1) | arm_breath | no | no | 1 |
| `rtArmUpper` | right upper arm | rtArmUpper (ModelPart of the same name) | - | (-5, 22, 2) | arm_breath | no | no | 1 |
| `rtArmUpperFire` | right upper arm, fire twin | rtArmUpperFire (ModelPart of the same name) | - | (-5, 25, 2) | arm_breath | no | no | 1 |
| `rtHorn1` | right flame horn, piece 1 (base, at the head) | rtHorn1 (ModelPart of the same name) | - | (-3, 32.7, 4) | horns | no | no | 1 |
| `rtHorn2` | right flame horn, piece 2 | rtHorn2 (ModelPart of the same name) | - | (-3, 34, 2) | horns | no | no | 1 |
| `rtHorn3` | right flame horn, piece 3 | rtHorn3 (ModelPart of the same name) | - | (-3, 35, -2) | horns | no | no | 1 |
| `rtHorn4` | right flame horn, piece 4 | rtHorn4 (ModelPart of the same name) | - | (-3, 35, -5) | horns | no | no | 1 |
| `rtHorn5` | right flame horn, piece 5 (tip) | rtHorn5 (ModelPart of the same name) | - | (-3, 35.5, -7) | horns | no | no | 1 |
| `rtLegLower` | right shin (placed 8 units along the thigh's pitch by the code) | rtLegLower (ModelPart of the same name) | - | (-3, 6, 4) | gait | yes | no | 1 |
| `rtLegLowerFire` | right shin, fire twin | rtLegLowerFire (ModelPart of the same name) | - | (-3, 9, 4) | gait | yes | no | 1 |
| `rtLegUpper` | right thigh | rtLegUpper (ModelPart of the same name) | - | (-2, 12, 0) | gait | yes | no | 1 |
| `rtLegUpperFire` | right thigh, fire twin | rtLegUpperFire (ModelPart of the same name) | - | (-2, 13, 0) | gait | yes | no | 1 |
| `tail0` | tail root (static, at the base of the spine) | tail0 (ModelPart of the same name) | - | (1, 15, -2) | - | no | no | 1 |
| `tail1` | tail segment 1 (base of the wave) | tail1 (ModelPart of the same name) | - | (0, 14, -4.3333) | tail_curl | no | no | 1 |
| `tail2` | tail segment 2 | tail2 (ModelPart of the same name) | - | (0, 14, -7) | tail_curl | no | no | 1 |
| `tail3` | tail segment 3 | tail3 (ModelPart of the same name) | - | (0, 17.5, -10) | tail_curl | no | no | 1 |
| `tail4` | tail segment 4 | tail4 (ModelPart of the same name) | - | (0, 21, -12) | tail_curl | no | no | 1 |
| `tail5` | tail segment 5 | tail5 (ModelPart of the same name) | - | (0, 22, -17) | tail_curl | no | no | 1 |
| `tail6` | tail segment 6 | tail6 (ModelPart of the same name) | - | (0, 21, -21) | tail_curl | no | no | 1 |
| `tail7` | tail segment 7 | tail7 (ModelPart of the same name) | - | (0, 19, -24) | tail_curl | no | no | 1 |
| `tail8` | tail segment 8 | tail8 (ModelPart of the same name) | - | (0, 17, -25.75) | tail_curl | no | no | 1 |
| `tail9` | tail segment 9 (tip, curled 2 rad over the back) | tail9 (ModelPart of the same name) | - | (0, 15, -26) | tail_curl | no | no | 1 |

## 4. Current animation behaviour — plain English and the source formulas

- One gait rhythm on the limbs (KyuubiModel.java:323-348): above a walking speed of 0.1 the cosine of the age x 1.1 x 0.5 (wingspeed), x pi x 0.2, x the walking speed pitches the right thigh and shin forward and the left pair back (rests +0.59 / -0.15 and +0.26 / -0.44 rad); the arms take the same cosine at pi x 0.08 x the walking speed (no threshold) plus a 2-degree breathing at 0.5 x 0.5 (:337), the right arm with the right leg. Each shin and forearm is slid 8 units along its upper part's pitch (:328-329, :334-335, :341-342, :347-348) - a hand-written FK chain on flat bones.
- Every body part has a Fire twin (bodyFire, chestFire, headFire, the arm, leg and horn twins) posed to exactly the same angle as its part (:325, :327, :332, :334, :338, :340, :344, :346); the chest, body and tail root never move.
- The head turns to the look (:350-351): the head part is built facing backward (its geo rotation is 180 deg about y) and the code adds pi to netHeadYaw, so `head` faces forward; `headFire` takes the look yaw straight. The horns hang from headFire's yaw: five pieces per side, each placed along the previous piece's yaw (3.6, 2, 4, 3, 2 units) and turned by +-0.244 rad plus a cosine at 1.3 x 0.5 lagging pi/4 per piece (:352-368) - an 18-degree wave running base to tip every 9.7 ticks, mirrored left and right.
- The nine-segment tail carries two travelling waves (:369-410): a sideways one - 36 degrees per segment at 0.9 x 0.5, lagging pi/4 per segment, each segment's x placed along the previous yaw - and a slower up-and-down one at 0.5 x 0.5, 18 degrees around a curl that rises from -0.26 rad at the base to 2.0 rad at the tip, y and z placed along the previous pitch. The tail's bones are flat (no parents) in the rig, so when this rig lands a keyframed wave must key positions as well as rotations, or the tail must be re-parented.
- Nothing in the pose reads the attack: the fireball leaves from 1.25 blocks up (EntityKyuubi.java:120-122) with no wind-up; the entity sets itself on fire every ~10 ticks (:82-89) and puffs smoke when water hurts it (:90-98).

### 4.1 Tempo table (contract §7.1)

| group | bones | axis | omega (rad/tick) | natural period | amplitude | speed-scaled | Blockbench preview rate | source |
|---|---|---|---|---|---|---|---|---|
| gait | rtLegUpper, rtLegUpperFire, rtLegLower, rtLegLowerFire, lfLegUpper, lfLegUppperFire, lfLegLower, lfLegLowerFire, rtArmUpper, rtArmUpperFire, rtArmLower, rtArmLowerFire, lfArmUpper, lfArmUpperFire, lfArmLower, lfArmLowerFire | x | 0.55 | 11.424 ticks (0.571 s) | legs: rests +0.59 / -0.15 rad (right thigh / shin), +0.26 / -0.44 (left), then pi x 0.2 = 0.628 rad (36 deg) x the walking speed above the 0.1 threshold; arms: rests 0 (upper) / +0.48 (lower), then pi x 0.08 = 0.251 rad (14 deg) x the walking speed, no threshold - each lower part slid 8 units along the upper part's pitch | yes | 1.75 clip-ticks per game tick at a 1.0 s authoring length | KyuubiModel.java:323-348 |
| arm_breath | rtArmUpper, rtArmUpperFire, rtArmLower, rtArmLowerFire, lfArmUpper, lfArmUpperFire, lfArmLower, lfArmLowerFire | x | 0.25 | 25.133 ticks (1.257 s) | pi x 0.01 = 0.031 rad (2 deg), added to the arms' gait term at all times | no | 0.8 clip-ticks per game tick at a 1.0 s authoring length | KyuubiModel.java:337-348 |
| horns | lfHorn1, lfHorn2, lfHorn3, lfHorn4, lfHorn5, rtHorn1, rtHorn2, rtHorn3, rtHorn4, rtHorn5 | y | 0.65 | 9.666 ticks (0.483 s) | +0.244 rad rest splay (left) / -0.244 (right, mirrored), then pi x 0.1 = 0.314 rad (18 deg), each piece lagging the one before it by pi/4 - all on top of the head's look yaw | no | 2.07 clip-ticks per game tick at a 1.0 s authoring length | KyuubiModel.java:352-368 |
| tail_sway | tail1, tail2, tail3, tail4, tail5, tail6, tail7, tail8, tail9 | y | 0.45 | 13.963 ticks (0.698 s) | pi x 0.2 = 0.628 rad (36 deg) per segment, each lagging the one before it by pi/4 (a full wave every eight segments) | no | 1.43 clip-ticks per game tick at a 1.0 s authoring length | KyuubiModel.java:369-385 |
| tail_curl | tail1, tail2, tail3, tail4, tail5, tail6, tail7, tail8, tail9 | x | 0.25 | 25.133 ticks (1.257 s) | rests -0.26 / -0.78 / -1.11 / -0.18 / +0.22 / +0.63 / +0.89 / +1.52 / +2.0 rad (the curl up and over the back), then pi x 0.1 = 0.314 rad (18 deg), each segment lagging the one before it by pi/4 | no | 0.8 clip-ticks per game tick at a 1.0 s authoring length | KyuubiModel.java:386-410 |

The bare `walk` clip is the gait group's (`gait`, scaled by walking speed in-game); every other group keys `walk_<group>` / `idle_<group>` (contract §2.1).

Author every loop at 1.0 s (README rule 5, the phase-locked kind; ruled 2026-09-06, Q14 (a)). In-game each loop plays once per natural period whatever its length; your timeline's length is the SHAPE's timeline and does not change that (P6). Blockbench previews at the declared length, so a 1.0 s preview of a fast group shows the shape slowed down (contract §9); a `_preview` copy stretched to the natural periods is a Blockbench-only aid, never in the jar and never in a delivery (ruled 2026-09-06, Q15 (a)).

### 4.2 The math

- **gait**: the legs and arms pump with the stride at one rhythm - the right leg and right arm one way, the left leg and left arm the other - the legs three times as wide as the arms; each fire twin moves exactly with its part — `a = limbSwingAmount > 0.1 ? cos(t * 1.1 * 0.5) * pi * 0.2 * limbSwingAmount : 0; rtLegUpper(+Fire).xRot = 0.59 + a; rtLegLower(+Fire).xRot = -0.15 + a; lfLegUpper(+Fire).xRot = 0.26 - a; lfLegLower(+Fire).xRot = -0.44 - a; b = cos(t * 1.1 * 0.5) * pi * 0.08 * limbSwingAmount (+ the arm_breath term); rtArmUpper(+Fire).xRot = b; rtArmLower(+Fire).xRot = 0.48 + b; lfArmUpper(+Fire).xRot = -b; lfArmLower(+Fire).xRot = 0.48 - b; every lower part's z = sin(upper xRot) * 8` (KyuubiModel.java:323-348)
- **arm_breath**: a barely visible slow breathing of the arms, present even when standing — `rtArm*.xRot += cos(t * 0.5 * 0.5) * pi * 0.01; lfArm*.xRot -= the same` (KyuubiModel.java:337-348)
- **horns**: a travelling wave runs from the base to the tip of each flame horn, the two sides mirrored, the whole horn turning with the head — `lfHornK.yRot = headFire.yRot + 0.244 + cos(t * 1.3 * 0.5 - (K-1) * pi/4) * pi * 0.1; rtHornK.yRot = headFire.yRot - 0.244 - cos(t * 1.3 * 0.5 - (K-1) * pi/4) * pi * 0.1; each piece's x/z placed along the previous piece's yaw (3.6, 2, 4, 3, 2 units)` (KyuubiModel.java:352-368)
- **tail_sway**: a sideways wave travels up the nine-segment tail from the base to the tip — `tailK.yRot = cos(t * 0.9 * 0.5 - (K-1) * pi/4) * pi * 0.2; tail(K+1).x = tailK.x - sin(tailK.yRot) * L_K (L = 3, 4, 3.5, 5, 4, 3, 2, 1)` (KyuubiModel.java:369-385)
- **tail_curl**: a slower up-and-down ripple runs along the curled tail, the segments' y/z placed along the previous segment's pitch — `tailK.xRot = rest_K + cos(t * 0.5 * 0.5 - (K-1) * pi/4) * pi * 0.1; tail(K+1).y = tailK.y + sin(tailK.xRot) * L_K; tail(K+1).z = tailK.z - cos(tailK.xRot) * L_K` (KyuubiModel.java:386-410)

### 4.3 Reference clip (reference-only)

_no reference clip yet: it is sampled from the classic hook when the rig lands through the seam; until then §4.1 / §4.2 and the plain-language formulas (where the seed carries them) are the motion's description._

This creature has NO exact keyframe transcription and is not yet on its hook (the rig is not yet in-game: §3). The plain-language transcription of its source formulas (migration design section 5 — the source method and line quoted, each formula and constant in words) is authored when the rig lands on its hook; until then the seed's description, clip verdicts and wishlist (§1, §4, §5) describe the motion.

## 5. Clips: what to improve, what to leave (AUTHORED verdicts on generated rows)
> **DRAFT** — DRAFT — authored by a seed lane from the entity code, 2026-09-13; the owner's edit pending. Nothing in this section is ruled: it was drafted from the entity code and the model, every claim citing its line, for the owner's edit.


| clip | loop | layer | weight / trigger | bones it may animate | length | code-triggered | verdict | notes |
|---|---|---|---|---|---|---|---|---|
| `idle` | true | base | w_idle = (1 - w_move)(1 - w_swim)(1 - w_fly) | any of the 42 bones — gait group (speed-scaled): lfArmLower, lfArmLowerFire, lfArmUpper, lfArmUpperFire, lfLegLower, lfLegLowerFire, lfLegUpper, lfLegUppperFire, rtArmLower, rtArmLowerFire, rtArmUpper, rtArmUpperFire, rtLegLower, rtLegLowerFire, rtLegUpper, rtLegUpperFire; free: body, bodyFire, chest, chestFire, head, headFire, tail0; other groups' bones: lfHorn1 (better left to `idle_horns`), lfHorn2 (better left to `idle_horns`), lfHorn3 (better left to `idle_horns`), lfHorn4 (better left to `idle_horns`), lfHorn5 (better left to `idle_horns`), rtHorn1 (better left to `idle_horns`), rtHorn2 (better left to `idle_horns`), rtHorn3 (better left to `idle_horns`), rtHorn4 (better left to `idle_horns`), rtHorn5 (better left to `idle_horns`), tail1 (better left to `idle_tail_curl`), tail2 (better left to `idle_tail_curl`), tail3 (better left to `idle_tail_curl`), tail4 (better left to `idle_tail_curl`), tail5 (better left to `idle_tail_curl`), tail6 (better left to `idle_tail_curl`), tail7 (better left to `idle_tail_curl`), tail8 (better left to `idle_tail_curl`), tail9 (better left to `idle_tail_curl`) | 1 s | no | author | REQUIRED with walk: the gate opens on the pair (today nothing ships; the classic code plays) - the standing loop: limbs at their rests with the 2-degree arm breathing, the horns and tail waving |
| `idle_arm_breath` | true | parallel layer | w_idle = (1 - w_move)(1 - w_swim)(1 - w_fly) (group arm_breath) | rtArmUpper, rtArmUpperFire, rtArmLower, rtArmLowerFire, lfArmUpper, lfArmUpperFire, lfArmLower, lfArmLowerFire | 1 s | no | author |  |
| `idle_horns` | true | parallel layer | w_idle = (1 - w_move)(1 - w_swim)(1 - w_fly) (group horns) | lfHorn1, lfHorn2, lfHorn3, lfHorn4, lfHorn5, rtHorn1, rtHorn2, rtHorn3, rtHorn4, rtHorn5 | 1 s | no | author |  |
| `idle_tail_sway` | true | parallel layer | w_idle = (1 - w_move)(1 - w_swim)(1 - w_fly) (group tail_sway) | tail1, tail2, tail3, tail4, tail5, tail6, tail7, tail8, tail9 | 1 s | no | author |  |
| `idle_tail_curl` | true | parallel layer | w_idle = (1 - w_move)(1 - w_swim)(1 - w_fly) (group tail_curl) | tail1, tail2, tail3, tail4, tail5, tail6, tail7, tail8, tail9 | 1 s | no | author |  |
| `walk` | true | base | w_walk = w_move (1 - w_swim)(1 - w_fly); the gait group additionally x limbSwingAmount (P3) | any of the 42 bones — gait group (speed-scaled): lfArmLower, lfArmLowerFire, lfArmUpper, lfArmUpperFire, lfLegLower, lfLegLowerFire, lfLegUpper, lfLegUppperFire, rtArmLower, rtArmLowerFire, rtArmUpper, rtArmUpperFire, rtLegLower, rtLegLowerFire, rtLegUpper, rtLegUpperFire; free: body, bodyFire, chest, chestFire, head, headFire, tail0; other groups' bones: lfHorn1 (better left to `walk_horns`), lfHorn2 (better left to `walk_horns`), lfHorn3 (better left to `walk_horns`), lfHorn4 (better left to `walk_horns`), lfHorn5 (better left to `walk_horns`), rtHorn1 (better left to `walk_horns`), rtHorn2 (better left to `walk_horns`), rtHorn3 (better left to `walk_horns`), rtHorn4 (better left to `walk_horns`), rtHorn5 (better left to `walk_horns`), tail1 (better left to `walk_tail_curl`), tail2 (better left to `walk_tail_curl`), tail3 (better left to `walk_tail_curl`), tail4 (better left to `walk_tail_curl`), tail5 (better left to `walk_tail_curl`), tail6 (better left to `walk_tail_curl`), tail7 (better left to `walk_tail_curl`), tail8 (better left to `walk_tail_curl`), tail9 (better left to `walk_tail_curl`) | 1 s | no | author | REQUIRED with idle: the locomotion loop - the gait group's 36-degree leg pump and 14-degree arm pump at full stride (KyuubiModel.java:323-348), scaled by walking speed in-game |
| `walk_arm_breath` | true | parallel layer | w_walk = w_move (1 - w_swim)(1 - w_fly); the gait group additionally x limbSwingAmount (P3) (group arm_breath) | rtArmUpper, rtArmUpperFire, rtArmLower, rtArmLowerFire, lfArmUpper, lfArmUpperFire, lfArmLower, lfArmLowerFire | 1 s | no | author |  |
| `walk_horns` | true | parallel layer | w_walk = w_move (1 - w_swim)(1 - w_fly); the gait group additionally x limbSwingAmount (P3) (group horns) | lfHorn1, lfHorn2, lfHorn3, lfHorn4, lfHorn5, rtHorn1, rtHorn2, rtHorn3, rtHorn4, rtHorn5 | 1 s | no | author |  |
| `walk_tail_sway` | true | parallel layer | w_walk = w_move (1 - w_swim)(1 - w_fly); the gait group additionally x limbSwingAmount (P3) (group tail_sway) | tail1, tail2, tail3, tail4, tail5, tail6, tail7, tail8, tail9 | 1 s | no | author |  |
| `walk_tail_curl` | true | parallel layer | w_walk = w_move (1 - w_swim)(1 - w_fly); the gait group additionally x limbSwingAmount (P3) (group tail_curl) | tail1, tail2, tail3, tail4, tail5, tail6, tail7, tail8, tail9 | 1 s | no | author |  |
| `swim` | true | base | w_swim from isInWater() | any of the 42 bones — gait group (speed-scaled): lfArmLower, lfArmLowerFire, lfArmUpper, lfArmUpperFire, lfLegLower, lfLegLowerFire, lfLegUpper, lfLegUppperFire, rtArmLower, rtArmLowerFire, rtArmUpper, rtArmUpperFire, rtLegLower, rtLegLowerFire, rtLegUpper, rtLegUpperFire; free: body, bodyFire, chest, chestFire, head, headFire, tail0; other groups' bones: lfHorn1 (better left to `swim_horns`), lfHorn2 (better left to `swim_horns`), lfHorn3 (better left to `swim_horns`), lfHorn4 (better left to `swim_horns`), lfHorn5 (better left to `swim_horns`), rtHorn1 (better left to `swim_horns`), rtHorn2 (better left to `swim_horns`), rtHorn3 (better left to `swim_horns`), rtHorn4 (better left to `swim_horns`), rtHorn5 (better left to `swim_horns`), tail1 (better left to `swim_tail_curl`), tail2 (better left to `swim_tail_curl`), tail3 (better left to `swim_tail_curl`), tail4 (better left to `swim_tail_curl`), tail5 (better left to `swim_tail_curl`), tail6 (better left to `swim_tail_curl`), tail7 (better left to `swim_tail_curl`), tail8 (better left to `swim_tail_curl`), tail9 (better left to `swim_tail_curl`) | 1 s | no | author | it has the state - a FloatGoal keeps it at the surface (EntityKyuubi.java:57) - but water hurts it (aiStep :90-91), so author a thrashing, hurried paddle; optional: falls back to walk then idle when absent (section 2.4) |
| `calm_idle` | true | base | = idle until this creature's SPEC adds a synched attacking byte (ruled 2026-09-06, Q12 (a)) | (as idle) | 1 s | no | leave | = idle: no synched attacking flag in EntityKyuubi.java (no DATA_ATTACKING; the target is found by the 1-in-10 scan at :110-114), so calm_idle is covered by idle until this creature's SPEC adds the byte (ruled 2026-09-06, Q12 (a)) |
| `attack` | false | triggered controller | event: a strike (the transport per species by the trigger inventory — ruled 2026-09-06, Q11 (a); §6 says which applies here) | (any unlocked) | 0.5 s | yes | author | a fireball throw: a ranged launch site (new SmallFireball at EntityKyuubi.java:120-122, fired from 1.25 blocks up within 8 blocks of the target on a 1-in-6-or-1-in-8 roll each think tick, :115-125; transport 3, a named launch site) - an arm flung forward, the head thrown back |
| `hurt` | false | triggered controller | event: hurtTime rising edge (client-observed); the red overlay stays (ruled 2026-09-06, Q4 (a)) | (any unlocked) | 0.3 s | yes | author | a flinch: no hurt() override (vanilla LivingEntity.hurt); the hurtTime edge is client-observed - it also fires when water burns it (doHurtTarget on itself at EntityKyuubi.java:91) |
| `death` | hold_on_last_frame | triggered controller | event: deathTime > 0; the vanilla death flip stays — the pilot ships no death clip (ruled 2026-09-06, Q3 (a)); a creature whose JSON ships one gets the clip-replaces-flip mode when the first such clip arrives, so a `death` delivered here plays under the flip until then | (any unlocked) | 1 s | yes | author | ruled 2026-09-06, Q3 (a): the vanilla death flip stays; a creature whose JSON ships a `death` clip gets the clip-replaces-flip mode, designed when the first such clip arrives (the Queen's own death clip the precedent) - a `death` delivered here plays under the flip until then |

Loop values are the JSON `loop` field exactly: `true` for cycles, `false` for one-shots, `hold_on_last_frame` only where written. Every name in this table is fixed (contract §2.4); `idle_alt_1`, `idle_alt_2`, ... may be added freely. A sheet may carry up to four extra clips of this creature's own beyond the contract's names (§2.3; ruled 2026-09-06, Q6 (a)) — more needs the owner's ruling; the extras rows above, if any, are those.

### 5.1 Wishlist (AUTHORED — only what `check` accepts today; anything else is marked)
> **DRAFT** — DRAFT — authored by a seed lane from the entity code, 2026-09-13; the owner's edit pending. Nothing in this section is ruled: it was drafted from the entity code and the model, every claim citing its line, for the owner's edit.


- key every Fire twin exactly with its part (bodyFire with body, lfLegUppperFire with lfLegUpper, and so on): the code poses them identically (KyuubiModel.java:325-348) and a twin drifting off its part reads as a rendering bug
- the tail and horn waves: the rig's bones are flat and the code slides each next segment along the previous one's angle (KyuubiModel.java:352-410) - author the waves as rotations and flag to the owner that the seam must either parent the chains or key the positions when the rig lands
- in `attack`: a wind-up the code never had - the fireball simply appears at 1.25 blocks (EntityKyuubi.java:120-122); an arm flung forward and the horns flaring sell the throw
- in `walk`: a lope, not a march - a heavier stride that dips the chest (the chest and body never move today) so the fire twins flicker with the step
- in `idle`: a slow head-and-horn turn reading as the scan for prey (the code re-picks a target every tenth think tick, EntityKyuubi.java:110-114)

## 6. Trigger inventory (generated from the entity's AI goals and state flags)

Source: `src/main/java/danger/orespawn/entity/EntityKyuubi.java` (Monster)

| selector | prio | goal | guard | category | what it does | bears on |
|---|---|---|---|---|---|---|
| goalSelector | 0 | `FloatGoal` | - | locomotion | bobs up to the surface in water (vanilla) | swim state (client reads isInWater) |
| goalSelector | 1 | `PanicGoal` | - | flee | runs in a panic after taking damage (vanilla) | walk (fast) |
| goalSelector | 2 | `WaterAvoidingRandomStrollGoal` | - | locomotion | strolls to random spots, avoiding water (vanilla) | walk |
| goalSelector | 3 | `LookAtPlayerGoal` | - | look | turns the head toward a nearby player (vanilla; head yaw/pitch only) | none: head look, not a clip |
| goalSelector | 4 | `RandomLookAroundGoal` | - | look | looks around idly (vanilla; head yaw/pitch only) | none: head look, not a clip |
| targetSelector | 1 | `HurtByTargetGoal` | - | targeting | retaliates against whoever hurt it (vanilla target selector) | aggro state (through the attacking flag where one exists) |

A `[modern: key]` guard means the goal is registered only under the modern config key named (read once, at construction); `UNPARSED` means the registration's shape is one the generator does not read (a local variable or a computed priority) — the owner reads that line.

**Synched state flags** (what the client can see): none

**Locomotion facts:** a walker. Overrides: aiStep, customServerAiStep, removeWhenFarAway.

**Strike and launch sites:**

- melee `doHurtTarget(this)` — EntityKyuubi.java:91 in `aiStep`, guard: if (this.isInWater()) within if (this.random.nextInt(10) == 1)
- ranged `new SmallFireball(...)` — EntityKyuubi.java:120 in `customServerAiStep`

**What fires each contract clip:**

| clip | signal | verdict |
|---|---|---|
| `walk` | limbSwingAmount from AnimationState (the seam's input; 0 at rest, 1 at full stride) | the gait group's weight and speed scale (P3) |
| `idle` | w_idle = (1 - w_move)(1 - w_swim)(1 - w_fly) | standing still |
| `swim` | Entity.isInWater() (client-evaluated); FloatGoal keeps it at the surface | falls back to walk then idle when absent (§2.4) |
| `aggro_idle / calm_idle` | no synched attacking flag | calm_idle only until this creature's SPEC adds a synched byte mirroring getTarget() != null (ruled 2026-09-06, Q12 (a): aggro_idle waits for that byte) |
| `attack` | melee: LivingDamageEvent.Post -> triggerAnim packet (transport 1); ranged: named launch sites (transport 3) | the transport per species by the trigger inventory (ruled 2026-09-06, Q11 (a)): no attacking flag here, so the signal column's transport applies — the server LivingDamageEvent.Post -> triggerAnim packet for melee, a named launch site for ranged |
| `hurt` | rising edge of LivingEntity.hurtTime (0 -> 10), client-observed | always available; the red overlay stays (ruled 2026-09-06, Q4 (a)) |
| `death` | LivingEntity.deathTime > 0, client-observed | hold_on_last_frame; the vanilla death flip stays and the pilot ships no death clip (ruled 2026-09-06, Q3 (a)); a creature whose JSON ships a `death` clip gets the clip-replaces-flip mode, designed when the first such clip arrives (the Queen's own death clip the precedent) — until then a delivered `death` plays under the flip |
| `idle_alt_N` | a roll at each idle loop boundary (p = 0.15) while w_idle > 0.9 | optional; one roll per idle loop boundary, p = 0.15, a uniform choice, only while the idle weight is above 0.9 and no triggered clip plays, keyed on the clip-clock cycle index (ruled 2026-09-06, Q5 (a)) |

## 7. Hitbox bones that must keep their names

No MultiHitboxLib profile: no locked bones. Every bone name is still immutable (the code poses bones by name).

## 8. Textures

| canonical file (edit this one) | canvas | also shipped as (fan-out on import) | referenced by the code as | note |
|---|---|---|---|---|
| `kyuubi.png` | 512x256 | - | kyuubi.png |  |

## 9. Reference screenshots

`reference/` holds the slots listed in `reference/SLOTS.md`; the owner fills them from the dev client (fixed camera, light and state per slot). Empty until then.

## 10. Effort and priority

- Artist scope: full contract (Tier 2, the gait-scaled class (with hand-written FK chains on the tail and horns): geckolib_migration_design.md section 5); the rig is not yet in-game (it lands through the seam in a later slice).
- Estimated effort: 26.4 h (generator: 4 h + 0.2 h/bone x 42 + 1 h/clip x 14 to author or improve (owner adjusts)).
- Tier 2 (gait-scaled — trigonometric pose depends on limbSwingAmount).
- Density statement (the harness's, not the artist's; ruled 2026-09-06, Q9 (a) and Q10): the classic transcription is verified at 2.5e-3 rad — Beaver reference leg 15 / 13 / 8 catmullrom keys per bone with spline arguments repaired at load; wrap sample T−ε vs 0+ε included; the key counts per bone are an output of the harness, re-derived on every re-transcription.

## 11. What 'done' looks like for this entity

_This rig is NOT yet in-game: the geometry is the converter's output over the port's compiled model, proven part for part against the 1.7.10 source by the reference-geometry leg; it lands through the seam in a later slice. Bone names are FINAL — the seam, the hitbox profiles and the transcriptions find bones by name._

1. `reference_kyuubi.geo.json` returned UNCHANGED (or not at all): every bone name, parent, pivot, rotation and cube as listed in §3 and the packaged file (the reference leg's geo; the rule is the shipped rig's) (a `locked` bone renamed, re-parented or deleted is refused by name).
2. `kyuubi.animation.json` — that exact file name, one file — (format 1.8.0) holding the clips of §5 with the loop values shown; keys on `locked` bones: Keying a locked bone is allowed; the checker warns, and the hitbox part follows the bone in-game (the consequence, so keep such keys deliberate). Renaming, re-parenting or deleting a locked bone is refused.
3. Textures at their canvas sizes under the canonical names of §8 (aliases are fanned out on import).
4. Optionally the `.bbmodel` working file. `tools/artist_package.py check <your folder>` must pass before hand-in. It REJECTS: a missing or empty folder, or a missing manifest; no `.animation.json`, one under the wrong name (the sheet names the file), or a second one; a clip not in the sheet (renamed or added; `idle_alt_N` only where the sheet allows it), or a clip delivered for a Tier-3 creature; a wrong `loop` value; a missing `idle` or `walk` (they open the game's switch only TOGETHER — one without the other leaves the creature on its code-driven motion); a missing clip the game already triggers by name; a key on a bone the rig does not have (renamed), or a channel other than rotation / position / scale; a Molang string, a non-numeric or non-finite value, a custom-instruction (`timeline`) key; an event keyframe (`sound_effects` / `particle_effects`) on a LOOPING clip of a phase-locked species — no event keyframes on loops; code-fired events come from the trigger inventory (one-shot clips may carry them; a creature whose controllers are its own GeckoLib controllers — the SPEC's controller kind `native`, the Queen — is exempt: native controllers are not phase-locked, event keys fire per loop); |rotation| > 3600 degrees or |position| > 1024 units; a key later than the clip's `animation_length` (a declared length shorter than the last key); a returned `.geo.json` whose bones, parents, pivots, rotations, cubes, UVs or canvas differ from the shipped rig; a `locked` bone renamed, re-parented or deleted in a returned `.geo.json` (the finding names the bone); a texture that is not this creature's, or whose canvas size changed; a `*_reference.animation.json` returned that is not the package's own untouched copy (the reference-only clip the package carries beside the sheet: the classic code sampled at fixed inputs, never edited, never delivered, never shipped — the finding names the file; the untouched copy coming back is warned, not a delivery). It WARNS on: a key on a `locked` bone (the clip and the bones are named) — Keying a locked bone is allowed; the checker warns, and the hitbox part follows the bone in-game (the consequence, so keep such keys deliberate). Renaming, re-parenting or deleting a locked bone is refused; a `_preview` file delivered (a Blockbench-only aid, never in the jar — ruled 2026-09-06, Q15 (a); leave it out of the delivery); a returned `.geo.json` at all (the shipped rig is used regardless — do not re-export it); a clip length far from the one the sheet states (rule 5: a phase-locked creature's loops at 1.0 s — ruled 2026-09-06, Q14 (a); a native creature's clips at their shipped length; a one-shot at the length its row states); an optional clip not delivered; an alias-named texture (the canonical name is preferred); a `format_version` other than 1.8.0; a number written as a string; an unknown keyframe key; a clip without `animation_length`; an unreadable `.bbmodel`.

