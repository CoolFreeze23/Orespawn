# Scorpion — `scorpion` — artist SPEC

Generated by `tools/artist_package.py` 0.4.0 (the reference clips revised, 2026-09-14, second set revised, items 2 to 6: every state a hook reads sampled and named by the seed - reference_walk, reference_idle, reference_attack, reference_fly, reference_swim, the species' own states, an unnamed one flagged - keyed by the exact transcriptions' density search (catmullrom, 1 degree, 1/32 block), embedded in every .bbmodel within the round-trip tolerance with the files beside the sheet the pinned source; a reference clip's keys as the starting point of idle / walk / aggro_idle / fly / swim, a species state offered as a SPEC extra; only the reference file itself under its own name refused; one tracked copy, the folder's copies ignored by git) from the repository's own sources; the paragraphs marked AUTHORED come from `tools/artist_specs/scorpion.json` (DRAFT — authored by a seed lane from the entity code, 2026-09-13; the owner's edit pending, landed on its hook by the remainder slice (2026-09-15)). Every contract decision this sheet rests on was ruled by the mod's owner on 2026-09-06 (`phase_g_reports/animation_contract/open_questions.md` records each ruling); a line that cites one ("ruled 2026-09-06, Q14 (a)") states that ruling — nothing here invents an answer. The one open item is the lock reject mode (§7, README rule 6).

## 1. What this mob is (AUTHORED — draft for the owner's edit)
> **DRAFT** — DRAFT — authored by a seed lane from the entity code, 2026-09-13; the owner's edit pending, landed on its hook by the remainder slice (2026-09-15). Nothing in this section is ruled: it was drafted from the entity code and the model, every claim citing its line, for the owner's edit.


A hostile scorpion: a 6x4x8 body with a small head, eight 11-long legs splayed to the sides, two arms of an upper arm, a forearm and a claw, and a six-piece tail curling up over the back to a stinger (ScorpionModel.java:74-182, 22 parts), drawn at x0.75. 15 HP, 4 damage, 10 armour, speed 0.2 (EntityScorpion.java:80-88). On a 1-in-6 tick it picks prey in an 8-block box: velocity raptors, spiders, creepers and anything living that is not a monster - animals, villagers, survival players - never another scorpion or the emperor (:140-143, :193-209); it chases at 1.2 and, within 3 blocks plus half the target's width, rolls its sting on a 1-in-6 cadence (:67-68; BugMeleeAttackGoal.Params.scorpion; BugMeleeAttackGoal.java:159-174), one hit in three playing scorpion_attack at the victim (:151-160); the synched DATA_ATTACKING int (:43-44) is held at 1 while it stands in reach (BugMeleeAttackGoal.java:168). Cactus cannot hurt it (:166-170). It spawns in the dark, at night or below y 50 by day (:211-218; ModEntities.java:287-289). Read it as a scorpion: eight legs rippling in a wave, the claws snapping and the tail whipping at random - rarely at rest, constantly when it is on its prey.

## 2. Size and scale in-game

- Hitbox (EntityType dims, `ModEntities.java`): 0.85 x 0.55 blocks (wide x tall).
- Renderer scale: x0.75; shadow radius 0.2625 (reference pin status: pin, `tools/reference_renderer_pins.json`).
- Rig units: 16 units = 1 block before the renderer scale, so a 0.55-block-tall hitbox is 11.7 rig units tall at x0.75.
- Texture canvas: 88 x 24 (keep it; the canvas size is checked on return).
- Notes (AUTHORED): Drawn at x0.75 of the rig (ScorpionRenderer.java:24, :37; shadow 0.2625, :26) on an 88x24 sheet (ScorpionModel.java:184), scorpion.png (ScorpionRenderer.java:20); the hitbox is 0.85 x 0.55 (ModEntities.java:287-289). The original's ModelScorpion(0.62f) argument was the wingspeed (ScorpionModel.java:21), not a scale. The forearms and the tail are position chains the code re-places each tick (:266-267, :280-292) - key the joints, not the positions.

## 3. Bone glossary (locked legacy names — readable labels beside them)

Never rename, delete or re-parent a bone: code and hitboxes reference them by name. Left/right in the labels follow the model author's own naming (the side the legacy name calls left); the rig opens in Blockbench exactly as the game draws it, so a bone the author calls left is on the creature's own left. `gait bone` = its motion is scaled by walking speed in-game (P3). `locked` = it carries or parents a hitbox part (contract §8.1). Keying a locked bone is allowed; the checker warns, and the hitbox part follows the bone in-game (the consequence, so keep such keys deliberate). Renaming, re-parenting or deleting a locked bone is refused.

| bone | readable label | classic part | parent | pivot (x, y, z) | group | gait bone | locked | cubes |
|---|---|---|---|---|---|---|---|---|
| `body` | body (6x4x8; static) | (native rig: no classic part) | - | (-3, 7, -4) | - | no | no | 1 |
| `head` | head (5x3x4 ahead of the body; static — no look) | (native rig: no classic part) | - | (-2.5, 6.5, -8) | - | no | no | 1 |
| `larm1` | left forearm (2x2x3, placed 4.5 along the upper arm's yaw; rides the snap) | (native rig: no classic part) | - | (7, 5, -7.2) | claws | no | no | 1 |
| `larm2` | left upper arm (6x2x2 from the head's side, rest yaw 0.52; the claw snap's root) | (native rig: no classic part) | - | (1, 6, -6) | claws | no | no | 1 |
| `lclaw` | left claw (3x2x4, placed 3 ahead of the forearm; pinches against the upper arm's swing about 0.381) | (native rig: no classic part) | - | (7, 5, -10) | claws | no | no | 1 |
| `lleg1` | left leg 1, the front (11x2x2 splayed out, rest yaw 0.49; the gait, phase 0) | (native rig: no classic part) | - | (2, 6, -3) | gait | yes | no | 1 |
| `lleg2` | left leg 2 (rest 0.24; the gait, a quarter cycle behind) | (native rig: no classic part) | - | (2, 6, -1) | gait | yes | no | 1 |
| `lleg3` | left leg 3 (rest -0.24; the gait, half a cycle behind) | (native rig: no classic part) | - | (2, 6, 1) | gait | yes | no | 1 |
| `lleg4` | left leg 4, the rear (rest -0.49; the gait, three quarters behind) | (native rig: no classic part) | - | (2, 6, 3) | gait | yes | no | 1 |
| `rarm1` | right forearm (rides the snap) | (native rig: no classic part) | - | (-7, 5, -7.2) | claws | no | no | 1 |
| `rarm2` | right upper arm (rest yaw 2.61; the mirror snap) | (native rig: no classic part) | - | (-1, 6, -6) | claws | no | no | 1 |
| `rclaw` | right claw (pinches about -0.381) | (native rig: no classic part) | - | (-7, 5, -10) | claws | no | no | 1 |
| `rleg1` | right leg 1, the front (rest 2.65; the gait, the negative of left leg 1) | (native rig: no classic part) | - | (-2, 6, -1) | gait | yes | no | 1 |
| `rleg2` | right leg 2 (rest 2.9; the gait) | (native rig: no classic part) | - | (-2, 6, 1) | gait | yes | no | 1 |
| `rleg3` | right leg 3 (rest -2.9; the gait) | (native rig: no classic part) | - | (-2, 6, 3) | gait | yes | no | 1 |
| `rleg4` | right leg 4, the rear (rest -2.65; the gait) | (native rig: no classic part) | - | (-2, 6, 5) | gait | yes | no | 1 |
| `tail1` | tail segment 1 (4x4x5 at the body's rear, rest 0.26; the tail whip's root) | (native rig: no classic part) | - | (-2, 7, 3) | tail | no | no | 1 |
| `tail2` | tail segment 2 (3x3x5, placed 4 along segment 1; curls 0.769 further plus the whip) | (native rig: no classic part) | - | (-1.5, 7.2, 6) | tail | no | no | 1 |
| `tail3` | tail segment 3 (3x3x4, placed 4 along segment 2; curls 0.701 further plus the whip) | (native rig: no classic part) | - | (-1.5, 9.5, 8) | tail | no | no | 1 |
| `tail4` | tail segment 4 (2x2x5, placed 3 along segment 3; folds back over by -5.9 minus 1.5x the whip) | (native rig: no classic part) | - | (-1, 12, 9) | tail | no | no | 1 |
| `tail5` | tail segment 5 (2x2x4, placed 4 along segment 4; keeps its own pitch) | (native rig: no classic part) | - | (-1, 15, 6) | tail | no | no | 1 |
| `tail6` | stinger (1x1x3, placed 4 along segment 5; keeps its own pitch) | (native rig: no classic part) | - | (-0.5, 16, 2) | tail | no | no | 1 |

## 4. Current animation behaviour — plain English and the source formulas

- One gait rhythm with no threshold (ScorpionModel.java:193-204): the eight legs sweep 22 degrees x the walking speed about their splay rests at 1.24 rad/tick (a 5-tick period), each pair a quarter cycle behind the one ahead, the right side the mirror of the left - a wave down each flank at the 0.2 speed (EntityScorpion.java:84).
- The claws and the tail play on a per-entity latch (:207-233; EntityScorpion.java:46-53): a 3.4-tick cosine at 1.86 rad/tick is rerolled at every negative-to-positive zero crossing - at rest the left claw plays one cycle in ten, the right one in ten, the tail one in twenty-five; with the synched DATA_ATTACKING int at 1 (held while it stands within 3 blocks plus half the target's width, BugMeleeAttackGoal.java:168) the claws each play one cycle in two and the tail one in three (:221-227). A claw cycle is a 27-degree pinch of the upper arm against its claw with the forearm placed along the arm (:264-276); a tail cycle is a 27-degree curl that grows along segments 1-3 and folds segment 4 back 1.5 times as far, the stinger riding the chain (:278-292). Unselected, each holds its zero pose.
- The body and head are static; the head does not follow the look. The sting is doHurtTarget (EntityScorpion.java:151-160, 4 of damage, scorpion_attack at the victim one time in three) from the goal's swing roll (BugMeleeAttackGoal.java:170-173, nextInt(5) == 0 or nextInt(6) == 1 on a 1-in-6 cadence tick in reach) - not an animation event.

### 4.1 Tempo table (contract §7.1)

| group | bones | axis | omega (rad/tick) | natural period | amplitude | speed-scaled | Blockbench preview rate | source |
|---|---|---|---|---|---|---|---|---|
| gait | lleg1, lleg2, lleg3, lleg4, rleg1, rleg2, rleg3, rleg4 | y | 1.24 | 5.067 ticks (0.253 s) | pi x 0.12 = 0.377 rad (22 deg) x the walking speed about each leg's splay rest, the four pairs lagged a quarter cycle (pi/2) each from front to rear, the right legs the negative; no threshold | yes | 3.95 clip-ticks per game tick at a 1.0 s authoring length | ScorpionModel.java:193-204; wingspeed 0.62 (:21) |
| claws | larm2, larm1, lclaw, rarm2, rarm1, rclaw | y | 1.86 | 3.378 ticks (0.169 s) | pi x 0.15 = 0.471 rad (27 deg) at 3.0 x 0.62 = 1.86 rad/tick: the upper arm swings about its rest (0.52 left, 2.61 right) and the claw counter-swings about ±0.381 - a pinch; the forearm is placed 4.5 along the upper arm and the claw 3 ahead of it; each claw plays only on cycles its latch selects - one in ten at rest, one in two while attacking - and holds the zero pose otherwise | no | 5.92 clip-ticks per game tick at a 1.0 s authoring length | ScorpionModel.java:207-229, :264-276; the RenderInfo latch (:207; EntityScorpion.java:46-53, :104-107) |
| tail | tail1, tail2, tail3, tail4, tail5, tail6 | x (and the chain's y / z positions) | 1.86 | 3.378 ticks (0.169 s) | pi x 0.15 = 0.471 rad (27 deg) at 1.86 rad/tick, accumulating along the chain: segment 1 about 0.26, segment 2 a further 0.769 plus the angle again, segment 3 a further 0.701 plus it again, segment 4 folded back -5.9 minus 1.5x the angle; segments 5 and 6 keep their own pitch and ride the chain (4 / 4 / 3 / 4 / 4 px); plays only on cycles the latch selects - one in twenty-five at rest, one in three while attacking | no | 5.92 clip-ticks per game tick at a 1.0 s authoring length | ScorpionModel.java:207-233, :278-292 |

The bare `walk` clip is the gait group's (`gait`, scaled by walking speed in-game); every other group keys `walk_<group>` / `idle_<group>` (contract §2.1).

Author every loop at 1.0 s (README rule 5, the phase-locked kind; ruled 2026-09-06, Q14 (a)). In-game each loop plays once per natural period whatever its length; your timeline's length is the SHAPE's timeline and does not change that (P6). Blockbench previews at the declared length, so a 1.0 s preview of a fast group shows the shape slowed down (contract §9); a `_preview` copy stretched to the natural periods is a Blockbench-only aid, never in the jar and never in a delivery (ruled 2026-09-06, Q15 (a)).

### 4.2 The math

- **gait**: the eight legs sweep fore and aft in a wave that runs from the front pair to the rear, the right side mirroring the left — `a_k = cos(t * 2.0 * 0.62 - k * pi/2) * pi * 0.12 * amount for k = 0..3; lleg1.yRot = a_0 + 0.49; lleg2.yRot = a_1 + 0.24; lleg3.yRot = a_2 - 0.24; lleg4.yRot = a_3 - 0.49; rleg1.yRot = -a_0 + 2.65; rleg2.yRot = -a_1 + 2.9; rleg3.yRot = -a_2 - 2.9; rleg4.yRot = -a_3 - 2.65` (ScorpionModel.java:193-204; wingspeed 0.62 (:21))
- **claws**: the claws snap at random: a quick pinch of the upper arm and claw against each other, rare at rest and constant when the scorpion is on its prey — `c = cos(t * 3.0 * 0.62) * pi * 0.15; at each negative-to-positive zero crossing: ri1 = attacking ? nextInt(4) : nextInt(20); left = (ri1 == 1 || ri1 == 3) ? c : 0; right = (ri1 == 2 || ri1 == 3) ? c : 0; larm2.yRot = 0.52 + left; larm1.z = larm2.z - sin(larm2.yRot) * 4.5; lclaw.z = larm1.z - 3; lclaw.yRot = 0.381 - left; rarm2.yRot = 2.61 - right; rarm1.z = rarm2.z - sin(rarm2.yRot) * 4.5; rclaw.z = rarm1.z - 3; rclaw.yRot = -0.381 + right` (ScorpionModel.java:207-229, :264-276; the RenderInfo latch (:207; EntityScorpion.java:46-53, :104-107))
- **tail**: the tail whips up and over at random: a curl that grows along the segments and snaps the stinger forward, rare at rest and frequent when it is on its prey — `w = (ri2 == 1) ? cos(t * 3.0 * 0.62) * pi * 0.15 : 0 with ri2 = attacking ? nextInt(3) : nextInt(25) rerolled at each zero crossing; tail1.xRot = 0.26 + w; tail2.xRot = tail1.xRot + 0.769 + w; tail3.xRot = tail2.xRot + 0.701 + w; tail4.xRot = tail3.xRot - 5.501 - 1.5 w - 0.4; each segment placed along the one before (4, 4, 3, 4, 4 px by sin / cos of its pitch)` (ScorpionModel.java:207-233, :278-292)
- `ScorpionModel.poseFrom` (ScorpionModel.java:196-244; orig ModelScorpion.java:186-248 in `render` (func_78088_a, :177-249; func_78087_a at :257-259 is a bare super call), wingspeed 0.62 from `new ModelScorpion(0.62f)` in orig ClientProxyOreSpawn.java:433 - the port's WINGSPEED): the four leg pairs yaw by the cosine of the age in ticks times 2.0 times the wingspeed, less a quarter turn per pair (0, one, two, three quarters), times pi, times 0.12, times the walking speed - no threshold - about their rests, plus or minus 0.49 and 0.24 for the front two pairs and minus or plus 2.9 and 2.65 for the rear two. The claws and the tail sit on a per-entity latch: on the frame the claw rhythm - the cosine of the age times 3.0 times the wingspeed - crosses zero upward (the next tenth of a tick positive, this one negative) the two latch values are rolled from the entity's random, one in twenty and one in twenty-five at rest, one in four and one in three while attacking; then every frame the first value at 1 or 3 swings the left claw and at 2 or 3 the right, and the second at 1 strikes the tail, each by the cosine of the age times 3.0 times the wingspeed, times pi, times 0.15, else 0. A swinging claw's arm yaws about its rest (0.52 the left, 2.61 the right) and its wrist and pincer are placed 4.5 times the sine of that yaw along and a further 3 units in z. The tail's six links pitch cumulatively - 0.26 plus the angle on the first, 0.769 plus it on the second and third, 0.701 plus it on the fourth, and minus 5.501 minus one and a half times it minus 0.4 on the last - with each pivot following the link below by 4, 4, 3, 4 and 4 units along minus the sine (y) and the cosine (z) of that link's pitch. The walk position plays no part. The GeckoLib hook is the same (ScorpionGeoReplacement.applyCustomAnimations).

### 4.3 Reference clips (reference-only)

`scorpion_reference_walk.animation.json`, `scorpion_reference_idle.animation.json` (beside this sheet, and embedded in `scorpion.bbmodel`'s Animation tab) are NOT clips to return or ship under their own names. Each is the creature's classic code — the motion the game draws today — SAMPLED by the harness at fixed inputs so you can see that motion beside the rig in Blockbench. The `.bbmodel` opens with every clip listed (the shipped clips and these); the files beside the sheet are the PINNED SOURCE (their bytes are what the checker knows; the generator's round trip proves the embedded copies within 5e-5 s on key times and 1e-6 on values). If you rebuild the `.bbmodel` and they are gone, Animation → Import Animations... on the files brings them back. One clip per state the code reads, each value alone: `_reference_walk` — full walking speed (limbSwingAmount 1, the walk position and the age advancing one tick per key), not attacking, looking straight ahead, every state flag at rest, full health; `_reference_idle` — standing still (limbSwingAmount 0, the walk position 0; the age advancing one tick per key), not attacking, looking straight ahead, every state flag at rest, full health; no `_reference_attack` — the code reads an attack, but attacking alone moves nothing at rest: getAttacking() is read only inside the claw and tail latch's re-roll, if (nextangle > 0 && newangle < 0) on cos(ageInTicks * 3.0 * WINGSPEED) with its 0.1-tick look-ahead (ScorpionGeoReplacement.java:82-92): the read happens only when a rising zero crossing of that 1.86 rad/tick rhythm falls within 0.1 tick after a whole-tick sample, which no sampled tick does, so ri1 stays 0, the claws and tail hold their rest and attacking alone moves nothing. Keys: the classic code is sampled once per tick, and the samples are reduced by the density search the exact transcriptions use — the fewest Catmull-Rom keys per bone and channel whose curve stays within 1 degree of rotation and 1/32 block of position of every sample, the closing key always kept — so each clip carries as few keys as that tolerance allows (each entry below states its key counts and the measured maximum error). Rotations are deltas from the bind pose under the same sign rule as the shipped clips (X, Y and Z as the classic degrees: the rig is in the Bedrock convention), positions only where the code moves a bone.

**Starting point (owner 2026-09-14, addendum item 13; extended by the second set revised, item 4):** a reference clip's KEYS may be the starting point of the delivered clip its state corresponds to — copy them into `walk` (from `_reference_walk`), `idle` (from `_reference_idle`), `aggro_idle` (from `_reference_attack`), `fly` (from `_reference_fly`) or `swim` (from `_reference_swim`) and improve from there; a delivered clip carrying those keys is a valid delivery (no rule compares them). A state with no contract clip (sit, sleep, display, ...) is OFFERED as an extra of this creature's own under §2.3 — §5 lists it as "offered from reference_<state>" beside the seed's extras, and a delivered clip under that name carrying the reference keys is a valid delivery. Only the reference file itself coming back under its own name is refused.

- `scorpion_reference_walk.animation.json` (sha256 `09a358d4f060c4edffc59a55385a62a7fccc5176a322f1384b2453069d68c700`; clip `reference_walk`; a starting point for `walk`): one period of its slowest rhythm — 5.067 ticks (0.253 s): every moving bone is back within 5 degrees of its start there, so the last key closes the loop; 2 to 7 keys per bone chosen by the density search from 7 per-tick samples, Catmull-Rom; max error 0 degrees / 0 block against the samples (the tolerance 1 degree / 1/32 block).
  - Sampled from: `danger.orespawn.entity.client.ScorpionGeoReplacement.applyCustomAnimations(AnimationProcessor, PoseInputs) through OreSpawnGeoReplacement.pose (the S4 doctrine: the shipped replacement, registry-free, on explicit PoseInputs)`.
  - Rule applied: wingspeed 0.62: the pi/2-phased gait cos(ageInTicks * 2.0 * WINGSPEED - n * pi/4) * PI * 0.12 * limbSwingAmount (ScorpionGeoReplacement.applyCustomAnimations:68-77, walk) at 1.24 rad/tick, 5.07 ticks - the slowest; the claw and tail latch cos(ageInTicks * 3.0 * WINGSPEED) * PI * 0.15 with its 0.1-tick look-ahead (:82-83) on RenderInfo ri1 / ri2, rolled on the entity RNG with attacking-picked ranges (the Robot2 precedent); the claw and tail chains follow by position (:117-159); closes at k = 1 (5.067085 ticks): every bone returns within 0.002076 degrees of its start at k x T (the 5-degree test under the 6 s cap; k = 1: 0.002076 deg).
  - Bones that move in it: `larm2`, `lclaw`, `lleg1`, `lleg2`, `lleg3`, `lleg4`, `rarm2`, `rclaw`, `rleg1`, `rleg2`, `rleg3`, `rleg4`, `tail1`, `tail2`, `tail3`, `tail4`; bones the code also MOVES (position keys): `larm1`, `lclaw`, `rarm1`, `rclaw`, `tail2`, `tail3`, `tail4`, `tail5`, `tail6`.
  - Loop seam: the closing key differs from the first by at most 0.0021 degrees (mod 360).
- `scorpion_reference_idle.animation.json` (sha256 `384c3ad23b1f45922c33de8eb9375eb80cab29c9cfd37b494490cac711cdab9d`; clip `reference_idle`; a starting point for `idle`): one key (nothing in this rig's code moves a bone at these inputs: the pose it holds); one key per bone (nothing to reduce).
  - Sampled from: `danger.orespawn.entity.client.ScorpionGeoReplacement.applyCustomAnimations(AnimationProcessor, PoseInputs) through OreSpawnGeoReplacement.pose (the S4 doctrine: the shipped replacement, registry-free, on explicit PoseInputs)`.
  - Rule applied: nothing moves at the idle inputs: every bone's rotation and position is the same at every sample of the span the closure test settled (period_multiple; wingspeed 0.62: the pi/2-phased gait cos(ageInTicks * 2.0 * WINGSPEED - n * pi/4) * PI * 0.12 * limbSwingAmount (ScorpionGeoReplacement.applyCustomAnimations:68-77, walk) at 1.24 rad/tick, 5.07 ticks - the slowest; the claw and tail latch cos(ageInTicks * 3.0 * WINGSPEED) * PI * 0.15 with its 0.1-tick look-ahead (:82-83) on RenderInfo ri1 / ri2, rolled on the entity RNG with attacking-picked ranges (the Robot2 precedent); the claw and tail chains follow by position (:117-159); closes at k = 1 (5.067085 ticks): every bone returns within 0 degrees of its start at k x T (the 5-degree test under the 6 s cap; k = 1: 0 deg)), so this state is one key.
  - Bones that move in it: `larm2`, `lclaw`, `lleg1`, `lleg2`, `lleg3`, `lleg4`, `rarm2`, `rclaw`, `rleg1`, `rleg2`, `rleg3`, `rleg4`, `tail1`, `tail2`, `tail3`, `tail4`; bones the code also MOVES (position keys): `larm1`, `lclaw`, `rarm1`, `rclaw`, `tail2`, `tail3`, `tail4`, `tail5`, `tail6`.
  - Loop seam: the closing key differs from the first by at most 0 degrees (mod 360).

Values the code branches on that move NOTHING at rest (no clip; nothing is silently missing):
- `getAttacking` 1: no motion at rest — getAttacking() is read only inside the claw and tail latch's re-roll, if (nextangle > 0 && newangle < 0) on cos(ageInTicks * 3.0 * WINGSPEED) with its 0.1-tick look-ahead (ScorpionGeoReplacement.java:82-92): the read happens only when a rising zero crossing of that 1.86 rad/tick rhythm falls within 0.1 tick after a whole-tick sample, which no sampled tick does, so ri1 stays 0, the claws and tail hold their rest and attacking alone moves nothing.

Reads the sampler cannot enumerate alone (no clip can show them): `ScorpionPose.getRandom()` — the entity RNG (the seeded source the pose rolls), not a value the code branches on; `ScorpionPose.getRenderInfo()` — a RenderInfo latch (the per-entity scratch the pose reads and writes), not a value the code branches on.

`check` REJECTS a `*_reference_<state>.animation.json` returned under its own name (the package's own untouched copy is warned, not a delivery), and the game's jar never carries one (the asset audit refuses it).

This creature has NO exact keyframe transcription (owner 2026-09-13, Amendment 2: its motion does not fit the current transcription form, and the game runs its classic code until you deliver `idle` and `walk`). Its source formulas in plain language (migration design section 5 — the source method and line quoted, each formula and constant in words):

- `ScorpionModel.poseFrom` (ScorpionModel.java:196-244; orig ModelScorpion.java:186-248 in `render` (func_78088_a, :177-249; func_78087_a at :257-259 is a bare super call), wingspeed 0.62 from `new ModelScorpion(0.62f)` in orig ClientProxyOreSpawn.java:433 - the port's WINGSPEED): the four leg pairs yaw by the cosine of the age in ticks times 2.0 times the wingspeed, less a quarter turn per pair (0, one, two, three quarters), times pi, times 0.12, times the walking speed - no threshold - about their rests, plus or minus 0.49 and 0.24 for the front two pairs and minus or plus 2.9 and 2.65 for the rear two. The claws and the tail sit on a per-entity latch: on the frame the claw rhythm - the cosine of the age times 3.0 times the wingspeed - crosses zero upward (the next tenth of a tick positive, this one negative) the two latch values are rolled from the entity's random, one in twenty and one in twenty-five at rest, one in four and one in three while attacking; then every frame the first value at 1 or 3 swings the left claw and at 2 or 3 the right, and the second at 1 strikes the tail, each by the cosine of the age times 3.0 times the wingspeed, times pi, times 0.15, else 0. A swinging claw's arm yaws about its rest (0.52 the left, 2.61 the right) and its wrist and pincer are placed 4.5 times the sine of that yaw along and a further 3 units in z. The tail's six links pitch cumulatively - 0.26 plus the angle on the first, 0.769 plus it on the second and third, 0.701 plus it on the fourth, and minus 5.501 minus one and a half times it minus 0.4 on the last - with each pivot following the link below by 4, 4, 3, 4 and 4 units along minus the sine (y) and the cosine (z) of that link's pitch. The walk position plays no part. The GeckoLib hook is the same (ScorpionGeoReplacement.applyCustomAnimations).

## 5. Clips: what to improve, what to leave (AUTHORED verdicts on generated rows)
> **DRAFT** — DRAFT — authored by a seed lane from the entity code, 2026-09-13; the owner's edit pending, landed on its hook by the remainder slice (2026-09-15). Nothing in this section is ruled: it was drafted from the entity code and the model, every claim citing its line, for the owner's edit.


| clip | loop | layer | weight / trigger | bones it may animate | length | code-triggered | verdict | notes |
|---|---|---|---|---|---|---|---|---|
| `idle` | true | base | w_idle = (1 - w_move)(1 - w_swim)(1 - w_fly) | any of the 22 bones — gait group (speed-scaled): lleg1, lleg2, lleg3, lleg4, rleg1, rleg2, rleg3, rleg4; free: body, head; other groups' bones: larm1 (better left to `idle_claws`), larm2 (better left to `idle_claws`), lclaw (better left to `idle_claws`), rarm1 (better left to `idle_claws`), rarm2 (better left to `idle_claws`), rclaw (better left to `idle_claws`), tail1 (better left to `idle_tail`), tail2 (better left to `idle_tail`), tail3 (better left to `idle_tail`), tail4 (better left to `idle_tail`), tail5 (better left to `idle_tail`), tail6 (better left to `idle_tail`) | 1 s | no | author | REQUIRED with walk: the gate opens on the pair (today nothing ships; the classic code plays) — the rest: legs at their splay, claws and tail in the zero pose (ScorpionModel.java:230-233 the else branches); the random snaps are the `claw_snap` and `tail_whip` extras |
| `idle_claws` | true | parallel layer | w_idle = (1 - w_move)(1 - w_swim)(1 - w_fly) (group claws) | larm2, larm1, lclaw, rarm2, rarm1, rclaw | 1 s | no | author |  |
| `idle_tail` | true | parallel layer | w_idle = (1 - w_move)(1 - w_swim)(1 - w_fly) (group tail) | tail1, tail2, tail3, tail4, tail5, tail6 | 1 s | no | author |  |
| `walk` | true | base | w_walk = w_move (1 - w_swim)(1 - w_fly); the gait group additionally x limbSwingAmount (P3) | any of the 22 bones — gait group (speed-scaled): lleg1, lleg2, lleg3, lleg4, rleg1, rleg2, rleg3, rleg4; free: body, head; other groups' bones: larm1 (better left to `walk_claws`), larm2 (better left to `walk_claws`), lclaw (better left to `walk_claws`), rarm1 (better left to `walk_claws`), rarm2 (better left to `walk_claws`), rclaw (better left to `walk_claws`), tail1 (better left to `walk_tail`), tail2 (better left to `walk_tail`), tail3 (better left to `walk_tail`), tail4 (better left to `walk_tail`), tail5 (better left to `walk_tail`), tail6 (better left to `walk_tail`) | 1 s | no | author | REQUIRED with idle: the locomotion loop — the eight-leg wave at 1.24 rad/tick, 22 degrees x the walking speed with a pi/2 lag per pair (:193-204); the reference clip is that wave at full stride |
| `walk_claws` | true | parallel layer | w_walk = w_move (1 - w_swim)(1 - w_fly); the gait group additionally x limbSwingAmount (P3) (group claws) | larm2, larm1, lclaw, rarm2, rarm1, rclaw | 1 s | no | author |  |
| `walk_tail` | true | parallel layer | w_walk = w_move (1 - w_swim)(1 - w_fly); the gait group additionally x limbSwingAmount (P3) (group tail) | tail1, tail2, tail3, tail4, tail5, tail6 | 1 s | no | author |  |
| `swim` | true | base | w_swim from isInWater() | any of the 22 bones — gait group (speed-scaled): lleg1, lleg2, lleg3, lleg4, rleg1, rleg2, rleg3, rleg4; free: body, head; other groups' bones: larm1 (better left to `swim_claws`), larm2 (better left to `swim_claws`), lclaw (better left to `swim_claws`), rarm1 (better left to `swim_claws`), rarm2 (better left to `swim_claws`), rclaw (better left to `swim_claws`), tail1 (better left to `swim_tail`), tail2 (better left to `swim_tail`), tail3 (better left to `swim_tail`), tail4 (better left to `swim_tail`), tail5 (better left to `swim_tail`), tail6 (better left to `swim_tail`) | 1 s | no | leave | no swim behaviour beyond the FloatGoal that keeps it at the surface (EntityScorpion.java:62); the code draws the same wave in water; the fallback to walk covers it (section 2.4) |
| `aggro_idle` | true | base | w_idle x w_aggro (DATA_ATTACKING held) | any of the 22 bones — gait group (speed-scaled): lleg1, lleg2, lleg3, lleg4, rleg1, rleg2, rleg3, rleg4; free: body, head; other groups' bones: larm1 (better left to `idle_claws`), larm2 (better left to `idle_claws`), lclaw (better left to `idle_claws`), rarm1 (better left to `idle_claws`), rarm2 (better left to `idle_claws`), rclaw (better left to `idle_claws`), tail1 (better left to `idle_tail`), tail2 (better left to `idle_tail`), tail3 (better left to `idle_tail`), tail4 (better left to `idle_tail`), tail5 (better left to `idle_tail`), tail6 (better left to `idle_tail`) | 1 s | no | author | DATA_ATTACKING held at 1 while it stands in reach (EntityScorpion.java:43-44, :100-102; BugMeleeAttackGoal.java:168): the claws snapping one cycle in two and the tail whipping one in three (ScorpionModel.java:224-227) — a raised-tail threat pose with the claws open would carry it |
| `calm_idle` | true | base | w_idle x (1 - w_aggro) | any of the 22 bones — gait group (speed-scaled): lleg1, lleg2, lleg3, lleg4, rleg1, rleg2, rleg3, rleg4; free: body, head; other groups' bones: larm1 (better left to `idle_claws`), larm2 (better left to `idle_claws`), lclaw (better left to `idle_claws`), rarm1 (better left to `idle_claws`), rarm2 (better left to `idle_claws`), rclaw (better left to `idle_claws`), tail1 (better left to `idle_tail`), tail2 (better left to `idle_tail`), tail3 (better left to `idle_tail`), tail4 (better left to `idle_tail`), tail5 (better left to `idle_tail`), tail6 (better left to `idle_tail`) | 1 s | no | author | the flag at 0 (BugMeleeAttackGoal.java:132, :145, :176): the rare snaps (one claw cycle in ten, one tail cycle in twenty-five, ScorpionModel.java:221-223) over the same rest as idle |
| `attack` | false | triggered controller | event: a strike (the transport per species by the trigger inventory — ruled 2026-09-06, Q11 (a); §6 says which applies here) | (any unlocked) | 0.4 s | yes | author | the sting: doHurtTarget (EntityScorpion.java:151-160) for 4 with scorpion_attack at the victim one time in three, rolled by the melee goal within 3 blocks plus half the target's width on a 1-in-6 cadence tick (BugMeleeAttackGoal.java:159-174; Params.scorpion: nextInt(5) == 0 or nextInt(6) == 1); the flag is held while in reach, not pulsed at the strike, so the strike is transport 1 (the server LivingDamageEvent.Post) — a tail stab over the head |
| `hurt` | false | triggered controller | event: hurtTime rising edge (client-observed); the red overlay stays (ruled 2026-09-06, Q4 (a)) | (any unlocked) | 0.3 s | yes | author | a flinch: hurt() (EntityScorpion.java:166-170) refuses cactus; the scorpion_hit sound at 1.5 volume (:115-118, :125-128); the hurtTime edge is client-observed |
| `death` | hold_on_last_frame | triggered controller | event: deathTime > 0; the vanilla death flip stays — the pilot ships no death clip (ruled 2026-09-06, Q3 (a)); a creature whose JSON ships one gets the clip-replaces-flip mode when the first such clip arrives, so a `death` delivered here plays under the flip until then | (any unlocked) | 1 s | yes | author | ruled 2026-09-06, Q3 (a): the vanilla death flip stays; a creature whose JSON ships a `death` clip gets the clip-replaces-flip mode, designed when the first such clip arrives (the Queen's own death clip the precedent) - a `death` delivered here plays under the flip until then |
| `claw_snap` | false | overlay | the claw latch (ScorpionModel.java:207-229): at each zero crossing of the 3.4-tick cycle a roll on the per-entity RenderInfo selects the left claw (ri1 == 1 or 3), the right (2 or 3) or both for one cycle - nextInt(20) at rest, nextInt(4) while attacking (:221-227); a client-side roll — an idle variant when the rig lands (the idle_alt_N roll, ruled 2026-09-06, Q5 (a), is the natural home) | larm2, larm1, lclaw, rarm2, rarm1, rclaw | 0.2 s | no | author | one pinch of a claw, or both |
| `tail_whip` | false | overlay | the tail latch (ScorpionModel.java:207-233): at each zero crossing ri2 = nextInt(25) at rest or nextInt(3) while attacking, and ri2 == 1 plays one 27-degree curl of the chain (:278-292); a client-side roll — an idle variant when the rig lands (Q5 (a)) | tail1, tail2, tail3, tail4, tail5, tail6 | 0.2 s | no | author | one whip of the tail with the stinger snapping forward |

Loop values are the JSON `loop` field exactly: `true` for cycles, `false` for one-shots, `hold_on_last_frame` only where written. Every name in this table is fixed (contract §2.4); `idle_alt_1`, `idle_alt_2`, ... may be added freely. A sheet may carry up to four extra clips of this creature's own beyond the contract's names (§2.3; ruled 2026-09-06, Q6 (a)) — more needs the owner's ruling; the extras rows above, if any, are those.

### 5.1 Wishlist (AUTHORED — only what `check` accepts today; anything else is marked)
> **DRAFT** — DRAFT — authored by a seed lane from the entity code, 2026-09-13; the owner's edit pending, landed on its hook by the remainder slice (2026-09-15). Nothing in this section is ruled: it was drafted from the entity code and the model, every claim citing its line, for the owner's edit.


- in `walk`: the leg wave (ScorpionModel.java:193-204) is yaw-only — the legs never lift; add a lift on each leg's forward sweep so the wave reads as steps
- in `attack`: a stab — the code's tail cycle (:278-292) is a random curl, not aimed; a deliberate over-the-head strike on the bite (EntityScorpion.java:151-160)
- in `aggro_idle`: a raised tail and open claws as a held threat, with the code's frequent snaps (:224-227) over it
- in `idle`: a slow breath of the body and the rare snaps (:221-223) as idle variants

## 6. Trigger inventory (generated from the entity's AI goals and state flags)

Source: `src/main/java/danger/orespawn/entity/EntityScorpion.java` (Monster, ScorpionPose)

| selector | prio | goal | guard | category | what it does | bears on |
|---|---|---|---|---|---|---|
| goalSelector | 0 | `FloatGoal` | - | locomotion | bobs up to the surface in water (vanilla) | swim state (client reads isInWater) |
| goalSelector | 1 | `BugMeleeAttackGoal` | - | attack | OreSpawn bug melee | attack (event) |
| goalSelector | 2 | `MyEntityAIWanderALot` | - | locomotion | OreSpawn's restless wander: picks a new spot often | walk |
| goalSelector | 3 | `LookAtPlayerGoal` | - | look | turns the head toward a nearby player (vanilla; head yaw/pitch only) | none: head look, not a clip |
| goalSelector | 4 | `RandomLookAroundGoal` | - | look | looks around idly (vanilla; head yaw/pitch only) | none: head look, not a clip |

A `[modern: key]` guard means the goal is registered only under the modern config key named (read once, at construction); `UNPARSED` means the registration's shape is one the generator does not read (a local variable or a computed priority) — the owner reads that line.

**Synched state flags** (what the client can see): `DATA_ATTACKING` (Integer, line 44)

**Attacking flag `DATA_ATTACKING`** — classified **STATE** (cleared when the target is lost at line(s) [145, 176]; other clears at line(s) [153] (guards: if (this.params.forgetTargetRoll() > 0 && this.mob.getRandom().nextInt(this.params.forgetTargetRoll()) == 0)) — the owner reads them) — a mechanical reading; the owner confirms it against the sites:

| line | method | sets | guard (the enclosing block) | within | comment |
|---|---|---|---|---|---|
| ai/BugMeleeAttackGoal.java:132 | stop | 0 | (unguarded: the method body) | - |  [via BugMeleeAttackGoal (registered at line 68)] |
| ai/BugMeleeAttackGoal.java:145 | tick | 0 | if (target == null \|\| !target.isAlive()) | - |  [via BugMeleeAttackGoal (registered at line 68)] |
| ai/BugMeleeAttackGoal.java:153 | tick | 0 | if (this.params.forgetTargetRoll() > 0 && this.mob.getRandom().nextInt(this.params.forgetTargetRoll()) == 0) | - |  [via BugMeleeAttackGoal (registered at line 68)] |
| ai/BugMeleeAttackGoal.java:168 | tick | 1 | if (distSq < reachSq) | - |  [via BugMeleeAttackGoal (registered at line 68)] |
| ai/BugMeleeAttackGoal.java:176 | tick | 0 | NOT(if (distSq < reachSq)) | - |  [via BugMeleeAttackGoal (registered at line 68)] |

The guard is the header of the block that actually encloses the write (found by brace depth, comments and strings blanked); an `else` branch is written as the negated `if` chain it closes; a header the parser cannot read is `(unparsed: ...)`, never dropped.

The flag's writes traced into the goal class the entity hands its setter to (TEST-011 (b); the `[via ...]` rows above): `BugMeleeAttackGoal` registered at line 68 (`this, this::setAttacking, BugMeleeAttackGoal.Params.scorpion()`) — ai/BugMeleeAttackGoal.java, sites ai/BugMeleeAttackGoal.java:132, ai/BugMeleeAttackGoal.java:145, ai/BugMeleeAttackGoal.java:153, ai/BugMeleeAttackGoal.java:168, ai/BugMeleeAttackGoal.java:176.

**Locomotion facts:** uses a per-entity RenderInfo scratch (client-only, never synced). Overrides: hurt, doHurtTarget, customServerAiStep.

**Strike and launch sites:**

- melee `doHurtTarget(target)` — EntityScorpion.java:154 in `doHurtTarget`, guard: (unparsed: boolean hit =)
- melee `doHurtTarget(target)` — ai/BugMeleeAttackGoal.java:172 in `tick`, guard: (unparsed: boolean hit =) within if (this.mob.getRandom().nextInt(this.params.outerAttackRoll()) == 0 || this.mob.getRandom().nextInt(this.params.innerAttackRoll()) == 1) within if (distSq < reachSq)

**What fires each contract clip:**

| clip | signal | verdict |
|---|---|---|
| `walk` | limbSwingAmount from AnimationState (the seam's input; 0 at rest, 1 at full stride) | the gait group's weight and speed scale (P3) |
| `idle` | w_idle = (1 - w_move)(1 - w_swim)(1 - w_fly) | standing still |
| `swim` | Entity.isInWater() (client-evaluated); FloatGoal keeps it at the surface | falls back to walk then idle when absent (§2.4) |
| `aggro_idle / calm_idle` | DATA_ATTACKING held while engaged (cleared when the target is lost at line(s) [145, 176]; other clears at line(s) [153] (guards: if (this.params.forgetTargetRoll() > 0 && this.mob.getRandom().nextInt(this.params.forgetTargetRoll()) == 0)) — the owner reads them) | STATE flag drives w_aggro (§4.3) |
| `attack` | melee: LivingDamageEvent.Post -> triggerAnim packet (transport 1); ranged: named launch sites (transport 3) | the transport per species by the trigger inventory (ruled 2026-09-06, Q11 (a)): this flag is held, not pulsed at the strike, so the signal column's transport applies — the server LivingDamageEvent.Post -> triggerAnim packet for melee, a named launch site for ranged |
| `hurt` | rising edge of LivingEntity.hurtTime (0 -> 10), client-observed | always available; the red overlay stays (ruled 2026-09-06, Q4 (a)) |
| `death` | LivingEntity.deathTime > 0, client-observed | hold_on_last_frame; the vanilla death flip stays and the pilot ships no death clip (ruled 2026-09-06, Q3 (a)); a creature whose JSON ships a `death` clip gets the clip-replaces-flip mode, designed when the first such clip arrives (the Queen's own death clip the precedent) — until then a delivered `death` plays under the flip |
| `idle_alt_N` | a roll at each idle loop boundary (p = 0.15) while w_idle > 0.9 | optional; one roll per idle loop boundary, p = 0.15, a uniform choice, only while the idle weight is above 0.9 and no triggered clip plays, keyed on the clip-clock cycle index (ruled 2026-09-06, Q5 (a)) |

## 7. Hitbox bones that must keep their names

No MultiHitboxLib profile: no locked bones. Every bone name is still immutable (the code poses bones by name).

## 8. Textures

| canonical file (edit this one) | canvas | also shipped as (fan-out on import) | referenced by the code as | note |
|---|---|---|---|---|
| `scorpion.png` | 88x24 | - | scorpion.png |  |

## 9. Reference screenshots

`reference/` holds the slots listed in `reference/SLOTS.md`; the owner fills them from the dev client (fixed camera, light and state per slot). Empty until then.

## 10. Effort and priority

- Artist scope: full contract (Tier 2, the state-branching class: geckolib_migration_design.md section 5); ON THE HOOK until idle and walk are delivered (Amendment 2): in-game through the seam behind the dev switch since the remainder slice (2026-09-15).
- Estimated effort: 21.4 h (generator: 4 h + 0.2 h/bone x 22 + 1 h/clip x 13 to author or improve (owner adjusts)).
- Tier 2 (state-branching — setupAnim branches on entity state).
- Density statement (the harness's, not the artist's; ruled 2026-09-06, Q9 (a) and Q10): the classic transcription is verified at 2.5e-3 rad — Beaver reference leg 15 / 13 / 8 catmullrom keys per bone with spline arguments repaired at load; wrap sample T−ε vs 0+ε included; the key counts per bone are an output of the harness, re-derived on every re-transcription.

## 11. What 'done' looks like for this entity

1. `scorpion.geo.json` returned UNCHANGED (or not at all): every bone name, parent, pivot, rotation and cube as listed in §3 and the shipped file (a `locked` bone renamed, re-parented or deleted is refused by name).
2. `scorpion.animation.json` — that exact file name, one file — (format 1.8.0) holding the clips of §5 with the loop values shown; keys on `locked` bones: Keying a locked bone is allowed; the checker warns, and the hitbox part follows the bone in-game (the consequence, so keep such keys deliberate). Renaming, re-parenting or deleting a locked bone is refused.
3. Textures at their canvas sizes under the canonical names of §8 (aliases are fanned out on import).
4. Optionally the `.bbmodel` working file. `tools/artist_package.py check <your folder>` must pass before hand-in. It REJECTS: a missing or empty folder, or a missing manifest; no `.animation.json`, one under the wrong name (the sheet names the file), or a second one; a clip not in the sheet (renamed or added; `idle_alt_N` only where the sheet allows it), or a clip delivered for a Tier-3 creature; a wrong `loop` value; a missing `idle` or `walk` (they open the game's switch only TOGETHER — one without the other leaves the creature on its code-driven motion); a missing clip the game already triggers by name; a key on a bone the rig does not have (renamed), or a channel other than rotation / position / scale; a Molang string, a non-numeric or non-finite value, a custom-instruction (`timeline`) key; an event keyframe (`sound_effects` / `particle_effects`) on a LOOPING clip of a phase-locked species — no event keyframes on loops; code-fired events come from the trigger inventory (one-shot clips may carry them; a creature whose controllers are its own GeckoLib controllers — the SPEC's controller kind `native`, the Queen — is exempt: native controllers are not phase-locked, event keys fire per loop); |rotation| > 3600 degrees or |position| > 1024 units; a key later than the clip's `animation_length` (a declared length shorter than the last key); a returned `.geo.json` whose bones, parents, pivots, rotations, cubes, UVs or canvas differ from the shipped rig; a `locked` bone renamed, re-parented or deleted in a returned `.geo.json` (the finding names the bone); a texture that is not this creature's, or whose canvas size changed; a `*_reference_<state>.animation.json` returned under its own name that is not the package's own untouched copy (the reference-only clips the package carries beside the sheet: the classic code sampled at fixed inputs, never shipped — the finding names the file; the untouched copy coming back is warned, not a delivery; copying a reference clip's KEYS into `idle` / `walk` / `aggro_idle` / `fly` / `swim` or into an offered extra is a delivery and passes); a returned `.bbmodel` that lists fewer clips than the folder's animation files hold, or whose embedded keys leave the round-trip tolerance (key times within 5e-5 s, values within 1e-6) of those files (the files beside the sheet are the pinned source; the `.bbmodel` must open with every clip listed — owner 2026-09-14). It WARNS on: a key on a `locked` bone (the clip and the bones are named) — Keying a locked bone is allowed; the checker warns, and the hitbox part follows the bone in-game (the consequence, so keep such keys deliberate). Renaming, re-parenting or deleting a locked bone is refused; a `_preview` file delivered (a Blockbench-only aid, never in the jar — ruled 2026-09-06, Q15 (a); leave it out of the delivery); a returned `.geo.json` at all (the shipped rig is used regardless — do not re-export it); a clip length far from the one the sheet states (rule 5: a phase-locked creature's loops at 1.0 s — ruled 2026-09-06, Q14 (a); a native creature's clips at their shipped length; a one-shot at the length its row states); an optional clip not delivered; an alias-named texture (the canonical name is preferred); a `format_version` other than 1.8.0; a number written as a string; an unknown keyframe key; a clip without `animation_length`; an unreadable `.bbmodel`.

