# Pointysaurus — `pointysaurus` — artist SPEC

**This rig is NOT yet in-game: the geometry is the converter's output over the port's compiled model, proven part for part against the 1.7.10 source by the reference-geometry leg; it lands through the seam in a later slice. Bone names are FINAL — the seam, the hitbox profiles and the transcriptions find bones by name.**

Generated by `tools/artist_package.py` 0.2.9 (the full folder, 2026-09-13, fourth set, items 5, 7 and 8: every artist-tier species packaged - a rig not yet in-game from the reference leg's converter output, bone names final; the Tier-1 bosses' intended locked bones from the design's section 6 rendered in section 7 and validated against the geo; the README's priority table the full list, bosses first, closing with the deliverable count) from the repository's own sources; the paragraphs marked AUTHORED come from `tools/artist_specs/pointysaurus.json` (DRAFT — authored by a seed lane from the entity code, 2026-09-13; the owner's edit pending). Every contract decision this sheet rests on was ruled by the mod's owner on 2026-09-06 (`phase_g_reports/animation_contract/open_questions.md` records each ruling); a line that cites one ("ruled 2026-09-06, Q14 (a)") states that ruling — nothing here invents an answer. The one open item is the lock reject mode (§7, README rule 6).

## 1. What this mob is (AUTHORED — draft for the owner's edit)
> **DRAFT** — DRAFT — authored by a seed lane from the entity code, 2026-09-13; the owner's edit pending. Nothing in this section is ruled: it was drafted from the entity code and the model, every claim citing its line, for the owner's edit.


A hostile three-horned dinosaur: a 22x9x30 body slab with two upper body blocks, four stumpy block legs, a 12x10x12 head with a nose, two 23-long brow horns and a nose horn, a 28x23 neck frill studded with sixteen bumps, and a stub tail (ModelPointysaurus.java:92-240, 30 parts). 80 HP, 10 damage, 16 armour, speed 0.35 (Pointysaurus.java:219-228). It hunts players only: any player whose box meets its own grown 12 x 5 x 12, on a 1-in-6 cadence (:123-152), and - a modern addition behind a config flag - any player who stares at it from within 32 blocks for five ticks (:83-91; PointysaurusStareGoal.java:47-59, :86-96); it chases at 1.25 and, within 4 blocks plus half the target's width, rolls its gore on a 1-in-6 cadence (:62-64; DinosaurMeleeAttackGoal.Presets.pointysaurus; BugMeleeAttackGoal.java:159-174), landing 10 with a 0.8 knockback (:269-284). The synched DATA_ATTACKING int (:44-45) is held at 1 while it stands in reach (BugMeleeAttackGoal.java:168) and dropped when it chases or stands down (:132, :145, :176). It spawns in the dark at night above y 50 under five blocks of clear air (:303-312; ModEntities.java:125-126). Read it as a triceratops: a four-legged plod, the head and frill swinging with the look, the tail lashing when it closes in.

## 2. Size and scale in-game

- Hitbox (EntityType dims, `ModEntities.java`): 2.9 x 2.9 blocks (wide x tall).
- Renderer scale: x1; shadow radius 1 (reference pin status: pin, `tools/reference_renderer_pins.json`).
- Rig units: 16 units = 1 block before the renderer scale, so a 2.9-block-tall hitbox is 46.4 rig units tall at x1.
- Texture canvas: 128 x 128 (keep it; the canvas size is checked on return).
- Notes (AUTHORED): Drawn at x1.0 of the rig (PointysaurusRenderer.java:20, :30; shadow 1.0, :23) on a 128x128 sheet (ModelPointysaurus.java:242), pointysaurus.png (PointysaurusRenderer.java:15); the hitbox is 2.9 x 2.9 (ModEntities.java:125-126). The original's ModelPointysaurus wingspeed is 1.0 (ModelPointysaurus.java:23).

## 3. Bone glossary (locked legacy names — readable labels beside them)

Never rename, delete or re-parent a bone: code and hitboxes reference them by name. Left/right in the labels follow the model author's own naming (the side the legacy name calls left); Blockbench mirrors X for display, so the author's left appears on your right when the mob faces you. `gait bone` = its motion is scaled by walking speed in-game (P3). `locked` = it carries or parents a hitbox part (contract §8.1). Keying a locked bone is allowed; the checker warns, and the hitbox part follows the bone in-game (the consequence, so keep such keys deliberate). Renaming, re-parenting or deleting a locked bone is refused.

This rig is NOT yet in-game: the geometry is the converter's output over the port's compiled model, proven part for part against the 1.7.10 source by the reference-geometry leg; it lands through the seam in a later slice. Bone names are FINAL — the seam, the hitbox profiles and the transcriptions find bones by name.

| bone | readable label | classic part | parent | pivot (x, y, z) | group | gait bone | locked | cubes |
|---|---|---|---|---|---|---|---|---|
| `body1` | body (the 22x9x30 slab; static) | body1 (ModelPart of the same name) | - | (7, 15, -12) | - | no | no | 1 |
| `body2` | upper body, front block (18x7x15; static) | body2 (ModelPart of the same name) | - | (0, 22, -9) | - | no | no | 1 |
| `body3` | upper body, rear block (16x6x11; static) | body3 (ModelPart of the same name) | - | (0, 21, 6) | - | no | no | 1 |
| `bump1` | frill bump, right edge 1 (2x2x2; rides the frill) | bump1 (ModelPart of the same name) | - | (0, 13, -7) | - | no | no | 1 |
| `bump10` | frill bump, left edge 5, the lowest (rides the frill) | bump10 (ModelPart of the same name) | - | (0, 13, -7) | - | no | no | 1 |
| `bump11` | frill bump, top edge 1, right (rides the frill) | bump11 (ModelPart of the same name) | - | (0, 13, -7) | - | no | no | 1 |
| `bump12` | frill bump, top edge 2 (rides the frill) | bump12 (ModelPart of the same name) | - | (0, 13, -7) | - | no | no | 1 |
| `bump13` | frill bump, top edge 3 (rides the frill) | bump13 (ModelPart of the same name) | - | (0, 13, -7) | - | no | no | 1 |
| `bump14` | frill bump, top edge 4 (rides the frill) | bump14 (ModelPart of the same name) | - | (0, 13, -7) | - | no | no | 1 |
| `bump15` | frill bump, top edge 5 (rides the frill) | bump15 (ModelPart of the same name) | - | (0, 13, -7) | - | no | no | 1 |
| `bump16` | frill bump, top edge 6, left (rides the frill) | bump16 (ModelPart of the same name) | - | (0, 13, -7) | - | no | no | 1 |
| `bump2` | frill bump, right edge 2 (rides the frill) | bump2 (ModelPart of the same name) | - | (0, 13, -7) | - | no | no | 1 |
| `bump3` | frill bump, right edge 3 (rides the frill) | bump3 (ModelPart of the same name) | - | (0, 13, -7) | - | no | no | 1 |
| `bump4` | frill bump, right edge 4 (rides the frill) | bump4 (ModelPart of the same name) | - | (0, 13, -7) | - | no | no | 1 |
| `bump5` | frill bump, right edge 5, the lowest (rides the frill) | bump5 (ModelPart of the same name) | - | (0, 13, -7) | - | no | no | 1 |
| `bump6` | frill bump, left edge 1 (rides the frill) | bump6 (ModelPart of the same name) | - | (0, 13, -7) | - | no | no | 1 |
| `bump7` | frill bump, left edge 2 (rides the frill) | bump7 (ModelPart of the same name) | - | (0, 13, -7) | - | no | no | 1 |
| `bump8` | frill bump, left edge 3 (rides the frill) | bump8 (ModelPart of the same name) | - | (0, 13, -7) | - | no | no | 1 |
| `bump9` | frill bump, left edge 4 (rides the frill) | bump9 (ModelPart of the same name) | - | (0, 13, -7) | - | no | no | 1 |
| `chorn` | nose horn (3 wide; follows the head) | chorn (ModelPart of the same name) | - | (0, 13, -7) | - | no | no | 1 |
| `guard` | neck frill (a 28x23x3 plate raked back 15 deg behind the head; follows the head's yaw and pitch, 0.262 rad back) | guard (ModelPart of the same name) | - | (0, 13, -7) | - | no | no | 1 |
| `head` | head (12x10x12, pitched down 11 deg; yaws and pitches with the look at 0.45) | head (ModelPart of the same name) | - | (0, 13, -7) | - | no | no | 1 |
| `lfleg` | left front leg (a 6x8x6 block; the gait, with the right rear) | lfleg (ModelPart of the same name) | - | (-9, 8, -8) | gait | yes | no | 1 |
| `lhorn` | left brow horn (2x2x23, splayed -0.14 yaw and -0.16 pitch off the head; follows the head) | lhorn (ModelPart of the same name) | - | (0, 13, -7) | - | no | no | 1 |
| `lrleg` | left rear leg (an 8x8x8 block; the gait, with the right front) | lrleg (ModelPart of the same name) | - | (-9, 8, 12) | gait | yes | no | 1 |
| `nose` | nose (10x6x5 ahead of the head; follows the head) | nose (ModelPart of the same name) | - | (0, 13, -7) | - | no | no | 1 |
| `rfleg` | right front leg (the gait, with the left rear) | rfleg (ModelPart of the same name) | - | (9, 8, -8) | gait | yes | no | 1 |
| `rhorn` | right brow horn (splayed +0.14 yaw, -0.16 pitch; follows the head) | rhorn (ModelPart of the same name) | - | (0, 13, -7) | - | no | no | 1 |
| `rrleg` | right rear leg (the gait, with the left front) | rrleg (ModelPart of the same name) | - | (9, 8, 12) | gait | yes | no | 1 |
| `tail` | tail (a 6x6x9 stub; sways side to side - slow at rest, a lash while attacking - and lifts on a very slow wave) | tail (ModelPart of the same name) | - | (0, 17, 15) | tail_lift | no | no | 1 |

## 4. Current animation behaviour — plain English and the source formulas

- One gait rhythm above a limbSwingAmount of 0.1 (ModelPointysaurus.java:249-253): the four block legs swing 45 degrees x the walking speed in diagonal pairs at 1.3 rad/tick, a 4.8-tick period - a quick plod at the 0.35 walk (Pointysaurus.java:224).
- The head, nose, nose horn, both brow horns, the frill and its sixteen bumps turn together with the look at 0.45 of the head yaw and pitch (:255-296): the brow horns splayed 0.14 rad out and 0.16 rad down off the head, the frill and bumps 0.262 rad back. The body blocks are static.
- The tail has one branch on the synched DATA_ATTACKING int (Pointysaurus.java:44-45; held at 1 while it stands within 4 blocks plus half the target's width, BugMeleeAttackGoal.java:168, dropped at :132 / :145 / :176): a 9-degree wag at 0.3 rad/tick at rest, a 45-degree lash at 1.3 rad/tick while attacking (:298-299); over both a 27-degree lift-and-drop on a 314-tick wave (:300-301).
- The gore is doHurtTarget (Pointysaurus.java:269-284: 10 of damage and a 0.8 knockback) from the goal's swing roll (BugMeleeAttackGoal.java:170-173, nextInt(5) == 0 or nextInt(6) == 1 on a 1-in-6 cadence tick in reach); the stare lock turns the look control onto the starer's eyes for five ticks before taking the target (PointysaurusStareGoal.java:86-96) - neither is an animation event today.

### 4.1 Tempo table (contract §7.1)

| group | bones | axis | omega (rad/tick) | natural period | amplitude | speed-scaled | Blockbench preview rate | source |
|---|---|---|---|---|---|---|---|---|
| gait | lfleg, rfleg, lrleg, rrleg | x | 1.3 | 4.833 ticks (0.242 s) | pi x 0.25 = 0.785 rad (45 deg) x the walking speed above a limbSwingAmount of 0.1; the left front and right rear one way, the right front and left rear the other; 0 below the threshold | yes | 4.14 clip-ticks per game tick at a 1.0 s authoring length | ModelPointysaurus.java:249-253; wingspeed 1.0 (:23) |
| tail_sway | tail | y | 0.3 | 20.944 ticks (1.047 s) | calm: pi x 0.05 = 0.157 rad (9 deg) at 0.3 rad/tick; attacking: pi x 0.25 = 0.785 rad (45 deg) at 1.3 rad/tick | no | 0.95 clip-ticks per game tick at a 1.0 s authoring length | ModelPointysaurus.java:298-299 |
| tail_lift | tail | x | 0.02 | 314.159 ticks (15.708 s) | pi x 0.15 = 0.471 rad (27 deg) about a 0.28 rad rest | no | 0.06 clip-ticks per game tick at a 1.0 s authoring length | ModelPointysaurus.java:300-301 |

The bare `walk` clip is the gait group's (`gait`, scaled by walking speed in-game); every other group keys `walk_<group>` / `idle_<group>` (contract §2.1).

Author every loop at 1.0 s (README rule 5, the phase-locked kind; ruled 2026-09-06, Q14 (a)). In-game each loop plays once per natural period whatever its length; your timeline's length is the SHAPE's timeline and does not change that (P6). Blockbench previews at the declared length, so a 1.0 s preview of a fast group shows the shape slowed down (contract §9); a `_preview` copy stretched to the natural periods is a Blockbench-only aid, never in the jar and never in a delivery (ruled 2026-09-06, Q15 (a)).

### 4.2 The math

- **gait**: the four block legs swing fore and aft in diagonal pairs with the stride — `a = amount > 0.1 ? cos(t * 1.3 * 1.0) * pi * 0.25 * amount : 0; lfleg.xRot = rrleg.xRot = a; rfleg.xRot = lrleg.xRot = -a` (ModelPointysaurus.java:249-253; wingspeed 1.0 (:23))
- **tail_sway**: the tail sways side to side: a slow small wag at rest, a fast wide lash while the attacking flag is up — `tail.yRot = attacking ? cos(t * 1.3 * 1.0) * pi * 0.25 : cos(t * 0.3 * 1.0) * pi * 0.05` (ModelPointysaurus.java:298-299)
- **tail_lift**: the tail lifts and drops on a very slow wave at all times — `tail.xRot = 0.28 + cos(t * 0.02 * 1.0) * pi * 0.15` (ModelPointysaurus.java:300-301)

### 4.3 Reference clip (reference-only)

_no reference clip yet: it is sampled from the classic hook when the rig lands through the seam; until then §4.1 / §4.2 and the plain-language formulas (where the seed carries them) are the motion's description._

This creature has NO exact keyframe transcription and is not yet on its hook (the rig is not yet in-game: §3). The plain-language transcription of its source formulas (migration design section 5 — the source method and line quoted, each formula and constant in words) is authored when the rig lands on its hook; until then the seed's description, clip verdicts and wishlist (§1, §4, §5) describe the motion.

## 5. Clips: what to improve, what to leave (AUTHORED verdicts on generated rows)
> **DRAFT** — DRAFT — authored by a seed lane from the entity code, 2026-09-13; the owner's edit pending. Nothing in this section is ruled: it was drafted from the entity code and the model, every claim citing its line, for the owner's edit.


| clip | loop | layer | weight / trigger | bones it may animate | length | code-triggered | verdict | notes |
|---|---|---|---|---|---|---|---|---|
| `idle` | true | base | w_idle = (1 - w_move)(1 - w_swim)(1 - w_fly) | any of the 30 bones — gait group (speed-scaled): lfleg, lrleg, rfleg, rrleg; free: body1, body2, body3, bump1, bump10, bump11, bump12, bump13, bump14, bump15, bump16, bump2, bump3, bump4, bump5, bump6, bump7, bump8, bump9, chorn, guard, head, lhorn, nose, rhorn; other groups' bones: tail (better left to `idle_tail_lift`) | 1 s | no | author | REQUIRED with walk: the gate opens on the pair (today nothing ships; the classic code plays) — the stand: the tail's 9-degree wag (ModelPointysaurus.java:299) and slow lift (:300-301), the legs still |
| `idle_tail_sway` | true | parallel layer | w_idle = (1 - w_move)(1 - w_swim)(1 - w_fly) (group tail_sway) | tail | 1 s | no | author |  |
| `idle_tail_lift` | true | parallel layer | w_idle = (1 - w_move)(1 - w_swim)(1 - w_fly) (group tail_lift) | tail | 1 s | no | author |  |
| `walk` | true | base | w_walk = w_move (1 - w_swim)(1 - w_fly); the gait group additionally x limbSwingAmount (P3) | any of the 30 bones — gait group (speed-scaled): lfleg, lrleg, rfleg, rrleg; free: body1, body2, body3, bump1, bump10, bump11, bump12, bump13, bump14, bump15, bump16, bump2, bump3, bump4, bump5, bump6, bump7, bump8, bump9, chorn, guard, head, lhorn, nose, rhorn; other groups' bones: tail (better left to `walk_tail_lift`) | 1 s | no | author | REQUIRED with idle: the locomotion loop — the diagonal 45-degree leg swing at 1.3 rad/tick (:249-253); the reference clip is that plod at full walking speed |
| `walk_tail_sway` | true | parallel layer | w_walk = w_move (1 - w_swim)(1 - w_fly); the gait group additionally x limbSwingAmount (P3) (group tail_sway) | tail | 1 s | no | author |  |
| `walk_tail_lift` | true | parallel layer | w_walk = w_move (1 - w_swim)(1 - w_fly); the gait group additionally x limbSwingAmount (P3) (group tail_lift) | tail | 1 s | no | author |  |
| `swim` | true | base | w_swim from isInWater() | any of the 30 bones — gait group (speed-scaled): lfleg, lrleg, rfleg, rrleg; free: body1, body2, body3, bump1, bump10, bump11, bump12, bump13, bump14, bump15, bump16, bump2, bump3, bump4, bump5, bump6, bump7, bump8, bump9, chorn, guard, head, lhorn, nose, rhorn; other groups' bones: tail (better left to `swim_tail_lift`) | 1 s | no | leave | no swim behaviour beyond the FloatGoal that keeps it at the surface (Pointysaurus.java:62); the code draws the same plod in water; the fallback to walk covers it (section 2.4) |
| `aggro_idle` | true | base | w_idle x w_aggro (DATA_ATTACKING held) | any of the 30 bones — gait group (speed-scaled): lfleg, lrleg, rfleg, rrleg; free: body1, body2, body3, bump1, bump10, bump11, bump12, bump13, bump14, bump15, bump16, bump2, bump3, bump4, bump5, bump6, bump7, bump8, bump9, chorn, guard, head, lhorn, nose, rhorn; other groups' bones: tail (better left to `idle_tail_lift`) | 1 s | no | author | DATA_ATTACKING held at 1 while it stands in reach (Pointysaurus.java:44-45, :296-298; BugMeleeAttackGoal.java:168): the tail lashing 45 degrees at 1.3/tick (ModelPointysaurus.java:298) — a lowered head and the horns forward would carry it |
| `calm_idle` | true | base | w_idle x (1 - w_aggro) | any of the 30 bones — gait group (speed-scaled): lfleg, lrleg, rfleg, rrleg; free: body1, body2, body3, bump1, bump10, bump11, bump12, bump13, bump14, bump15, bump16, bump2, bump3, bump4, bump5, bump6, bump7, bump8, bump9, chorn, guard, head, lhorn, nose, rhorn; other groups' bones: tail (better left to `idle_tail_lift`) | 1 s | no | author | the flag at 0 (BugMeleeAttackGoal.java:132, :145, :176): the tail's slow 9-degree wag (ModelPointysaurus.java:299) over the same stand as idle |
| `attack` | false | triggered controller | event: a strike (the transport per species by the trigger inventory — ruled 2026-09-06, Q11 (a); §6 says which applies here) | (any unlocked) | 0.5 s | yes | author | the gore: doHurtTarget (Pointysaurus.java:269-284) for 10 with a 0.8 knockback, rolled by the melee goal within 4 blocks plus half the target's width on a 1-in-6 cadence tick (BugMeleeAttackGoal.java:159-174; Presets.pointysaurus); the flag is held while in reach, not pulsed at the strike, so the strike is transport 1 (the server LivingDamageEvent.Post) — a head toss with the three horns |
| `hurt` | false | triggered controller | event: hurtTime rising edge (client-observed); the red overlay stays (ruled 2026-09-06, Q4 (a)) | (any unlocked) | 0.4 s | yes | author | a flinch: hurt() (Pointysaurus.java:286-290) refuses cactus and stores the attacker for the revenge goal (:68-69); the alo_hurt sound at 0.9 volume and a 1.5 pitch (:249-252, :259-267); the hurtTime edge is client-observed |
| `death` | hold_on_last_frame | triggered controller | event: deathTime > 0; the vanilla death flip stays — the pilot ships no death clip (ruled 2026-09-06, Q3 (a)); a creature whose JSON ships one gets the clip-replaces-flip mode when the first such clip arrives, so a `death` delivered here plays under the flip until then | (any unlocked) | 1.5 s | yes | author | ruled 2026-09-06, Q3 (a): the vanilla death flip stays; a creature whose JSON ships a `death` clip gets the clip-replaces-flip mode, designed when the first such clip arrives (the Queen's own death clip the precedent) - a `death` delivered here plays under the flip until then |
| `stare` | false | overlay | PointysaurusStareGoal (registered when the config flag is on, Pointysaurus.java:83-91): a non-creative player within 32 blocks looking straight at it (dot > 0.97, line of sight; PointysaurusStareGoal.java:47-59) is tracked by the look control and, after five ticks, taken as the target (:68-70, :86-96); the moment the target is set has no packet today — a named site (transport 3) when the rig lands | head, nose, chorn, lhorn, rhorn, guard, bump1, bump2, bump3, bump4, bump5, bump6, bump7, bump8, bump9, bump10, bump11, bump12, bump13, bump14, bump15, bump16 | 0.5 s | no | author | the tell on the lock: a head drop and a frill shake at the starer |
| `roar` | false | overlay | getAmbientSound (Pointysaurus.java:241-247): the vanilla ambient roll answers alo_living one time in four, at 0.9 volume and a 1.5 pitch (:259-267); a sound the server plays, no animation hook today — a named site (transport 3) when the rig lands | head, nose, chorn, lhorn, rhorn, guard | 0.8 s | no | author | a short head-up bellow |

Loop values are the JSON `loop` field exactly: `true` for cycles, `false` for one-shots, `hold_on_last_frame` only where written. Every name in this table is fixed (contract §2.4); `idle_alt_1`, `idle_alt_2`, ... may be added freely. A sheet may carry up to four extra clips of this creature's own beyond the contract's names (§2.3; ruled 2026-09-06, Q6 (a)) — more needs the owner's ruling; the extras rows above, if any, are those.

### 5.1 Wishlist (AUTHORED — only what `check` accepts today; anything else is marked)
> **DRAFT** — DRAFT — authored by a seed lane from the entity code, 2026-09-13; the owner's edit pending. Nothing in this section is ruled: it was drafted from the entity code and the model, every claim citing its line, for the owner's edit.


- in `walk`: the four legs are single blocks swung 45 degrees (ModelPointysaurus.java:249-253) — a ceratopsian's rolling plod with a body sway sells the weight; keep the diagonal pairing
- in `aggro_idle`: the code lashes only the tail (:298); drop the head and bring the horns forward so the in-reach state (BugMeleeAttackGoal.java:168) reads as a threat
- in `attack`: a horn toss — the head is look-only in the code (:255-296); the gore lands 10 within 4 blocks plus half the target's width (Pointysaurus.java:269-284)
- in `idle`: a head sway with the frill and a slow tail lift — the code already lifts the tail on a 314-tick wave (:300-301); keep that as the breath

## 6. Trigger inventory (generated from the entity's AI goals and state flags)

Source: `src/main/java/danger/orespawn/entity/Pointysaurus.java` (Monster)

| selector | prio | goal | guard | category | what it does | bears on |
|---|---|---|---|---|---|---|
| goalSelector | 0 | `FloatGoal` | - | locomotion | bobs up to the surface in water (vanilla) | swim state (client reads isInWater) |
| goalSelector | 1 | `DinosaurMeleeAttackGoal` | - | attack | OreSpawn dinosaur melee | attack (event) |
| goalSelector | 2 | `MyEntityAIWanderALot` | - | locomotion | OreSpawn's restless wander: picks a new spot often | walk |
| goalSelector | 3 | `LookAtPlayerGoal` | - | look | turns the head toward a nearby player (vanilla; head yaw/pitch only) | none: head look, not a clip |
| goalSelector | 4 | `RandomLookAroundGoal` | - | look | looks around idly (vanilla; head yaw/pitch only) | none: head look, not a clip |
| targetSelector | 1 | `RevengeGoal` | - | targeting | the species' own revenge target goal (an inner class) | aggro state (through the attacking flag where one exists) |
| targetSelector | 2 | `PointysaurusStareGoal` | [modern: pointysaurusStareAggro] | targeting | eye-contact aggression (a modern addition, TARGET flag): finds the nearest survival player within 32 blocks whose view vector points at the dino (dot > 0.97) with line of sight, stares back at them and after 5 held ticks sets them as the combat target; drops when they look away or leave range (PointysaurusStareGoal.java:43-96) | aggro state (it sets the target; the stare itself is head look, not a clip) |
| targetSelector | 3 | `NearestAttackableTargetGoal` | - | targeting | hunts the nearest attackable target (vanilla) | aggro state |

A `[modern: key]` guard means the goal is registered only under the modern config key named (read once, at construction); `UNPARSED` means the registration's shape is one the generator does not read (a local variable or a computed priority) — the owner reads that line.

**Synched state flags** (what the client can see): `DATA_ATTACKING` (Integer, line 44)

**Attacking flag `DATA_ATTACKING`** — classified **STATE** (cleared when the target is lost at line(s) [145, 176]; other clears at line(s) [153] (guards: if (this.params.forgetTargetRoll() > 0 && this.mob.getRandom().nextInt(this.params.forgetTargetRoll()) == 0)) — the owner reads them) — a mechanical reading; the owner confirms it against the sites:

| line | method | sets | guard (the enclosing block) | within | comment |
|---|---|---|---|---|---|
| ai/BugMeleeAttackGoal.java:132 | stop | 0 | (unguarded: the method body) | - |  [via DinosaurMeleeAttackGoal (registered at line 63)] |
| ai/BugMeleeAttackGoal.java:145 | tick | 0 | if (target == null \|\| !target.isAlive()) | - |  [via DinosaurMeleeAttackGoal (registered at line 63)] |
| ai/BugMeleeAttackGoal.java:153 | tick | 0 | if (this.params.forgetTargetRoll() > 0 && this.mob.getRandom().nextInt(this.params.forgetTargetRoll()) == 0) | - |  [via DinosaurMeleeAttackGoal (registered at line 63)] |
| ai/BugMeleeAttackGoal.java:168 | tick | 1 | if (distSq < reachSq) | - |  [via DinosaurMeleeAttackGoal (registered at line 63)] |
| ai/BugMeleeAttackGoal.java:176 | tick | 0 | NOT(if (distSq < reachSq)) | - |  [via DinosaurMeleeAttackGoal (registered at line 63)] |

The guard is the header of the block that actually encloses the write (found by brace depth, comments and strings blanked); an `else` branch is written as the negated `if` chain it closes; a header the parser cannot read is `(unparsed: ...)`, never dropped.

The flag's writes traced into the goal class the entity hands its setter to (TEST-011 (b); the `[via ...]` rows above): `DinosaurMeleeAttackGoal` registered at line 63 (`this, this::setAttacking, DinosaurMeleeAttackGoal.Presets.pointysaurus()`) — ai/DinosaurMeleeAttackGoal.java, ai/BugMeleeAttackGoal.java, sites ai/BugMeleeAttackGoal.java:132, ai/BugMeleeAttackGoal.java:145, ai/BugMeleeAttackGoal.java:153, ai/BugMeleeAttackGoal.java:168, ai/BugMeleeAttackGoal.java:176.

**Locomotion facts:** a walker. Overrides: hurt, doHurtTarget, customServerAiStep, removeWhenFarAway.

**Strike and launch sites:**

- melee `doHurtTarget(target)` — Pointysaurus.java:271 in `doHurtTarget`
- melee `doHurtTarget(target)` — ai/BugMeleeAttackGoal.java:172 in `tick`, guard: (unparsed: boolean hit =) within if (this.mob.getRandom().nextInt(this.params.outerAttackRoll()) == 0 || this.mob.getRandom().nextInt(this.params.innerAttackRoll()) == 1) within if (distSq < reachSq)

**What fires each contract clip:**

| clip | signal | verdict |
|---|---|---|
| `walk` | limbSwingAmount from AnimationState (the seam's input; 0 at rest, 1 at full stride) | the gait group's weight and speed scale (P3) |
| `idle` | w_idle = (1 - w_move)(1 - w_swim)(1 - w_fly) | standing still |
| `swim` | Entity.isInWater() (client-evaluated); FloatGoal keeps it at the surface | falls back to walk then idle when absent (§2.4) |
| `aggro_idle / calm_idle` | DATA_ATTACKING held while engaged (cleared when the target is lost at line(s) [145, 176]; other clears at line(s) [153] (guards: if (this.params.forgetTargetRoll() > 0 && this.mob.getRandom().nextInt(this.params.forgetTargetRoll()) == 0)) — the owner reads them) | STATE flag drives w_aggro (§4.3) |
| `attack` | melee: LivingDamageEvent.Post -> triggerAnim packet (transport 1); ranged: named launch sites (transport 3) | the transport per species by the trigger inventory (ruled 2026-09-06, Q11 (a)): this flag is held, not pulsed at the strike, so the signal column's transport applies — the server LivingDamageEvent.Post -> triggerAnim packet for melee, a named launch site for ranged |
| `hurt` | rising edge of LivingEntity.hurtTime (0 -> 10), client-observed | always available; the red overlay stays (ruled 2026-09-06, Q4 (a)) |
| `death` | LivingEntity.deathTime > 0, client-observed | hold_on_last_frame; the vanilla death flip stays and the pilot ships no death clip (ruled 2026-09-06, Q3 (a)); a creature whose JSON ships a `death` clip gets the clip-replaces-flip mode, designed when the first such clip arrives (the Queen's own death clip the precedent) — until then a delivered `death` plays under the flip |
| `idle_alt_N` | a roll at each idle loop boundary (p = 0.15) while w_idle > 0.9 | optional; one roll per idle loop boundary, p = 0.15, a uniform choice, only while the idle weight is above 0.9 and no triggered clip plays, keyed on the clip-clock cycle index (ruled 2026-09-06, Q5 (a)) |

## 7. Hitbox bones that must keep their names

No MultiHitboxLib profile: no locked bones. Every bone name is still immutable (the code poses bones by name).

## 8. Textures

| canonical file (edit this one) | canvas | also shipped as (fan-out on import) | referenced by the code as | note |
|---|---|---|---|---|
| `pointysaurus.png` | 128x128 | pointysaurustexture.png | pointysaurus.png |  |

## 9. Reference screenshots

`reference/` holds the slots listed in `reference/SLOTS.md`; the owner fills them from the dev client (fixed camera, light and state per slot). Empty until then.

## 10. Effort and priority

- Artist scope: full contract (Tier 2, the gait-scaled (with an attacking branch) class: geckolib_migration_design.md section 5); the rig is not yet in-game (it lands through the seam in a later slice).
- Estimated effort: 23 h (generator: 4 h + 0.2 h/bone x 30 + 1 h/clip x 13 to author or improve (owner adjusts)).
- Tier 2 (gait-scaled — trigonometric pose depends on limbSwingAmount).
- Density statement (the harness's, not the artist's; ruled 2026-09-06, Q9 (a) and Q10): the classic transcription is verified at 2.5e-3 rad — Beaver reference leg 15 / 13 / 8 catmullrom keys per bone with spline arguments repaired at load; wrap sample T−ε vs 0+ε included; the key counts per bone are an output of the harness, re-derived on every re-transcription.

## 11. What 'done' looks like for this entity

_This rig is NOT yet in-game: the geometry is the converter's output over the port's compiled model, proven part for part against the 1.7.10 source by the reference-geometry leg; it lands through the seam in a later slice. Bone names are FINAL — the seam, the hitbox profiles and the transcriptions find bones by name._

1. `reference_pointysaurus.geo.json` returned UNCHANGED (or not at all): every bone name, parent, pivot, rotation and cube as listed in §3 and the packaged file (the reference leg's geo; the rule is the shipped rig's) (a `locked` bone renamed, re-parented or deleted is refused by name).
2. `pointysaurus.animation.json` — that exact file name, one file — (format 1.8.0) holding the clips of §5 with the loop values shown; keys on `locked` bones: Keying a locked bone is allowed; the checker warns, and the hitbox part follows the bone in-game (the consequence, so keep such keys deliberate). Renaming, re-parenting or deleting a locked bone is refused.
3. Textures at their canvas sizes under the canonical names of §8 (aliases are fanned out on import).
4. Optionally the `.bbmodel` working file. `tools/artist_package.py check <your folder>` must pass before hand-in. It REJECTS: a missing or empty folder, or a missing manifest; no `.animation.json`, one under the wrong name (the sheet names the file), or a second one; a clip not in the sheet (renamed or added; `idle_alt_N` only where the sheet allows it), or a clip delivered for a Tier-3 creature; a wrong `loop` value; a missing `idle` or `walk` (they open the game's switch only TOGETHER — one without the other leaves the creature on its code-driven motion); a missing clip the game already triggers by name; a key on a bone the rig does not have (renamed), or a channel other than rotation / position / scale; a Molang string, a non-numeric or non-finite value, a custom-instruction (`timeline`) key; an event keyframe (`sound_effects` / `particle_effects`) on a LOOPING clip of a phase-locked species — no event keyframes on loops; code-fired events come from the trigger inventory (one-shot clips may carry them; a creature whose controllers are its own GeckoLib controllers — the SPEC's controller kind `native`, the Queen — is exempt: native controllers are not phase-locked, event keys fire per loop); |rotation| > 3600 degrees or |position| > 1024 units; a key later than the clip's `animation_length` (a declared length shorter than the last key); a returned `.geo.json` whose bones, parents, pivots, rotations, cubes, UVs or canvas differ from the shipped rig; a `locked` bone renamed, re-parented or deleted in a returned `.geo.json` (the finding names the bone); a texture that is not this creature's, or whose canvas size changed; a `*_reference.animation.json` returned that is not the package's own untouched copy (the reference-only clip the package carries beside the sheet: the classic code sampled at fixed inputs, never edited, never delivered, never shipped — the finding names the file; the untouched copy coming back is warned, not a delivery). It WARNS on: a key on a `locked` bone (the clip and the bones are named) — Keying a locked bone is allowed; the checker warns, and the hitbox part follows the bone in-game (the consequence, so keep such keys deliberate). Renaming, re-parenting or deleting a locked bone is refused; a `_preview` file delivered (a Blockbench-only aid, never in the jar — ruled 2026-09-06, Q15 (a); leave it out of the delivery); a returned `.geo.json` at all (the shipped rig is used regardless — do not re-export it); a clip length far from the one the sheet states (rule 5: a phase-locked creature's loops at 1.0 s — ruled 2026-09-06, Q14 (a); a native creature's clips at their shipped length; a one-shot at the length its row states); an optional clip not delivered; an alias-named texture (the canonical name is preferred); a `format_version` other than 1.8.0; a number written as a string; an unknown keyframe key; a clip without `animation_length`; an unreadable `.bbmodel`.

