# Leaf Monster — `leaf_monster` — artist SPEC

Generated by `tools/artist_package.py` 0.4.0 (the reference clips revised, 2026-09-14, second set revised, items 2 to 6: every state a hook reads sampled and named by the seed - reference_walk, reference_idle, reference_attack, reference_fly, reference_swim, the species' own states, an unnamed one flagged - keyed by the exact transcriptions' density search (catmullrom, 1 degree, 1/32 block), embedded in every .bbmodel within the round-trip tolerance with the files beside the sheet the pinned source; a reference clip's keys as the starting point of idle / walk / aggro_idle / fly / swim, a species state offered as a SPEC extra; only the reference file itself under its own name refused; one tracked copy, the folder's copies ignored by git) from the repository's own sources; the paragraphs marked AUTHORED come from `tools/artist_specs/leaf_monster.json` (DRAFT — authored by a seed lane from the entity code, 2026-09-13; the owner's edit pending, landed on its hook by the fourth Tier-2 slice (T2d, 2026-09-14)). Every contract decision this sheet rests on was ruled by the mod's owner on 2026-09-06 (`phase_g_reports/animation_contract/open_questions.md` records each ruling); a line that cites one ("ruled 2026-09-06, Q14 (a)") states that ruling — nothing here invents an answer. The one open item is the lock reject mode (§7, README rule 6).

## 1. What this mob is (AUTHORED — draft for the owner's edit)
> **DRAFT** — DRAFT — authored by a seed lane from the entity code, 2026-09-13; the owner's edit pending, landed on its hook by the fourth Tier-2 slice (T2d, 2026-09-14). Nothing in this section is ruled: it was drafted from the entity code and the model, every claim citing its line, for the owner's edit.


A leaf-block mimic built of five one-block leaf cubes: a body cube, an arm cube at each side and a leg cube under each arm (LeafMonsterModel.java:37-41, a 128x128 sheet). Calm, it is a pile: the body cube drops to the ground between the two leg cubes with the arm cubes sitting on top of them, and the entity snaps itself to the block grid and to a 90-degree yaw every tick (EntityLeafMonster.java:89-104), so it passes for leaves. When prey - an ant, a butterfly, a luna moth or a non-creative player - comes within 4 blocks and in sight (:143-170) it raises the synched attacking flag (:130), rises a full block so the body hangs between the arms above the legs, flails both arms, walks at the prey and bites within 2.2 blocks (:126-139, doHurtTarget :134) for 2 damage; 6 HP, 1 armour (MobStats.LEAF_MONSTER, :57-64). It floats (:52), panics when hurt (:53) and spawns in darkness at night above y 50 (below y 20 in the Islands) with at most four others nearby (:194-206). Read it as an ambush: dead-still leaves, then a two-block figure of flailing blocks.

## 2. Size and scale in-game

- Hitbox (EntityType dims, `ModEntities.java`): 1 x 2.5 blocks (wide x tall).
- Renderer scale: x1; shadow radius 0.65 (reference pin status: pin, `tools/reference_renderer_pins.json`).
- Rig units: 16 units = 1 block before the renderer scale, so a 2.5-block-tall hitbox is 40 rig units tall at x1.
- Texture canvas: 128 x 128 (keep it; the canvas size is checked on return).
- Notes (AUTHORED): Drawn at x1.0 of the rig (LeafMonsterRenderer.java:24; no scale override) on a 128x128 sheet (LeafMonsterModel.java:42); the hitbox is 1.0 x 2.5 (ModEntities.java:257-259); shadow 0.65 (LeafMonsterRenderer.java:26). The bind pose is the RISEN figure (body at 0, arms at -8): the calm pile is a position offset the code applies.

## 3. Bone glossary (locked legacy names — readable labels beside them)

Never rename, delete or re-parent a bone: code and hitboxes reference them by name. Left/right in the labels follow the model author's own naming (the side the legacy name calls left); Blockbench mirrors X for display, so the author's left appears on your right when the mob faces you. `gait bone` = its motion is scaled by walking speed in-game (P3). `locked` = it carries or parents a hitbox part (contract §8.1). Keying a locked bone is allowed; the checker warns, and the hitbox part follows the bone in-game (the consequence, so keep such keys deliberate). Renaming, re-parenting or deleting a locked bone is refused.

| bone | readable label | classic part | parent | pivot (x, y, z) | group | gait bone | locked | cubes |
|---|---|---|---|---|---|---|---|---|
| `body` | body cube (drops one block to the ground when calm) | (native rig: no classic part) | - | (0, 24, 0) | - | no | no | 1 |
| `larm` | left arm cube (drops one block when calm; flails when attacking) | (native rig: no classic part) | - | (-8, 32, 0) | arms | no | no | 1 |
| `lleg` | left leg cube (swings with the stride when attacking) | (native rig: no classic part) | - | (-8, 16, 0) | gait | yes | no | 1 |
| `rarm` | right arm cube (drops one block when calm; flails when attacking) | (native rig: no classic part) | - | (8, 32, 0) | arms | no | no | 1 |
| `rleg` | right leg cube (swings with the stride when attacking) | (native rig: no classic part) | - | (8, 16, 0) | gait | yes | no | 1 |

## 4. Current animation behaviour — plain English and the source formulas

- Two branches on the synched attacking flag (LeafMonsterModel.java:47-70). Calm (attacking == 0, :48-57): the body cube is dropped 16 units to the ground and both arm cubes 16 units onto the leg cubes, every rotation is 0 - a still 3-wide pile of leaf blocks; the entity meanwhile snaps its position to the block centre and its yaw to a multiple of 90 degrees every tick (EntityLeafMonster.java:89-104), so it cannot walk or turn while calm.
- Attacking (:58-69): the body and arms return to the bind pose (the figure rises one block: legs at the ground, body above them, arms at the top); the legs swing with the stride - the cosine of the age x 0.95, x pi x 0.25, x the walking speed above the 0.1 threshold, left and right opposed (45 degrees at full stride every 6.6 ticks); the arms flail regardless of speed - the absolute value of the cosine of the age x 0.7 x pi x 0.55 (99 degrees) yaws both arms outward and pitches both forward, a rectified pump every 4.5 ticks.
- The switch between the branches is instant: the code snaps the body and arms between the two heights on the tick the flag changes (no transition), which is why the `rise` and `sink` extras exist.
- Nothing reads the head look (netHeadYaw unused; no look goal registered, EntityLeafMonster.java:50-55) and nothing scales with health.
- The attacking flag is held: set 1 every fourth think tick while prey is found (EntityLeafMonster.java:126-131) and 0 when none is (:137) - a state, so the strike (doHurtTarget :134, within 2.2 blocks on a roll) is transport 1.

### 4.1 Tempo table (contract §7.1)

| group | bones | axis | omega (rad/tick) | natural period | amplitude | speed-scaled | Blockbench preview rate | source |
|---|---|---|---|---|---|---|---|---|
| gait | lleg, rleg | x | 0.95 | 6.614 ticks (0.331 s) | pi x 0.25 = 0.785 rad (45 deg) x the walking speed above the 0.1 threshold - in the attacking branch only (calm: held at 0, and the entity cannot move while calm) | yes | 3.02 clip-ticks per game tick at a 1.0 s authoring length | LeafMonsterModel.java:56-57, 62-64 |
| arms | larm, rarm | y and x | 0.7 | 8.976 ticks (0.449 s); VISIBLE: 4.488 ticks (0.224 s) — a rectified shape (\|cos\|) repeats every half period | pi x 0.55 = 1.728 rad (99 deg), rectified (\|cos\|: the arms swing out and snap back twice per natural period) - in the attacking branch only, at any walking speed; calm: held at 0 | no | 2.23 clip-ticks per game tick at a 1.0 s authoring length (author one visible pump: 4.46 clip-ticks per game tick) | LeafMonsterModel.java:52-55, 65-69 |

The bare `walk` clip is the gait group's (`gait`, scaled by walking speed in-game); every other group keys `walk_<group>` / `idle_<group>` (contract §2.1).

A rectified group (`|cos|`, `|sin|`) has TWO periods: the natural 2π/ω of the underlying wave, and the visible one — half of it — at which the pump you see actually repeats; author the loop to the visible period.

Author every loop at 1.0 s (README rule 5, the phase-locked kind; ruled 2026-09-06, Q14 (a)). In-game each loop plays once per natural period whatever its length; your timeline's length is the SHAPE's timeline and does not change that (P6). Blockbench previews at the declared length, so a 1.0 s preview of a fast group shows the shape slowed down (contract §9); a `_preview` copy stretched to the natural periods is a Blockbench-only aid, never in the jar and never in a delivery (ruled 2026-09-06, Q15 (a)).

### 4.2 The math

- **gait**: the two leg cubes swing fore and aft in opposition with the stride, only while it is attacking — `attacking: a = limbSwingAmount > 0.1 ? cos(t * 0.95) * pi * 0.25 * limbSwingAmount : 0; lleg.xRot = a; rleg.xRot = -a; calm: lleg.xRot = rleg.xRot = 0` (LeafMonsterModel.java:56-57, 62-64)
- **arms**: both arm cubes flail: they yaw outward together and pitch forward together, dropping back through the rest each half period — `attacking: m = abs(cos(t * 0.7) * pi * 0.55); rarm.yRot = -m; larm.yRot = m; rarm.xRot = larm.xRot = -m; calm: all 0` (LeafMonsterModel.java:52-55, 65-69)
- `LeafMonsterModel.poseFrom` (LeafMonsterModel.java:54-79; orig ModelLeafMonster.java:59-81 in `render` (func_78088_a, :55-87; func_78087_a at :95-97 is a bare super call); no wingspeed): while the attacking flag is 0 the body sits at y 16 and both arms at y 8 (their x and z the bind) with every rotation of the arms and legs 0 - the bush; while attacking the body sits at y 0 and the arms at y -8, the legs pitch by the cosine of the age in ticks times 0.95, times pi, times 0.25, times the walking speed while the walking speed is above a tenth (else 0), the left leg positive and the right its negative, and the arms take the absolute value of the cosine of the age times 0.7, times pi, times 0.55: the right arm yaws by its negative and the left by it, and both arms pitch by its negative. The walk position and the head angles play no part. The GeckoLib hook is the same (LeafMonsterGeoReplacement.applyCustomAnimations).

### 4.3 Reference clips (reference-only)

`leaf_monster_reference_walk.animation.json`, `leaf_monster_reference_idle.animation.json`, `leaf_monster_reference_attack.animation.json` (beside this sheet, and embedded in `leaf_monster.bbmodel`'s Animation tab) are NOT clips to return or ship under their own names. Each is the creature's classic code — the motion the game draws today — SAMPLED by the harness at fixed inputs so you can see that motion beside the rig in Blockbench. The `.bbmodel` opens with every clip listed (the shipped clips and these); the files beside the sheet are the PINNED SOURCE (their bytes are what the checker knows; the generator's round trip proves the embedded copies within 5e-5 s on key times and 1e-6 on values). If you rebuild the `.bbmodel` and they are gone, Animation → Import Animations... on the files brings them back. One clip per state the code reads, each value alone: `_reference_walk` — full walking speed (limbSwingAmount 1, the walk position and the age advancing one tick per key), not attacking, looking straight ahead, every state flag at rest, full health; `_reference_idle` — standing still (limbSwingAmount 0, the walk position 0; the age advancing one tick per key), not attacking, looking straight ahead, every state flag at rest, full health; `_reference_attack` — attacking (the code's attacking flag raised) while standing still (limbSwingAmount 0, the walk position 0; the age advancing one tick per key), looking straight ahead, every other state flag at rest, full health. Keys: the classic code is sampled once per tick, and the samples are reduced by the density search the exact transcriptions use — the fewest Catmull-Rom keys per bone and channel whose curve stays within 1 degree of rotation and 1/32 block of position of every sample, the closing key always kept — so each clip carries as few keys as that tolerance allows (each entry below states its key counts and the measured maximum error). Rotations are deltas from the bind pose under the same sign rule as the shipped clips (X as the classic degrees, Y and Z negated), positions only where the code moves a bone.

**Starting point (owner 2026-09-14, addendum item 13; extended by the second set revised, item 4):** a reference clip's KEYS may be the starting point of the delivered clip its state corresponds to — copy them into `walk` (from `_reference_walk`), `idle` (from `_reference_idle`), `aggro_idle` (from `_reference_attack`), `fly` (from `_reference_fly`) or `swim` (from `_reference_swim`) and improve from there; a delivered clip carrying those keys is a valid delivery (no rule compares them). A state with no contract clip (sit, sleep, display, ...) is OFFERED as an extra of this creature's own under §2.3 — §5 lists it as "offered from reference_<state>" beside the seed's extras, and a delivered clip under that name carrying the reference keys is a valid delivery. Only the reference file itself coming back under its own name is refused.

- `leaf_monster_reference_walk.animation.json` (sha256 `73338027769cac57745784170e7d739cfcf95f4d73a8bd4bec78224ce90959b7`; clip `reference_walk`; a starting point for `walk`): one key (nothing in this rig's code moves a bone at these inputs: the pose it holds); one key per bone (nothing to reduce).
  - Sampled from: `danger.orespawn.entity.client.LeafMonsterGeoReplacement.applyCustomAnimations(AnimationProcessor, PoseInputs) through OreSpawnGeoReplacement.pose (the S4 doctrine: the shipped replacement, registry-free, on explicit PoseInputs)`.
  - Rule applied: nothing moves at the walk inputs: every bone's rotation and position is the same at every sample of the span the closure test settled (period_multiple; the threshold gait cos(ageInTicks * 0.95) * PI * 0.25 * limbSwingAmount (LeafMonsterGeoReplacement.applyCustomAnimations:64, walk in the attacking branch) at 0.95 rad/tick, 6.61 ticks - the slowest by period; the |cos| arms cos(ageInTicks * 0.7) * PI * 0.55 through Math.abs (:67-71, attacking) fold to a 4.49-tick half period; at rest the bush (attacking 0) holds still; closes at k = 1 (6.613879 ticks): every bone returns within 0 degrees of its start at k x T (the 5-degree test under the 6 s cap; k = 1: 0 deg)), so this state is one key.
  - Bones that move in it: none; bones the code also MOVES (position keys): `body`, `larm`, `rarm`.
  - Loop seam: the closing key differs from the first by at most 0 degrees (mod 360).
- `leaf_monster_reference_idle.animation.json` (sha256 `b28ca4165d249898b7c1f2cb7bdac94552e13f89feee6d14b9c35a4dc9a8621b`; clip `reference_idle`; a starting point for `idle`): one key (nothing in this rig's code moves a bone at these inputs: the pose it holds); one key per bone (nothing to reduce).
  - Sampled from: `danger.orespawn.entity.client.LeafMonsterGeoReplacement.applyCustomAnimations(AnimationProcessor, PoseInputs) through OreSpawnGeoReplacement.pose (the S4 doctrine: the shipped replacement, registry-free, on explicit PoseInputs)`.
  - Rule applied: nothing moves at the idle inputs: every bone's rotation and position is the same at every sample of the span the closure test settled (period_multiple; the threshold gait cos(ageInTicks * 0.95) * PI * 0.25 * limbSwingAmount (LeafMonsterGeoReplacement.applyCustomAnimations:64, walk in the attacking branch) at 0.95 rad/tick, 6.61 ticks - the slowest by period; the |cos| arms cos(ageInTicks * 0.7) * PI * 0.55 through Math.abs (:67-71, attacking) fold to a 4.49-tick half period; at rest the bush (attacking 0) holds still; closes at k = 1 (6.613879 ticks): every bone returns within 0 degrees of its start at k x T (the 5-degree test under the 6 s cap; k = 1: 0 deg)), so this state is one key.
  - Bones that move in it: none; bones the code also MOVES (position keys): `body`, `larm`, `rarm`.
  - Loop seam: the closing key differs from the first by at most 0 degrees (mod 360).
- `leaf_monster_reference_attack.animation.json` (sha256 `2cd3399bb75923479fafa7e171ab0a96c3b6a2427ac9b3b99d4c3cc0c081818c`; clip `reference_attack`; a starting point for `aggro_idle`): 2 periods of its slowest rhythm (6.614 ticks each) — 13.228 ticks (0.661 s): the smallest multiple at which EVERY rhythm returns within 5 degrees of its start (the rule caps this search at 6 s), so the last key closes the loop; 2 to 15 keys per bone chosen by the density search from 15 per-tick samples, Catmull-Rom; max error 0 degrees against the samples (the tolerance 1 degree / 1/32 block).
  - State: `getAttacking` answers 1, every other getter at rest (named by the contract (getAttacking 1)).
  - Sampled from: `danger.orespawn.entity.client.LeafMonsterGeoReplacement.applyCustomAnimations(AnimationProcessor, PoseInputs) through OreSpawnGeoReplacement.pose (the S4 doctrine: the shipped replacement, registry-free, on explicit PoseInputs)`.
  - Rule applied: the threshold gait cos(ageInTicks * 0.95) * PI * 0.25 * limbSwingAmount (LeafMonsterGeoReplacement.applyCustomAnimations:64, walk in the attacking branch) at 0.95 rad/tick, 6.61 ticks - the slowest by period; the |cos| arms cos(ageInTicks * 0.7) * PI * 0.55 through Math.abs (:67-71, attacking) fold to a 4.49-tick half period; at rest the bush (attacking 0) holds still; closes at k = 2 (13.227759 ticks): every bone returns within 1.350807 degrees of its start at k x T (the 5-degree test under the 6 s cap; k = 1: 90.818179 deg, k = 2: 1.350807 deg).
  - Bones that move in it: `larm`, `rarm`.
  - Loop seam: the closing key differs from the first by at most 1.3508 degrees (mod 360).
  - Attacking read through `LeafMonsterPose.getAttacking()`.

`check` REJECTS a `*_reference_<state>.animation.json` returned under its own name (the package's own untouched copy is warned, not a delivery), and the game's jar never carries one (the asset audit refuses it).

This creature has NO exact keyframe transcription (owner 2026-09-13, Amendment 2: its motion does not fit the current transcription form, and the game runs its classic code until you deliver `idle` and `walk`). Its source formulas in plain language (migration design section 5 — the source method and line quoted, each formula and constant in words):

- `LeafMonsterModel.poseFrom` (LeafMonsterModel.java:54-79; orig ModelLeafMonster.java:59-81 in `render` (func_78088_a, :55-87; func_78087_a at :95-97 is a bare super call); no wingspeed): while the attacking flag is 0 the body sits at y 16 and both arms at y 8 (their x and z the bind) with every rotation of the arms and legs 0 - the bush; while attacking the body sits at y 0 and the arms at y -8, the legs pitch by the cosine of the age in ticks times 0.95, times pi, times 0.25, times the walking speed while the walking speed is above a tenth (else 0), the left leg positive and the right its negative, and the arms take the absolute value of the cosine of the age times 0.7, times pi, times 0.55: the right arm yaws by its negative and the left by it, and both arms pitch by its negative. The walk position and the head angles play no part. The GeckoLib hook is the same (LeafMonsterGeoReplacement.applyCustomAnimations).

## 5. Clips: what to improve, what to leave (AUTHORED verdicts on generated rows)
> **DRAFT** — DRAFT — authored by a seed lane from the entity code, 2026-09-13; the owner's edit pending, landed on its hook by the fourth Tier-2 slice (T2d, 2026-09-14). Nothing in this section is ruled: it was drafted from the entity code and the model, every claim citing its line, for the owner's edit.


| clip | loop | layer | weight / trigger | bones it may animate | length | code-triggered | verdict | notes |
|---|---|---|---|---|---|---|---|---|
| `idle` | true | base | w_idle = (1 - w_move)(1 - w_swim)(1 - w_fly) | any of the 5 bones — gait group (speed-scaled): lleg, rleg; free: body; other groups' bones: larm (better left to `idle_arms`), rarm (better left to `idle_arms`) | 1 s | no | author | REQUIRED with walk: the gate opens on the pair (today nothing ships; the classic code plays) - the disguise: the body and both arms dropped one block (16 units) onto the ground and the legs, nothing moving (LeafMonsterModel.java:48-57); position keys, no rotation |
| `idle_arms` | true | parallel layer | w_idle = (1 - w_move)(1 - w_swim)(1 - w_fly) (group arms) | larm, rarm | 1 s | no | author |  |
| `walk` | true | base | w_walk = w_move (1 - w_swim)(1 - w_fly); the gait group additionally x limbSwingAmount (P3) | any of the 5 bones — gait group (speed-scaled): lleg, rleg; free: body; other groups' bones: larm (better left to `walk_arms`), rarm (better left to `walk_arms`) | 1 s | no | author | REQUIRED with idle: the locomotion loop - the risen figure's stride: legs 45 deg in opposition (LeafMonsterModel.java:62-64), the arms flailing (:65-69); it only ever walks while attacking (calm, the entity is pinned to the grid, EntityLeafMonster.java:89-104) |
| `walk_arms` | true | parallel layer | w_walk = w_move (1 - w_swim)(1 - w_fly); the gait group additionally x limbSwingAmount (P3) (group arms) | larm, rarm | 1 s | no | author |  |
| `swim` | true | base | w_swim from isInWater() | any of the 5 bones — gait group (speed-scaled): lleg, rleg; free: body; other groups' bones: larm (better left to `swim_arms`), rarm (better left to `swim_arms`) | 1 s | no | author | it has the state - a FloatGoal keeps it at the surface (EntityLeafMonster.java:52) - though only the risen figure can reach water (calm it is pinned to the grid); a bobbing pile; optional: falls back to walk then idle when absent (section 2.4) |
| `aggro_idle` | true | base | w_idle x w_aggro (DATA_ATTACKING held) | any of the 5 bones — gait group (speed-scaled): lleg, rleg; free: body; other groups' bones: larm (better left to `idle_arms`), rarm (better left to `idle_arms`) | 1 s | no | author | the attacking branch standing still: the figure risen (body and arms at the bind height), legs at 0, both arms flailing 99 deg at the rectified 4.5-tick pump (LeafMonsterModel.java:58-61, 65-69); the synched attacking flag is held while prey is in range (EntityLeafMonster.java:130, cleared :137) |
| `calm_idle` | true | base | w_idle x (1 - w_aggro) | any of the 5 bones — gait group (speed-scaled): lleg, rleg; free: body; other groups' bones: larm (better left to `idle_arms`), rarm (better left to `idle_arms`) | 1 s | no | author | the calm branch: identical to idle (the dropped, motionless pile, LeafMonsterModel.java:48-57) - may be delivered as a copy of idle |
| `attack` | false | triggered controller | event: a strike (the transport per species by the trigger inventory — ruled 2026-09-06, Q11 (a); §6 says which applies here) | (any unlocked) | 0.5 s | yes | author | a bite: doHurtTarget within 2.2 blocks on a 1-in-8-or-1-in-10 roll each fourth think tick (EntityLeafMonster.java:132-135); the flag is held, so the strike is transport 1 (the server LivingDamageEvent.Post) - a lunge of the body cube and both arms |
| `hurt` | false | triggered controller | event: hurtTime rising edge (client-observed); the red overlay stays (ruled 2026-09-06, Q4 (a)) | (any unlocked) | 0.3 s | yes | author | a flinch: no hurt() override (vanilla LivingEntity.hurt; leaves_hit sound at EntityLeafMonster.java:178-181); the hurtTime edge is client-observed |
| `death` | hold_on_last_frame | triggered controller | event: deathTime > 0; the vanilla death flip stays — the pilot ships no death clip (ruled 2026-09-06, Q3 (a)); a creature whose JSON ships one gets the clip-replaces-flip mode when the first such clip arrives, so a `death` delivered here plays under the flip until then | (any unlocked) | 1 s | yes | author | ruled 2026-09-06, Q3 (a): the vanilla death flip stays; a creature whose JSON ships a `death` clip gets the clip-replaces-flip mode, designed when the first such clip arrives (the Queen's own death clip the precedent) - a `death` delivered here plays under the flip until then |
| `rise` | false | overlay | the rising edge of DATA_ATTACKING (EntityLeafMonster.java:130, set every fourth think tick while prey is in range); today the pose snaps between the branches on that tick (LeafMonsterModel.java:48-61) - the seam needs an edge trigger to play it | body, larm, rarm | 0.5 s | no | author | the ambush reveal: the body and both arms rise one block (16 units) from the pile to the bind height - position keys, ending where aggro_idle begins |
| `sink` | false | overlay | the falling edge of DATA_ATTACKING (EntityLeafMonster.java:137, when no prey is found); today the pose snaps (LeafMonsterModel.java:48-57) - the seam needs an edge trigger to play it | body, larm, rarm | 0.5 s | no | author | the settle back into the pile: the body and both arms drop one block, ending where idle / calm_idle begin |

Loop values are the JSON `loop` field exactly: `true` for cycles, `false` for one-shots, `hold_on_last_frame` only where written. Every name in this table is fixed (contract §2.4); `idle_alt_1`, `idle_alt_2`, ... may be added freely. A sheet may carry up to four extra clips of this creature's own beyond the contract's names (§2.3; ruled 2026-09-06, Q6 (a)) — more needs the owner's ruling; the extras rows above, if any, are those.

### 5.1 Wishlist (AUTHORED — only what `check` accepts today; anything else is marked)
> **DRAFT** — DRAFT — authored by a seed lane from the entity code, 2026-09-13; the owner's edit pending, landed on its hook by the fourth Tier-2 slice (T2d, 2026-09-14). Nothing in this section is ruled: it was drafted from the entity code and the model, every claim citing its line, for the owner's edit.


- the reveal and the settle (`rise` / `sink`): the code snaps between the pile and the figure on one tick (LeafMonsterModel.java:48-61) - a half-second rise with the arms coming up last sells the ambush
- in `walk`: a lurching stride - five rigid cubes with 45-degree leg swings (LeafMonsterModel.java:62-64) read as a golem; let the body cube bob with the step
- in `aggro_idle`: keep the 99-degree arm flail (:65-69) but give the body a menace lean toward the prey; the code moves only the arms
- in `attack`: a lunge - the bite (EntityLeafMonster.java:134) has no pose of its own today
- in `idle`: nothing - the disguise must be dead still (the entity is pinned to the block grid, EntityLeafMonster.java:89-104); resist adding a breath

## 6. Trigger inventory (generated from the entity's AI goals and state flags)

Source: `src/main/java/danger/orespawn/entity/EntityLeafMonster.java` (Monster, LeafMonsterPose)

| selector | prio | goal | guard | category | what it does | bears on |
|---|---|---|---|---|---|---|
| goalSelector | 0 | `FloatGoal` | - | locomotion | bobs up to the surface in water (vanilla) | swim state (client reads isInWater) |
| goalSelector | 1 | `PanicGoal` | - | flee | runs in a panic after taking damage (vanilla) | walk (fast) |
| targetSelector | 1 | `HurtByTargetGoal` | - | targeting | retaliates against whoever hurt it (vanilla target selector) | aggro state (through the attacking flag where one exists) |

A `[modern: key]` guard means the goal is registered only under the modern config key named (read once, at construction); `UNPARSED` means the registration's shape is one the generator does not read (a local variable or a computed priority) — the owner reads that line.

**Synched state flags** (what the client can see): `DATA_ATTACKING` (Integer, line 38)

**Attacking flag `DATA_ATTACKING`** — classified **STATE** (cleared when the target is lost at line(s) [138]) — a mechanical reading; the owner confirms it against the sites:

| line | method | sets | guard (the enclosing block) | within | comment |
|---|---|---|---|---|---|
| 131 | customServerAiStep | 1 | if (target != null) | if (this.random.nextInt(4) == 1) |  |
| 138 | customServerAiStep | 0 | NOT(if (target != null)) | if (this.random.nextInt(4) == 1) |  |

The guard is the header of the block that actually encloses the write (found by brace depth, comments and strings blanked); an `else` branch is written as the negated `if` chain it closes; a header the parser cannot read is `(unparsed: ...)`, never dropped.

**Locomotion facts:** a walker. Overrides: tick, customServerAiStep, removeWhenFarAway.

**Strike and launch sites:**

- melee `doHurtTarget(target)` — EntityLeafMonster.java:135 in `customServerAiStep`, guard: if (this.distanceToSqr(target) < 5.0 && (this.random.nextInt(8) == 0 || this.random.nextInt(10) == 1)) within if (target != null) within if (this.random.nextInt(4) == 1)

**What fires each contract clip:**

| clip | signal | verdict |
|---|---|---|
| `walk` | limbSwingAmount from AnimationState (the seam's input; 0 at rest, 1 at full stride) | the gait group's weight and speed scale (P3) |
| `idle` | w_idle = (1 - w_move)(1 - w_swim)(1 - w_fly) | standing still |
| `swim` | Entity.isInWater() (client-evaluated); FloatGoal keeps it at the surface | falls back to walk then idle when absent (§2.4) |
| `aggro_idle / calm_idle` | DATA_ATTACKING held while engaged (cleared when the target is lost at line(s) [138]) | STATE flag drives w_aggro (§4.3) |
| `attack` | melee: LivingDamageEvent.Post -> triggerAnim packet (transport 1); ranged: named launch sites (transport 3) | the transport per species by the trigger inventory (ruled 2026-09-06, Q11 (a)): this flag is held, not pulsed at the strike, so the signal column's transport applies — the server LivingDamageEvent.Post -> triggerAnim packet for melee, a named launch site for ranged |
| `hurt` | rising edge of LivingEntity.hurtTime (0 -> 10), client-observed | always available; the red overlay stays (ruled 2026-09-06, Q4 (a)) |
| `death` | LivingEntity.deathTime > 0, client-observed | hold_on_last_frame; the vanilla death flip stays and the pilot ships no death clip (ruled 2026-09-06, Q3 (a)); a creature whose JSON ships a `death` clip gets the clip-replaces-flip mode, designed when the first such clip arrives (the Queen's own death clip the precedent) — until then a delivered `death` plays under the flip |
| `idle_alt_N` | a roll at each idle loop boundary (p = 0.15) while w_idle > 0.9 | optional; one roll per idle loop boundary, p = 0.15, a uniform choice, only while the idle weight is above 0.9 and no triggered clip plays, keyed on the clip-clock cycle index (ruled 2026-09-06, Q5 (a)) |

## 7. Hitbox bones that must keep their names

No MultiHitboxLib profile: no locked bones. Every bone name is still immutable (the code poses bones by name).

## 8. Textures

| canonical file (edit this one) | canvas | also shipped as (fan-out on import) | referenced by the code as | note |
|---|---|---|---|---|
| `leafmonster.png` | 128x128 | leafmonstertexture.png | leafmonster.png |  |

## 9. Reference screenshots

`reference/` holds the slots listed in `reference/SLOTS.md`; the owner fills them from the dev client (fixed camera, light and state per slot). Empty until then.

## 10. Effort and priority

- Artist scope: full contract (Tier 2, the gait-scaled class (with an attacking branch that raises the figure): geckolib_migration_design.md section 5); ON THE HOOK until idle and walk are delivered (Amendment 2): in-game through the seam behind the dev switch since the fourth Tier-2 slice (T2d, 2026-09-14).
- Estimated effort: 17 h (generator: 4 h + 0.2 h/bone x 5 + 1 h/clip x 12 to author or improve (owner adjusts)).
- Tier 2 (gait-scaled — trigonometric pose depends on limbSwingAmount).
- Density statement (the harness's, not the artist's; ruled 2026-09-06, Q9 (a) and Q10): the classic transcription is verified at 2.5e-3 rad — Beaver reference leg 15 / 13 / 8 catmullrom keys per bone with spline arguments repaired at load; wrap sample T−ε vs 0+ε included; the key counts per bone are an output of the harness, re-derived on every re-transcription.

## 11. What 'done' looks like for this entity

1. `leafmonster.geo.json` returned UNCHANGED (or not at all): every bone name, parent, pivot, rotation and cube as listed in §3 and the shipped file (a `locked` bone renamed, re-parented or deleted is refused by name).
2. `leafmonster.animation.json` — that exact file name, one file — (format 1.8.0) holding the clips of §5 with the loop values shown; keys on `locked` bones: Keying a locked bone is allowed; the checker warns, and the hitbox part follows the bone in-game (the consequence, so keep such keys deliberate). Renaming, re-parenting or deleting a locked bone is refused.
3. Textures at their canvas sizes under the canonical names of §8 (aliases are fanned out on import).
4. Optionally the `.bbmodel` working file. `tools/artist_package.py check <your folder>` must pass before hand-in. It REJECTS: a missing or empty folder, or a missing manifest; no `.animation.json`, one under the wrong name (the sheet names the file), or a second one; a clip not in the sheet (renamed or added; `idle_alt_N` only where the sheet allows it), or a clip delivered for a Tier-3 creature; a wrong `loop` value; a missing `idle` or `walk` (they open the game's switch only TOGETHER — one without the other leaves the creature on its code-driven motion); a missing clip the game already triggers by name; a key on a bone the rig does not have (renamed), or a channel other than rotation / position / scale; a Molang string, a non-numeric or non-finite value, a custom-instruction (`timeline`) key; an event keyframe (`sound_effects` / `particle_effects`) on a LOOPING clip of a phase-locked species — no event keyframes on loops; code-fired events come from the trigger inventory (one-shot clips may carry them; a creature whose controllers are its own GeckoLib controllers — the SPEC's controller kind `native`, the Queen — is exempt: native controllers are not phase-locked, event keys fire per loop); |rotation| > 3600 degrees or |position| > 1024 units; a key later than the clip's `animation_length` (a declared length shorter than the last key); a returned `.geo.json` whose bones, parents, pivots, rotations, cubes, UVs or canvas differ from the shipped rig; a `locked` bone renamed, re-parented or deleted in a returned `.geo.json` (the finding names the bone); a texture that is not this creature's, or whose canvas size changed; a `*_reference_<state>.animation.json` returned under its own name that is not the package's own untouched copy (the reference-only clips the package carries beside the sheet: the classic code sampled at fixed inputs, never shipped — the finding names the file; the untouched copy coming back is warned, not a delivery; copying a reference clip's KEYS into `idle` / `walk` / `aggro_idle` / `fly` / `swim` or into an offered extra is a delivery and passes); a returned `.bbmodel` that lists fewer clips than the folder's animation files hold, or whose embedded keys leave the round-trip tolerance (key times within 5e-5 s, values within 1e-6) of those files (the files beside the sheet are the pinned source; the `.bbmodel` must open with every clip listed — owner 2026-09-14). It WARNS on: a key on a `locked` bone (the clip and the bones are named) — Keying a locked bone is allowed; the checker warns, and the hitbox part follows the bone in-game (the consequence, so keep such keys deliberate). Renaming, re-parenting or deleting a locked bone is refused; a `_preview` file delivered (a Blockbench-only aid, never in the jar — ruled 2026-09-06, Q15 (a); leave it out of the delivery); a returned `.geo.json` at all (the shipped rig is used regardless — do not re-export it); a clip length far from the one the sheet states (rule 5: a phase-locked creature's loops at 1.0 s — ruled 2026-09-06, Q14 (a); a native creature's clips at their shipped length; a one-shot at the length its row states); an optional clip not delivered; an alias-named texture (the canonical name is preferred); a `format_version` other than 1.8.0; a number written as a string; an unknown keyframe key; a clip without `animation_length`; an unreadable `.bbmodel`.

