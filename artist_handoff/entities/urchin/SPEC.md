# Urchin — `urchin` — artist SPEC

**This rig is NOT yet in-game: the geometry is the converter's output over the port's compiled model, proven part for part against the 1.7.10 source by the reference-geometry leg; it lands through the seam in a later slice. Bone names are FINAL — the seam, the hitbox profiles and the transcriptions find bones by name.**

Generated by `tools/artist_package.py` 0.2.9 (the full folder, 2026-09-13, fourth set, items 5, 7 and 8: every artist-tier species packaged - a rig not yet in-game from the reference leg's converter output, bone names final; the Tier-1 bosses' intended locked bones from the design's section 6 rendered in section 7 and validated against the geo; the README's priority table the full list, bosses first, closing with the deliverable count) from the repository's own sources; the paragraphs marked AUTHORED come from `tools/artist_specs/urchin.json` (DRAFT — authored by a seed lane from the entity code, 2026-09-13; the owner's edit pending). Every contract decision this sheet rests on was ruled by the mod's owner on 2026-09-06 (`phase_g_reports/animation_contract/open_questions.md` records each ruling); a line that cites one ("ruled 2026-09-06, Q14 (a)") states that ruling — nothing here invents an answer. The one open item is the lock reject mode (§7, README rule 6).

## 1. What this mob is (AUTHORED — draft for the owner's edit)
> **DRAFT** — DRAFT — authored by a seed lane from the entity code, 2026-09-13; the owner's edit pending. Nothing in this section is ruled: it was drafted from the entity code and the model, every claim citing its line, for the owner's edit.


A hostile crystal urchin: seventeen spines on one pivot - a 30-tall central spine, four 25-long and four 20-long spines fanned upward around it, and eight 8-long spines splayed downward as feet (ModelUrchin.java:64-130) - drawn at x1.25. 25 HP, 10 damage, 4 armour, speed 0.3, fire-immune (Urchin.java:82-89; ModEntities.java:172-175). It sheds flame particles (:118-123) and burns in water, hurting itself and smoking (:124-135). On a 1-in-8 tick it picks the nearest living thing in a 16-block box - not the Crystal dimension's other creatures nor a creative player (:164-184, :209-240) - walks at 1.2 and, within 2.8 blocks, raises its attacking flag and one time in seven bites for 10 and five seconds of fire (:105-109, :171-176). It spawns in the dark in the night half of the day with six air blocks around its head (:268-284) and despawns in daylight unless a Crystal Urchin spawner made it (:138-147). Read it as a spiky crystal star: the feet scuttling, the crown of spines slowly turning and swaying, a fast spin when it is about to bite.

## 2. Size and scale in-game

- Hitbox (EntityType dims, `ModEntities.java`): 1.35 x 2.1 blocks (wide x tall).
- Renderer scale: x1.25; shadow radius 0.4375 (reference pin status: pin, `tools/reference_renderer_pins.json`).
- Rig units: 16 units = 1 block before the renderer scale, so a 2.1-block-tall hitbox is 26.9 rig units tall at x1.25.
- Texture canvas: 128 x 128 (keep it; the canvas size is checked on return).
- Notes (AUTHORED): Drawn at x1.25 of the rig (UrchinRenderer.java:25, :38; shadow 0.4375, :27) on a 128x128 sheet (ModelUrchin.java:132), urchin.png (UrchinRenderer.java:20); the hitbox is 1.35 x 2.1 (ModEntities.java:172-175). The original's ModelUrchin(1.0f) argument was the wingspeed (ModelUrchin.java:21), not a scale. Every part is a 1-wide spine on the same pivot at y 16.

## 3. Bone glossary (locked legacy names — readable labels beside them)

Never rename, delete or re-parent a bone: code and hitboxes reference them by name. Left/right in the labels follow the model author's own naming (the side the legacy name calls left); Blockbench mirrors X for display, so the author's left appears on your right when the mob faces you. `gait bone` = its motion is scaled by walking speed in-game (P3). `locked` = it carries or parents a hitbox part (contract §8.1). Keying a locked bone is allowed; the checker warns, and the hitbox part follows the bone in-game (the consequence, so keep such keys deliberate). Renaming, re-parenting or deleting a locked bone is refused.

This rig is NOT yet in-game: the geometry is the converter's output over the port's compiled model, proven part for part against the 1.7.10 source by the reference-geometry leg; it lands through the seam in a later slice. Bone names are FINAL — the seam, the hitbox profiles and the transcriptions find bones by name.

| bone | readable label | classic part | parent | pivot (x, y, z) | group | gait bone | locked | cubes |
|---|---|---|---|---|---|---|---|---|
| `center` | central spine (1x30x1 rising 30; spins about the vertical - slowly at rest, fast while attacking) | center (ModelPart of the same name) | - | (0, 8, 0) | spin | no | no | 1 |
| `if1` | inner foot 1 (1x8x1 hanging down, splayed +15 deg; paddles with the walk at 1.7 rad/tick) | if1 (ModelPart of the same name) | - | (0, 8, 0) | gait | yes | no | 1 |
| `if2` | inner foot 2 (splayed -15 deg; paddles at 1.65 rad/tick) | if2 (ModelPart of the same name) | - | (0, 8, 0) | gait | yes | no | 1 |
| `if3` | inner foot 3 (paddles at 1.75 rad/tick) | if3 (ModelPart of the same name) | - | (0, 8, 0) | gait | yes | no | 1 |
| `if4` | inner foot 4 (paddles at 1.8 rad/tick) | if4 (ModelPart of the same name) | - | (0, 8, 0) | gait | yes | no | 1 |
| `of1` | outer foot 1 (at x +2, splayed -30 deg; rolls with the walk at 0.7 rad/tick) | of1 (ModelPart of the same name) | - | (-2, 8, 0) | gait | yes | no | 1 |
| `of2` | outer foot 2 (at x -2, splayed +30 deg; rolls at 0.7 rad/tick, opposite) | of2 (ModelPart of the same name) | - | (2, 8, 0) | gait | yes | no | 1 |
| `of3` | outer foot 3 (at z -2, splayed -30 deg; pitches at 0.7 rad/tick) | of3 (ModelPart of the same name) | - | (0, 8, -2) | gait | yes | no | 1 |
| `of4` | outer foot 4 (at z +2, splayed +30 deg; pitches at 0.7 rad/tick, opposite) | of4 (ModelPart of the same name) | - | (0, 8, 2) | gait | yes | no | 1 |
| `tis1` | top inner spine 1 (1x25x1, splayed +15 deg forward; sways on two slow rhythms) | tis1 (ModelPart of the same name) | - | (0, 8, 0) | crown | no | no | 1 |
| `tis2` | top inner spine 2 (splayed -15 deg; sways) | tis2 (ModelPart of the same name) | - | (0, 8, 0) | crown | no | no | 1 |
| `tis3` | top inner spine 3 (splayed +15 deg sideways; sways) | tis3 (ModelPart of the same name) | - | (0, 8, 0) | crown | no | no | 1 |
| `tis4` | top inner spine 4 (splayed -15 deg sideways; sways) | tis4 (ModelPart of the same name) | - | (0, 8, 0) | crown | no | no | 1 |
| `tos1` | top outer spine 1 (1x20x1 at z +2, splayed -30 deg; sways) | tos1 (ModelPart of the same name) | - | (0, 8, 2) | crown | no | no | 1 |
| `tos2` | top outer spine 2 (at x -2, splayed -30 deg; sways) | tos2 (ModelPart of the same name) | - | (0, 8, 0) | crown | no | no | 1 |
| `tos3` | top outer spine 3 (at x +2, splayed +30 deg; sways) | tos3 (ModelPart of the same name) | - | (-2, 8, 0) | crown | no | no | 1 |
| `tos4` | top outer spine 4 (at z -2, splayed +30 deg; sways) | tos4 (ModelPart of the same name) | - | (0, 8, -2) | crown | no | no | 1 |

## 4. Current animation behaviour — plain English and the source formulas

- One gait rhythm above a limbSwingAmount of 0.1 (ModelUrchin.java:146-166): the eight downward spines scuttle 27 degrees x the walking speed about their splay rests, the outer four together at 0.7 rad/tick and the inner four at 1.7, 1.65, 1.75 and 1.8 rad/tick, so the feet never fall into step.
- Two branches on the synched DATA_ATTACKING int (Urchin.java:45-46), which the 1-in-8 pass sets to 1 within 2.8 blocks of a target (:171-173) and clears when it has none (:180-182): calm, the central spine creeps round once every 314 ticks and the eight upward spines shimmer 3.6 degrees on eight slow rhythms (0.035-0.08 rad/tick); attacking, the spine spins once every 31 ticks and the crown shivers 11 degrees at ten times the rate (:167-204). Note the flag stays 1 while the target is merely out of reach (:177-179 sets nothing), so the fast spin also plays during the chase.
- There is no head and no look: every part sits on one pivot at y 16. The bite (doHurtTarget, Urchin.java:105-109: 10 and five seconds of fire, one time in seven on the pass) and the water burn (:133-135) draw no pose.

### 4.1 Tempo table (contract §7.1)

| group | bones | axis | omega (rad/tick) | natural period | amplitude | speed-scaled | Blockbench preview rate | source |
|---|---|---|---|---|---|---|---|---|
| gait | if1, if2, if3, if4, of1, of2, of3, of4 | x (the inner feet and of3 / of4) and z (of1 / of2) | 0.7 | 8.976 ticks (0.449 s) | pi x 0.15 = 0.471 rad (27 deg) x the walking speed above a limbSwingAmount of 0.1 about each foot's splay rest (±0.261 inner, ±0.523 outer); the four outer feet share one rhythm at 0.7 rad/tick, the four inner feet run at 1.7, 1.65, 1.75 and 1.8 so they never line up; 0 below the threshold | yes | 2.23 clip-ticks per game tick at a 1.0 s authoring length | ModelUrchin.java:146-166; wingspeed 1.0 (:21) |
| spin | center | y | 0.02 | 314.159 ticks (15.708 s) | a continuous turn: calm at 0.02 rad/tick (one turn every 314 ticks); attacking at 0.2 rad/tick (one turn every 31 ticks) | no | 0.06 clip-ticks per game tick at a 1.0 s authoring length | ModelUrchin.java:167-168, :177-178, :188 |
| crown | tis1, tis2, tis3, tis4, tos1, tos2, tos3, tos4 | x and z | 0.07 | 89.76 ticks (4.488 s) | calm: pi x 0.02 = 0.063 rad (3.6 deg) on eight rhythms at 0.07 / 0.065 / 0.075 / 0.08 / 0.055 / 0.045 / 0.035 / 0.04 rad/tick; attacking: pi x 0.06 = 0.188 rad (11 deg) on the same eight rhythms ten times faster (0.7 / 0.65 / 0.75 / 0.8 / 0.55 / 0.45 / 0.35 / 0.4); each of the eight spines takes one rhythm on x and another on z about its splay rest | no | 0.22 clip-ticks per game tick at a 1.0 s authoring length | ModelUrchin.java:167-204 |

The bare `walk` clip is the gait group's (`gait`, scaled by walking speed in-game); every other group keys `walk_<group>` / `idle_<group>` (contract §2.1).

Author every loop at 1.0 s (README rule 5, the phase-locked kind; ruled 2026-09-06, Q14 (a)). In-game each loop plays once per natural period whatever its length; your timeline's length is the SHAPE's timeline and does not change that (P6). Blockbench previews at the declared length, so a 1.0 s preview of a fast group shows the shape slowed down (contract §9); a `_preview` copy stretched to the natural periods is a Blockbench-only aid, never in the jar and never in a delivery (ruled 2026-09-06, Q15 (a)).

### 4.2 The math

- **gait**: the eight downward spines scuttle: the outer four rock together, the inner four paddle at four slightly different rates — `n = cos(t * 0.7) * pi * 0.15 * amount; n1..n4 = cos(t * 1.7 / 1.65 / 1.75 / 1.8) * pi * 0.15 * amount (all 0 when amount <= 0.1); if1.xRot = 0.261 + n1; if2.xRot = -0.261 - n2; if3.xRot = n3; if4.xRot = -n4; of1.zRot = -0.523 + n; of2.zRot = 0.523 - n; of3.xRot = -0.523 + n; of4.xRot = 0.523 - n` (ModelUrchin.java:146-166; wingspeed 1.0 (:21))
- **spin**: the central spine turns steadily about the vertical - a slow creep at rest, a fast spin while the attacking flag is up — `center.yRot = (t * (attacking ? 0.2 : 0.02)) mod 2pi` (ModelUrchin.java:167-168, :177-178, :188)
- **crown**: the eight upward spines sway on two axes each at eight unrelated rates: a slow shimmer at rest, a fast shiver while attacking — `n1..n8 = cos(t * r_i) * pi * A with (r, A) = calm (0.07, 0.065, 0.075, 0.08, 0.055, 0.045, 0.035, 0.04; 0.02) or attacking (x10; 0.06); tis1.xRot = 0.261 + n1; tis2.xRot = -0.261 + n2; tis3.xRot = n3; tis4.xRot = n4; tis1.zRot = n5; tis2.zRot = n6; tis3.zRot = 0.261 + n7; tis4.zRot = -0.261 + n8; tos1.xRot = -0.532 + n1; tos2.xRot = n7; tos3.xRot = n3; tos4.xRot = 0.532 + n5; tos1.zRot = n4; tos2.zRot = -0.523 + n6; tos3.zRot = 0.523 + n2; tos4.zRot = n8` (ModelUrchin.java:167-204)

### 4.3 Reference clip (reference-only)

_no reference clip yet: it is sampled from the classic hook when the rig lands through the seam; until then §4.1 / §4.2 and the plain-language formulas (where the seed carries them) are the motion's description._

This creature has NO exact keyframe transcription and is not yet on its hook (the rig is not yet in-game: §3). The plain-language transcription of its source formulas (migration design section 5 — the source method and line quoted, each formula and constant in words) is authored when the rig lands on its hook; until then the seed's description, clip verdicts and wishlist (§1, §4, §5) describe the motion.

## 5. Clips: what to improve, what to leave (AUTHORED verdicts on generated rows)
> **DRAFT** — DRAFT — authored by a seed lane from the entity code, 2026-09-13; the owner's edit pending. Nothing in this section is ruled: it was drafted from the entity code and the model, every claim citing its line, for the owner's edit.


| clip | loop | layer | weight / trigger | bones it may animate | length | code-triggered | verdict | notes |
|---|---|---|---|---|---|---|---|---|
| `idle` | true | base | w_idle = (1 - w_move)(1 - w_swim)(1 - w_fly) | any of the 17 bones — gait group (speed-scaled): if1, if2, if3, if4, of1, of2, of3, of4; other groups' bones: center (better left to `idle_spin`), tis1 (better left to `idle_crown`), tis2 (better left to `idle_crown`), tis3 (better left to `idle_crown`), tis4 (better left to `idle_crown`), tos1 (better left to `idle_crown`), tos2 (better left to `idle_crown`), tos3 (better left to `idle_crown`), tos4 (better left to `idle_crown`) | 1 s | no | author | REQUIRED with walk: the gate opens on the pair (today nothing ships; the classic code plays) — the rest: the slow spin (ModelUrchin.java:178) and the 3.6-degree shimmer of the crown (:179-186), the feet still |
| `idle_spin` | true | parallel layer | w_idle = (1 - w_move)(1 - w_swim)(1 - w_fly) (group spin) | center | 1 s | no | author |  |
| `idle_crown` | true | parallel layer | w_idle = (1 - w_move)(1 - w_swim)(1 - w_fly) (group crown) | tis1, tis2, tis3, tis4, tos1, tos2, tos3, tos4 | 1 s | no | author |  |
| `walk` | true | base | w_walk = w_move (1 - w_swim)(1 - w_fly); the gait group additionally x limbSwingAmount (P3) | any of the 17 bones — gait group (speed-scaled): if1, if2, if3, if4, of1, of2, of3, of4; other groups' bones: center (better left to `walk_spin`), tis1 (better left to `walk_crown`), tis2 (better left to `walk_crown`), tis3 (better left to `walk_crown`), tis4 (better left to `walk_crown`), tos1 (better left to `walk_crown`), tos2 (better left to `walk_crown`), tos3 (better left to `walk_crown`), tos4 (better left to `walk_crown`) | 1 s | no | author | REQUIRED with idle: the locomotion loop — the eight feet scuttling at 0.7 and 1.65-1.8 rad/tick, 27 degrees x the walking speed (:146-166); the reference clip is that scuttle at full stride |
| `walk_spin` | true | parallel layer | w_walk = w_move (1 - w_swim)(1 - w_fly); the gait group additionally x limbSwingAmount (P3) (group spin) | center | 1 s | no | author |  |
| `walk_crown` | true | parallel layer | w_walk = w_move (1 - w_swim)(1 - w_fly); the gait group additionally x limbSwingAmount (P3) (group crown) | tis1, tis2, tis3, tis4, tos1, tos2, tos3, tos4 | 1 s | no | author |  |
| `swim` | true | base | w_swim from isInWater() | any of the 17 bones — gait group (speed-scaled): if1, if2, if3, if4, of1, of2, of3, of4; other groups' bones: center (better left to `swim_spin`), tis1 (better left to `swim_crown`), tis2 (better left to `swim_crown`), tis3 (better left to `swim_crown`), tis4 (better left to `swim_crown`), tos1 (better left to `swim_crown`), tos2 (better left to `swim_crown`), tos3 (better left to `swim_crown`), tos4 (better left to `swim_crown`) | 1 s | no | leave | no swim state: water burns it — in water it hurts itself one tick in five and smokes (Urchin.java:124-135); the FloatGoal (:72) only lifts it to the surface; the fallback to walk covers it (section 2.4) |
| `aggro_idle` | true | base | w_idle x w_aggro (DATA_ATTACKING held) | any of the 17 bones — gait group (speed-scaled): if1, if2, if3, if4, of1, of2, of3, of4; other groups' bones: center (better left to `idle_spin`), tis1 (better left to `idle_crown`), tis2 (better left to `idle_crown`), tis3 (better left to `idle_crown`), tis4 (better left to `idle_crown`), tos1 (better left to `idle_crown`), tos2 (better left to `idle_crown`), tos3 (better left to `idle_crown`), tos4 (better left to `idle_crown`) | 1 s | no | author | DATA_ATTACKING held at 1 by the 1-in-8 pass while a target is within 2.8 blocks (Urchin.java:171-173; it stays 1 through the chase, :177-179): the 31-tick spin (ModelUrchin.java:168) and the 11-degree shiver of the crown (:169-176) |
| `calm_idle` | true | base | w_idle x (1 - w_aggro) | any of the 17 bones — gait group (speed-scaled): if1, if2, if3, if4, of1, of2, of3, of4; other groups' bones: center (better left to `idle_spin`), tis1 (better left to `idle_crown`), tis2 (better left to `idle_crown`), tis3 (better left to `idle_crown`), tis4 (better left to `idle_crown`), tos1 (better left to `idle_crown`), tos2 (better left to `idle_crown`), tos3 (better left to `idle_crown`), tos4 (better left to `idle_crown`) | 1 s | no | author | the flag at 0 (Urchin.java:180-182): the 314-tick creep and the 3.6-degree shimmer (ModelUrchin.java:177-186) — the same rest as idle |
| `attack` | false | triggered controller | event: a strike (the transport per species by the trigger inventory — ruled 2026-09-06, Q11 (a); §6 says which applies here) | (any unlocked) | 0.4 s | yes | author | the bite: doHurtTarget (Urchin.java:105-109) ignites the target for 5 s and lands 10 (:87), one time in seven on the 1-in-8 pass within 2.8 blocks (:171-176); the flag is held while in reach, not pulsed at the strike, so the strike is transport 1 (the server LivingDamageEvent.Post) — a snap of the whole crown toward the target |
| `hurt` | false | triggered controller | event: hurtTime rising edge (client-observed); the red overlay stays (ruled 2026-09-06, Q4 (a)) | (any unlocked) | 0.3 s | yes | author | a flinch: hurt() (Urchin.java:111-115) refuses cactus; the glasshit sound at 1.1 volume (:248-251, :258-261); the hurtTime edge is client-observed |
| `death` | hold_on_last_frame | triggered controller | event: deathTime > 0; the vanilla death flip stays — the pilot ships no death clip (ruled 2026-09-06, Q3 (a)); a creature whose JSON ships one gets the clip-replaces-flip mode when the first such clip arrives, so a `death` delivered here plays under the flip until then | (any unlocked) | 1 s | yes | author | ruled 2026-09-06, Q3 (a): the vanilla death flip stays; a creature whose JSON ships a `death` clip gets the clip-replaces-flip mode, designed when the first such clip arrives (the Queen's own death clip the precedent) - a `death` delivered here plays under the flip until then |
| `water_burn` | false | overlay | aiStep (Urchin.java:133-135): in water, one server tick in five, it bites itself (doHurtTarget on itself - 10 and fire it is immune to), the client puffing smoke above it one tick in five (:124-131); the self-hurt is a hurtTime edge like any hit, so today the `hurt` flinch would play — a named site (transport 3) tells it apart when the rig lands | center, tis1, tis2, tis3, tis4, tos1, tos2, tos3, tos4 | 0.4 s | no | author | a shudder of the crown with the spines drawing in |

Loop values are the JSON `loop` field exactly: `true` for cycles, `false` for one-shots, `hold_on_last_frame` only where written. Every name in this table is fixed (contract §2.4); `idle_alt_1`, `idle_alt_2`, ... may be added freely. A sheet may carry up to four extra clips of this creature's own beyond the contract's names (§2.3; ruled 2026-09-06, Q6 (a)) — more needs the owner's ruling; the extras rows above, if any, are those.

### 5.1 Wishlist (AUTHORED — only what `check` accepts today; anything else is marked)
> **DRAFT** — DRAFT — authored by a seed lane from the entity code, 2026-09-13; the owner's edit pending. Nothing in this section is ruled: it was drafted from the entity code and the model, every claim citing its line, for the owner's edit.


- in `walk`: the code paddles the eight feet at five unrelated rates (ModelUrchin.java:147-151) — a scuttle with a hop, the crown bobbing, reads better than the shimmer; keep the feet out of step
- in `aggro_idle`: the fast spin (:168) is the tell — add a crouch of the crown before the bite so the 2.8-block reach (Urchin.java:172) reads
- in `attack`: a snap of the crown toward the target and a flare — the bite ignites (Urchin.java:107); nothing in the rig points at the victim today
- in `idle`: keep the 314-tick creep (:178) and the eight-rate shimmer (:179-186); a slow breath of the whole star sells a living crystal

## 6. Trigger inventory (generated from the entity's AI goals and state flags)

Source: `src/main/java/danger/orespawn/entity/Urchin.java` (Monster)

| selector | prio | goal | guard | category | what it does | bears on |
|---|---|---|---|---|---|---|
| goalSelector | 0 | `FloatGoal` | - | locomotion | bobs up to the surface in water (vanilla) | swim state (client reads isInWater) |
| goalSelector | 1 | `MyEntityAIWanderALot` | - | locomotion | OreSpawn's restless wander: picks a new spot often | walk |
| goalSelector | 2 | `LookAtPlayerGoal` | - | look | turns the head toward a nearby player (vanilla; head yaw/pitch only) | none: head look, not a clip |
| goalSelector | 3 | `RandomLookAroundGoal` | - | look | looks around idly (vanilla; head yaw/pitch only) | none: head look, not a clip |

A `[modern: key]` guard means the goal is registered only under the modern config key named (read once, at construction); `UNPARSED` means the registration's shape is one the generator does not read (a local variable or a computed priority) — the owner reads that line.

**Synched state flags** (what the client can see): `DATA_ATTACKING` (Integer, line 45)

**Attacking flag `DATA_ATTACKING`** — classified **STATE** (cleared when the target is lost at line(s) [181]) — a mechanical reading; the owner confirms it against the sites:

| line | method | sets | guard (the enclosing block) | within | comment |
|---|---|---|---|---|---|
| 173 | customServerAiStep | 1 | if (this.distanceToSqr(target) < 8.0) | if (target != null && target.isAlive()) within if (this.random.nextInt(8) == 0) | orig :199 |
| 181 | customServerAiStep | 0 | NOT(if (target != null && target.isAlive())) | if (this.random.nextInt(8) == 0) | orig :207 |

The guard is the header of the block that actually encloses the write (found by brace depth, comments and strings blanked); an `else` branch is written as the negated `if` chain it closes; a header the parser cannot read is `(unparsed: ...)`, never dropped.

**Locomotion facts:** a walker. Overrides: hurt, doHurtTarget, aiStep, tick, customServerAiStep, removeWhenFarAway.

**Strike and launch sites:**

- melee `doHurtTarget(target)` — Urchin.java:108 in `doHurtTarget`
- melee `doHurtTarget(this)` — Urchin.java:134 in `aiStep`, guard: if (this.isInWater() && this.random.nextInt(5) == 1 && !this.level().isClientSide)
- melee `doHurtTarget(target)` — Urchin.java:175 in `customServerAiStep`, guard: if (this.random.nextInt(7) == 0) within if (this.distanceToSqr(target) < 8.0) within if (target != null && target.isAlive())

**What fires each contract clip:**

| clip | signal | verdict |
|---|---|---|
| `walk` | limbSwingAmount from AnimationState (the seam's input; 0 at rest, 1 at full stride) | the gait group's weight and speed scale (P3) |
| `idle` | w_idle = (1 - w_move)(1 - w_swim)(1 - w_fly) | standing still |
| `swim` | Entity.isInWater() (client-evaluated); FloatGoal keeps it at the surface | falls back to walk then idle when absent (§2.4) |
| `aggro_idle / calm_idle` | DATA_ATTACKING held while engaged (cleared when the target is lost at line(s) [181]) | STATE flag drives w_aggro (§4.3) |
| `attack` | melee: LivingDamageEvent.Post -> triggerAnim packet (transport 1); ranged: named launch sites (transport 3) | the transport per species by the trigger inventory (ruled 2026-09-06, Q11 (a)): this flag is held, not pulsed at the strike, so the signal column's transport applies — the server LivingDamageEvent.Post -> triggerAnim packet for melee, a named launch site for ranged |
| `hurt` | rising edge of LivingEntity.hurtTime (0 -> 10), client-observed | always available; the red overlay stays (ruled 2026-09-06, Q4 (a)) |
| `death` | LivingEntity.deathTime > 0, client-observed | hold_on_last_frame; the vanilla death flip stays and the pilot ships no death clip (ruled 2026-09-06, Q3 (a)); a creature whose JSON ships a `death` clip gets the clip-replaces-flip mode, designed when the first such clip arrives (the Queen's own death clip the precedent) — until then a delivered `death` plays under the flip |
| `idle_alt_N` | a roll at each idle loop boundary (p = 0.15) while w_idle > 0.9 | optional; one roll per idle loop boundary, p = 0.15, a uniform choice, only while the idle weight is above 0.9 and no triggered clip plays, keyed on the clip-clock cycle index (ruled 2026-09-06, Q5 (a)) |

## 7. Hitbox bones that must keep their names

No MultiHitboxLib profile: no locked bones. Every bone name is still immutable (the code poses bones by name).

## 8. Textures

| canonical file (edit this one) | canvas | also shipped as (fan-out on import) | referenced by the code as | note |
|---|---|---|---|---|
| `urchin.png` | 128x128 | urchintexture.png | urchin.png |  |

## 9. Reference screenshots

`reference/` holds the slots listed in `reference/SLOTS.md`; the owner fills them from the dev client (fixed camera, light and state per slot). Empty until then.

## 10. Effort and priority

- Artist scope: full contract (Tier 2, the state-branching class: geckolib_migration_design.md section 5); the rig is not yet in-game (it lands through the seam in a later slice).
- Estimated effort: 19.4 h (generator: 4 h + 0.2 h/bone x 17 + 1 h/clip x 12 to author or improve (owner adjusts)).
- Tier 2 (state-branching — setupAnim branches on entity state).
- Density statement (the harness's, not the artist's; ruled 2026-09-06, Q9 (a) and Q10): the classic transcription is verified at 2.5e-3 rad — Beaver reference leg 15 / 13 / 8 catmullrom keys per bone with spline arguments repaired at load; wrap sample T−ε vs 0+ε included; the key counts per bone are an output of the harness, re-derived on every re-transcription.

## 11. What 'done' looks like for this entity

_This rig is NOT yet in-game: the geometry is the converter's output over the port's compiled model, proven part for part against the 1.7.10 source by the reference-geometry leg; it lands through the seam in a later slice. Bone names are FINAL — the seam, the hitbox profiles and the transcriptions find bones by name._

1. `reference_urchin.geo.json` returned UNCHANGED (or not at all): every bone name, parent, pivot, rotation and cube as listed in §3 and the packaged file (the reference leg's geo; the rule is the shipped rig's) (a `locked` bone renamed, re-parented or deleted is refused by name).
2. `urchin.animation.json` — that exact file name, one file — (format 1.8.0) holding the clips of §5 with the loop values shown; keys on `locked` bones: Keying a locked bone is allowed; the checker warns, and the hitbox part follows the bone in-game (the consequence, so keep such keys deliberate). Renaming, re-parenting or deleting a locked bone is refused.
3. Textures at their canvas sizes under the canonical names of §8 (aliases are fanned out on import).
4. Optionally the `.bbmodel` working file. `tools/artist_package.py check <your folder>` must pass before hand-in. It REJECTS: a missing or empty folder, or a missing manifest; no `.animation.json`, one under the wrong name (the sheet names the file), or a second one; a clip not in the sheet (renamed or added; `idle_alt_N` only where the sheet allows it), or a clip delivered for a Tier-3 creature; a wrong `loop` value; a missing `idle` or `walk` (they open the game's switch only TOGETHER — one without the other leaves the creature on its code-driven motion); a missing clip the game already triggers by name; a key on a bone the rig does not have (renamed), or a channel other than rotation / position / scale; a Molang string, a non-numeric or non-finite value, a custom-instruction (`timeline`) key; an event keyframe (`sound_effects` / `particle_effects`) on a LOOPING clip of a phase-locked species — no event keyframes on loops; code-fired events come from the trigger inventory (one-shot clips may carry them; a creature whose controllers are its own GeckoLib controllers — the SPEC's controller kind `native`, the Queen — is exempt: native controllers are not phase-locked, event keys fire per loop); |rotation| > 3600 degrees or |position| > 1024 units; a key later than the clip's `animation_length` (a declared length shorter than the last key); a returned `.geo.json` whose bones, parents, pivots, rotations, cubes, UVs or canvas differ from the shipped rig; a `locked` bone renamed, re-parented or deleted in a returned `.geo.json` (the finding names the bone); a texture that is not this creature's, or whose canvas size changed; a `*_reference.animation.json` returned that is not the package's own untouched copy (the reference-only clip the package carries beside the sheet: the classic code sampled at fixed inputs, never edited, never delivered, never shipped — the finding names the file; the untouched copy coming back is warned, not a delivery). It WARNS on: a key on a `locked` bone (the clip and the bones are named) — Keying a locked bone is allowed; the checker warns, and the hitbox part follows the bone in-game (the consequence, so keep such keys deliberate). Renaming, re-parenting or deleting a locked bone is refused; a `_preview` file delivered (a Blockbench-only aid, never in the jar — ruled 2026-09-06, Q15 (a); leave it out of the delivery); a returned `.geo.json` at all (the shipped rig is used regardless — do not re-export it); a clip length far from the one the sheet states (rule 5: a phase-locked creature's loops at 1.0 s — ruled 2026-09-06, Q14 (a); a native creature's clips at their shipped length; a one-shot at the length its row states); an optional clip not delivered; an alias-named texture (the canonical name is preferred); a `format_version` other than 1.8.0; a number written as a string; an unknown keyframe key; a clip without `animation_length`; an unreadable `.bbmodel`.

