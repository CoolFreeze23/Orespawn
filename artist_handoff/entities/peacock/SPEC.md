# Peacock — `peacock` — artist SPEC

Generated by `tools/artist_package.py` 0.4.0 (the reference clips revised, 2026-09-14, second set revised, items 2 to 6: every state a hook reads sampled and named by the seed - reference_walk, reference_idle, reference_attack, reference_fly, reference_swim, the species' own states, an unnamed one flagged - keyed by the exact transcriptions' density search (catmullrom, 1 degree, 1/32 block), embedded in every .bbmodel within the round-trip tolerance with the files beside the sheet the pinned source; a reference clip's keys as the starting point of idle / walk / aggro_idle / fly / swim, a species state offered as a SPEC extra; only the reference file itself under its own name refused; one tracked copy, the folder's copies ignored by git) from the repository's own sources; the paragraphs marked AUTHORED come from `tools/artist_specs/peacock.json` (DRAFT — authored by a seed lane from the entity code, 2026-09-13; the owner's edit pending, landed on its hook by the sixth Tier-2 slice (T2f, 2026-09-15)). Every contract decision this sheet rests on was ruled by the mod's owner on 2026-09-06 (`phase_g_reports/animation_contract/open_questions.md` records each ruling); a line that cites one ("ruled 2026-09-06, Q14 (a)") states that ruling — nothing here invents an answer. The one open item is the lock reject mode (§7, README rule 6).

## 1. What this mob is (AUTHORED — draft for the owner's edit)
> **DRAFT** — DRAFT — authored by a seed lane from the entity code, 2026-09-13; the owner's edit pending, landed on its hook by the sixth Tier-2 slice (T2f, 2026-09-15). Nothing in this section is ruled: it was drafted from the entity code and the model, every claim citing its line, for the owner's edit.


A passive ground bird with a fan: a 5x4x11 body on two 7-long stick legs, a raked neck to a small head with a beak, three zero-thickness head-crest feathers and seven zero-thickness 8x30 tail feathers stacked on one pivot (ModelPeacock.java:62-124, sixteen parts). 15 HP, speed 0.38 and an attack attribute of 4 (Peacock.java:38-39, :67-72). It flees monsters within 8 blocks and players within 12 and panics when hurt (:60-62), strolls clear of water (:63), breeds on a crystal apple (:59, :186-188), and on a 1-in-10 tick hunts the nearest termite within 10 blocks, pecking it for a flat 6 within 2 (:141-151); one tick in 5000 it lays one to three spawn eggs (:152-162). Its fan is a timer: a blink state that flips on for 25-125 ticks and off for 50-350 (:40-42, :78-92) opens the seven tail feathers and raises the crest (ModelPeacock.java:135-151). An ambient creature that spawns in the first half of the day between y 50 and 100 with clear air above, at most two others near (:197-204; ModEntities.java:410-411). Read it as a peacock: a stiff strut, a sudden fan, the crest up.

## 2. Size and scale in-game

- Hitbox (EntityType dims, `ModEntities.java`): 0.65 x 1.2 blocks (wide x tall).
- Renderer scale: x1; shadow radius 0.25 (reference pin status: pin, `tools/reference_renderer_pins.json`).
- Rig units: 16 units = 1 block before the renderer scale, so a 1.2-block-tall hitbox is 19.2 rig units tall at x1.
- Texture canvas: 128 x 128 (keep it; the canvas size is checked on return).
- Notes (AUTHORED): Drawn at x1.0 of the rig, x0.5 as a baby (PeacockRenderer.java:28, :40-42; shadow 0.25, :30) on a 128x128 sheet (ModelPeacock.java:126), peacock.png (PeacockRenderer.java:22); the hitbox is 0.65 x 1.2 (ModEntities.java:410-411). The original's ModelPeacock(0.75f) argument was the wingspeed (ModelPeacock.java:21), not a scale. The head crest and the seven tail feathers are zero-thickness planes.

## 3. Bone glossary (locked legacy names — readable labels beside them)

Never rename, delete or re-parent a bone: code and hitboxes reference them by name. Left/right in the labels follow the model author's own naming (the side the legacy name calls left); the rig opens in Blockbench exactly as the game draws it, so a bone the author calls left is on the creature's own left. `gait bone` = its motion is scaled by walking speed in-game (P3). `locked` = it carries or parents a hitbox part (contract §8.1). Keying a locked bone is allowed; the checker warns, and the hitbox part follows the bone in-game (the consequence, so keep such keys deliberate). Renaming, re-parenting or deleting a locked bone is refused.

| bone | readable label | classic part | parent | pivot (x, y, z) | group | gait bone | locked | cubes |
|---|---|---|---|---|---|---|---|---|
| `body` | body (5x4x11, pitched up 8 deg; static) | (native rig: no classic part) | - | (0, 9, 1) | - | no | no | 1 |
| `head1` | head (2x2x4 at the neck's end; static — no look) | (native rig: no classic part) | - | (0, 12, -8) | - | no | no | 1 |
| `head2` | beak (1x1x2; static) | (native rig: no classic part) | - | (0, 12, -8) | - | no | no | 1 |
| `hf1` | head crest feather 1 (a zero-thickness 7x3 plane; up at 23 deg while the fan is open, folded back to -61 deg otherwise) | (native rig: no classic part) | - | (0, 12, -8) | fan | no | no | 1 |
| `hf2` | head crest feather 2 (-10 deg open, -61 deg folded) | (native rig: no classic part) | - | (0, 12, -8) | fan | no | no | 1 |
| `hf3` | head crest feather 3 (-40 deg open, -61 deg folded) | (native rig: no classic part) | - | (0, 12, -8) | fan | no | no | 1 |
| `lleg` | left leg (a 1x7x1 stick; swings with the gait) | (native rig: no classic part) | - | (1, 7, 0) | gait | yes | no | 1 |
| `neck` | neck (2x2x6 raked up 32 deg; static) | (native rig: no classic part) | - | (0, 10, -3) | - | no | no | 1 |
| `rleg` | right leg (the mirror; swings opposite) | (native rig: no classic part) | - | (-1, 7, 0) | gait | yes | no | 1 |
| `tailf1` | tail feather 1 (a zero-thickness 8x30 plane; fanned to 60 deg up and rolled -23 deg while open, flat otherwise) | (native rig: no classic part) | - | (0.5, 10, 7) | fan | no | no | 1 |
| `tailf2` | tail feather 2 (open: 60 deg up, rolled -46 deg) | (native rig: no classic part) | - | (0.5, 10, 7) | fan | no | no | 1 |
| `tailf3` | tail feather 3 (open: 60 deg up, rolled -69 deg) | (native rig: no classic part) | - | (0.5, 10, 7) | fan | no | no | 1 |
| `tailf4` | tail feather 4 (open: 60 deg up, rolled +23 deg) | (native rig: no classic part) | - | (0.5, 10, 7) | fan | no | no | 1 |
| `tailf5` | tail feather 5 (open: 60 deg up, rolled +46 deg) | (native rig: no classic part) | - | (0.5, 10, 7) | fan | no | no | 1 |
| `tailf6` | tail feather 6 (open: 60 deg up, rolled +69 deg) | (native rig: no classic part) | - | (0.5, 10, 7) | fan | no | no | 1 |
| `tailf7` | tail feather 7, the centre (open: 60 deg up, no roll; flat otherwise) | (native rig: no classic part) | - | (0.514, 10, 7) | fan | no | no | 1 |

## 4. Current animation behaviour — plain English and the source formulas

- One gait rhythm above a limbSwingAmount of 0.1 (ModelPeacock.java:132-134): the two legs swing 27 degrees x the walking speed in opposition at 0.975 rad/tick, a 6.4-tick period, at the 0.38 walk (Peacock.java:38).
- One state branch on the blink timer (ModelPeacock.java:135-169; Peacock.java:78-92): while it is on, the seven tail feathers pitch up 60 degrees and splay by 23-degree steps to either side of the centre feather and the three crest feathers rise; while it is off, the tail lies flat behind and the crest folds back at -61 degrees. The timer is a plain field ticked on both sides from each side's own random (:40-42, :78-92), so the fan is not a synched state — a transport for it waits for the SPEC when the rig lands.
- The body, neck, head and beak are static; the head does not follow the look. The termite peck (Peacock.java:144-146, a flat 6 within 2 blocks) and the egg (:152-162) draw no pose.

### 4.1 Tempo table (contract §7.1)

| group | bones | axis | omega (rad/tick) | natural period | amplitude | speed-scaled | Blockbench preview rate | source |
|---|---|---|---|---|---|---|---|---|
| gait | lleg, rleg | x | 0.975 | 6.444 ticks (0.322 s) | pi x 0.15 = 0.471 rad (27 deg) x the walking speed above a limbSwingAmount of 0.1, the right leg the negative; 0 below the threshold | yes | 3.1 clip-ticks per game tick at a 1.0 s authoring length | ModelPeacock.java:132-134; wingspeed 0.75 (:21) |
| fan | tailf1, tailf2, tailf3, tailf4, tailf5, tailf6, tailf7, hf1, hf2, hf3 | x and z (a pose switch, no rhythm) | - | not time-based | open (blink > 0): every tail feather pitched to 1.047 rad (60 deg) and rolled -0.4 / -0.8 / -1.2 / +0.4 / +0.8 / +1.2 / 0 in order, the crest at 0.401 / -0.174 / -0.698; closed: the tail flat at 0 and the crest folded to -1.06 | no | Peacock.getBlink() (Peacock.java:74-76): a per-side timer field, not a synched byte — on for 25-125 ticks, off for 50-350 (:78-92) | ModelPeacock.java:135-169 |

The bare `walk` clip is the gait group's (`gait`, scaled by walking speed in-game); every other group keys `walk_<group>` / `idle_<group>` (contract §2.1).

Author every loop at 1.0 s (README rule 5, the phase-locked kind; ruled 2026-09-06, Q14 (a)). In-game each loop plays once per natural period whatever its length; your timeline's length is the SHAPE's timeline and does not change that (P6). Blockbench previews at the declared length, so a 1.0 s preview of a fast group shows the shape slowed down (contract §9); a `_preview` copy stretched to the natural periods is a Blockbench-only aid, never in the jar and never in a delivery (ruled 2026-09-06, Q15 (a)).

### 4.2 The math

- **gait**: the two stick legs swing fore and aft in opposition with the stride — `a = amount > 0.1 ? cos(t * 1.3 * 0.75) * pi * 0.15 * amount : 0; lleg.xRot = a; rleg.xRot = -a` (ModelPeacock.java:132-134; wingspeed 0.75 (:21))
- **fan**: the tail fans open and the crest rises while the blink timer is on; the tail lies flat and the crest folds when it is off — `blink > 0 ? (tailf1..7.xRot = 1.047; tailf1..6.zRot = -0.4, -0.8, -1.2, 0.4, 0.8, 1.2; hf1..3.xRot = 0.401, -0.174, -0.698) : (tailf1..7.xRot = 0; tailf1..6.zRot = 0; hf1..3.xRot = -1.06)` (ModelPeacock.java:135-169)
- `ModelPeacock.poseFrom` (ModelPeacock.java:138-178; orig ModelPeacock.java:139-175 in `render` (func_78088_a, :134-193; func_78087_a at :201-203 is a bare super call), wingspeed 0.75 from `new ModelPeacock(0.75f)` in orig ClientProxyOreSpawn.java:478 - the port's WINGSPEED): while the walking speed is above a tenth the swing is the cosine of the age in ticks times 1.3 times the wingspeed, times pi, times 0.15, times the walking speed, else 0; the left leg pitches by it and the right leg by its negative. While the blink counter is above 0 the three head feathers pitch 0.401, -0.174 and -0.698, the seven tail feathers pitch 1.047 and the first six roll -0.4, -0.8, -1.2, 0.4, 0.8 and 1.2; otherwise the head feathers pitch -1.06 and the seven tail feathers pitch 0 with the first six rolled 0 (the seventh's roll is never written). The walk position and the head angles play no part. The GeckoLib hook is the same (PeacockGeoReplacement.applyCustomAnimations).

### 4.3 Reference clips (reference-only)

`peacock_reference_walk.animation.json`, `peacock_reference_idle.animation.json`, `peacock_reference_display.animation.json` (beside this sheet, and embedded in `peacock.bbmodel`'s Animation tab) are NOT clips to return or ship under their own names. Each is the creature's classic code — the motion the game draws today — SAMPLED by the harness at fixed inputs so you can see that motion beside the rig in Blockbench. The `.bbmodel` opens with every clip listed (the shipped clips and these); the files beside the sheet are the PINNED SOURCE (their bytes are what the checker knows; the generator's round trip proves the embedded copies within 5e-5 s on key times and 1e-6 on values). If you rebuild the `.bbmodel` and they are gone, Animation → Import Animations... on the files brings them back. One clip per state the code reads, each value alone: `_reference_walk` — full walking speed (limbSwingAmount 1, the walk position and the age advancing one tick per key), not attacking, looking straight ahead, every state flag at rest, full health; `_reference_idle` — standing still (limbSwingAmount 0, the walk position 0; the age advancing one tick per key), not attacking, looking straight ahead, every state flag at rest, full health; `_reference_display` — the `display` state: the code's `getBlink` answering 1 while standing still, every other state at rest; no `_reference_attack` — this creature's code reads no attack state. Keys: the classic code is sampled once per tick, and the samples are reduced by the density search the exact transcriptions use — the fewest Catmull-Rom keys per bone and channel whose curve stays within 1 degree of rotation and 1/32 block of position of every sample, the closing key always kept — so each clip carries as few keys as that tolerance allows (each entry below states its key counts and the measured maximum error). Rotations are deltas from the bind pose under the same sign rule as the shipped clips (X, Y and Z as the classic degrees: the rig is in the Bedrock convention), positions only where the code moves a bone.

**Starting point (owner 2026-09-14, addendum item 13; extended by the second set revised, item 4):** a reference clip's KEYS may be the starting point of the delivered clip its state corresponds to — copy them into `walk` (from `_reference_walk`), `idle` (from `_reference_idle`), `aggro_idle` (from `_reference_attack`), `fly` (from `_reference_fly`) or `swim` (from `_reference_swim`) and improve from there; a delivered clip carrying those keys is a valid delivery (no rule compares them). A state with no contract clip (sit, sleep, display, ...) is OFFERED as an extra of this creature's own under §2.3 — §5 lists it as "offered from reference_<state>" beside the seed's extras, and a delivered clip under that name carrying the reference keys is a valid delivery. Only the reference file itself coming back under its own name is refused.

- `peacock_reference_walk.animation.json` (sha256 `434091fdf88ebe5ad7e3bc16abd57872fc994512e2fc6b1a8d7c3d7999ed5553`; clip `reference_walk`; a starting point for `walk`): one period of its slowest rhythm — 6.444 ticks (0.322 s): every moving bone is back within 5 degrees of its start there, so the last key closes the loop; 2 to 8 keys per bone chosen by the density search from 8 per-tick samples, Catmull-Rom; max error 0 degrees against the samples (the tolerance 1 degree / 1/32 block).
  - Sampled from: `danger.orespawn.entity.client.PeacockGeoReplacement.applyCustomAnimations(AnimationProcessor, PoseInputs) through OreSpawnGeoReplacement.pose (the S4 doctrine: the shipped replacement, registry-free, on explicit PoseInputs)`.
  - Rule applied: wingspeed 0.75: one rhythm - the threshold legs cos(ageInTicks * 1.3 * WINGSPEED) * PI * 0.15 * limbSwingAmount (PeacockGeoReplacement.applyCustomAnimations:70, walk) at 0.975 rad/tick, 6.44 ticks; the display branch needs getBlink() > 0 (0 on the probe: the feathers stay folded); nothing moves below the threshold; closes at k = 1 (6.444293 ticks): every bone returns within 0 degrees of its start at k x T (the 5-degree test under the 6 s cap; k = 1: 0 deg).
  - Bones that move in it: `hf1`, `hf2`, `hf3`, `lleg`, `rleg`.
  - Loop seam: the closing key differs from the first by at most 0 degrees (mod 360).
- `peacock_reference_idle.animation.json` (sha256 `bdf2dd1cbe8f0c1b49eb7db9b8f5c7c4c3a809515ce4cefefecad42170570409`; clip `reference_idle`; a starting point for `idle`): one key (nothing in this rig's code moves a bone at these inputs: the pose it holds); one key per bone (nothing to reduce).
  - Sampled from: `danger.orespawn.entity.client.PeacockGeoReplacement.applyCustomAnimations(AnimationProcessor, PoseInputs) through OreSpawnGeoReplacement.pose (the S4 doctrine: the shipped replacement, registry-free, on explicit PoseInputs)`.
  - Rule applied: nothing moves at the idle inputs: every bone's rotation and position is the same at every sample of the span the closure test settled (period_multiple; wingspeed 0.75: one rhythm - the threshold legs cos(ageInTicks * 1.3 * WINGSPEED) * PI * 0.15 * limbSwingAmount (PeacockGeoReplacement.applyCustomAnimations:70, walk) at 0.975 rad/tick, 6.44 ticks; the display branch needs getBlink() > 0 (0 on the probe: the feathers stay folded); nothing moves below the threshold; closes at k = 1 (6.444293 ticks): every bone returns within 0 degrees of its start at k x T (the 5-degree test under the 6 s cap; k = 1: 0 deg)), so this state is one key.
  - Bones that move in it: `hf1`, `hf2`, `hf3`.
  - Loop seam: the closing key differs from the first by at most 0 degrees (mod 360).
- `peacock_reference_display.animation.json` (sha256 `78e4417727586fb851ab8d1c0daf8ad846eb60d0eeed7c21bdafc1a6a58b1115`; clip `reference_display`; offered as the extra `display` (§2.3; §5)): one key (nothing in this rig's code moves a bone at these inputs: the pose it holds); one key per bone (nothing to reduce).
  - State: `getBlink` answers 1, every other getter at rest (named by peacock.json reference_states.display = {getBlink: 1}).
  - Sampled from: `danger.orespawn.entity.client.PeacockGeoReplacement.applyCustomAnimations(AnimationProcessor, PoseInputs) through OreSpawnGeoReplacement.pose (the S4 doctrine: the shipped replacement, registry-free, on explicit PoseInputs)`.
  - Rule applied: nothing moves at the display inputs: every bone's rotation and position is the same at every sample of the span the closure test settled (period_multiple; wingspeed 0.75: one rhythm - the threshold legs cos(ageInTicks * 1.3 * WINGSPEED) * PI * 0.15 * limbSwingAmount (PeacockGeoReplacement.applyCustomAnimations:70, walk) at 0.975 rad/tick, 6.44 ticks; the display branch needs getBlink() > 0 (0 on the probe: the feathers stay folded); nothing moves below the threshold; closes at k = 1 (6.444293 ticks): every bone returns within 0 degrees of its start at k x T (the 5-degree test under the 6 s cap; k = 1: 0 deg)), so this state is one key.
  - Bones that move in it: `hf1`, `hf2`, `hf3`, `tailf1`, `tailf2`, `tailf3`, `tailf4`, `tailf5`, `tailf6`, `tailf7`.
  - Loop seam: the closing key differs from the first by at most 0 degrees (mod 360).

`check` REJECTS a `*_reference_<state>.animation.json` returned under its own name (the package's own untouched copy is warned, not a delivery), and the game's jar never carries one (the asset audit refuses it).

This creature has NO exact keyframe transcription (owner 2026-09-13, Amendment 2: its motion does not fit the current transcription form, and the game runs its classic code until you deliver `idle` and `walk`). Its source formulas in plain language (migration design section 5 — the source method and line quoted, each formula and constant in words):

- `ModelPeacock.poseFrom` (ModelPeacock.java:138-178; orig ModelPeacock.java:139-175 in `render` (func_78088_a, :134-193; func_78087_a at :201-203 is a bare super call), wingspeed 0.75 from `new ModelPeacock(0.75f)` in orig ClientProxyOreSpawn.java:478 - the port's WINGSPEED): while the walking speed is above a tenth the swing is the cosine of the age in ticks times 1.3 times the wingspeed, times pi, times 0.15, times the walking speed, else 0; the left leg pitches by it and the right leg by its negative. While the blink counter is above 0 the three head feathers pitch 0.401, -0.174 and -0.698, the seven tail feathers pitch 1.047 and the first six roll -0.4, -0.8, -1.2, 0.4, 0.8 and 1.2; otherwise the head feathers pitch -1.06 and the seven tail feathers pitch 0 with the first six rolled 0 (the seventh's roll is never written). The walk position and the head angles play no part. The GeckoLib hook is the same (PeacockGeoReplacement.applyCustomAnimations).

## 5. Clips: what to improve, what to leave (AUTHORED verdicts on generated rows)
> **DRAFT** — DRAFT — authored by a seed lane from the entity code, 2026-09-13; the owner's edit pending, landed on its hook by the sixth Tier-2 slice (T2f, 2026-09-15). Nothing in this section is ruled: it was drafted from the entity code and the model, every claim citing its line, for the owner's edit.


| clip | loop | layer | weight / trigger | bones it may animate | length | code-triggered | verdict | notes |
|---|---|---|---|---|---|---|---|---|
| `idle` | true | base | w_idle = (1 - w_move)(1 - w_swim)(1 - w_fly) | any of the 16 bones — gait group (speed-scaled): lleg, rleg; free: body, head1, head2, neck; other groups' bones: hf1 (better left to `idle_fan`), hf2 (better left to `idle_fan`), hf3 (better left to `idle_fan`), tailf1 (better left to `idle_fan`), tailf2 (better left to `idle_fan`), tailf3 (better left to `idle_fan`), tailf4 (better left to `idle_fan`), tailf5 (better left to `idle_fan`), tailf6 (better left to `idle_fan`), tailf7 (better left to `idle_fan`) | 1 s | no | author | REQUIRED with walk: the gate opens on the pair (today nothing ships; the classic code plays) — the stand with the fan closed: tail flat, crest folded (ModelPeacock.java:152-169); the open fan is the `display` extra |
| `idle_fan` | true | parallel layer | w_idle = (1 - w_move)(1 - w_swim)(1 - w_fly) (group fan) | tailf1, tailf2, tailf3, tailf4, tailf5, tailf6, tailf7, hf1, hf2, hf3 | 1 s | no | author |  |
| `walk` | true | base | w_walk = w_move (1 - w_swim)(1 - w_fly); the gait group additionally x limbSwingAmount (P3) | any of the 16 bones — gait group (speed-scaled): lleg, rleg; free: body, head1, head2, neck; other groups' bones: hf1 (better left to `walk_fan`), hf2 (better left to `walk_fan`), hf3 (better left to `walk_fan`), tailf1 (better left to `walk_fan`), tailf2 (better left to `walk_fan`), tailf3 (better left to `walk_fan`), tailf4 (better left to `walk_fan`), tailf5 (better left to `walk_fan`), tailf6 (better left to `walk_fan`), tailf7 (better left to `walk_fan`) | 1 s | no | author | REQUIRED with idle: the locomotion loop — the two-leg swing at 0.975 rad/tick, 27 degrees x the walking speed (:132-134); a strut at the 0.38 speed (Peacock.java:38) |
| `walk_fan` | true | parallel layer | w_walk = w_move (1 - w_swim)(1 - w_fly); the gait group additionally x limbSwingAmount (P3) (group fan) | tailf1, tailf2, tailf3, tailf4, tailf5, tailf6, tailf7, hf1, hf2, hf3 | 1 s | no | author |  |
| `swim` | true | base | w_swim from isInWater() | any of the 16 bones — gait group (speed-scaled): lleg, rleg; free: body, head1, head2, neck; other groups' bones: hf1 (better left to `swim_fan`), hf2 (better left to `swim_fan`), hf3 (better left to `swim_fan`), tailf1 (better left to `swim_fan`), tailf2 (better left to `swim_fan`), tailf3 (better left to `swim_fan`), tailf4 (better left to `swim_fan`), tailf5 (better left to `swim_fan`), tailf6 (better left to `swim_fan`), tailf7 (better left to `swim_fan`) | 1 s | no | leave | no swim behaviour: a FloatGoal keeps it at the surface (Peacock.java:58) and it strolls clear of water (:63); the fallback to walk covers it (section 2.4) |
| `calm_idle` | true | base | = idle until this creature's SPEC adds a synched attacking byte (ruled 2026-09-06, Q12 (a)) | (as idle) | 1 s | no | leave | = idle: no synched attacking flag in Peacock.java (the blink timer at :40-42 is the fan, not aggression), so calm_idle is covered by idle (ruled 2026-09-06, Q12 (a)) |
| `attack` | false | triggered controller | event: a strike (the transport per species by the trigger inventory — ruled 2026-09-06, Q11 (a); §6 says which applies here) | (any unlocked) | 0.4 s | yes | author | the peck: on a 1-in-10 tick the nearest visible termite within 10 blocks is hurt for a flat 6 within 2 blocks (Peacock.java:141-147, findSomethingToAttack :170-183), else it paths there at 1.2; no attacking flag, so the strike is transport 1 (the server LivingDamageEvent.Post) — a head jab; the neck and head are static in the code |
| `hurt` | false | triggered controller | event: hurtTime rising edge (client-observed); the red overlay stays (ruled 2026-09-06, Q4 (a)) | (any unlocked) | 0.3 s | yes | author | a flinch to the peacockhit sound (Peacock.java:106-109, at 0.4 volume :116-119); the PanicGoal at 1.5 speed runs after (:62); the hurtTime edge is client-observed |
| `death` | hold_on_last_frame | triggered controller | event: deathTime > 0; the vanilla death flip stays — the pilot ships no death clip (ruled 2026-09-06, Q3 (a)); a creature whose JSON ships one gets the clip-replaces-flip mode when the first such clip arrives, so a `death` delivered here plays under the flip until then | (any unlocked) | 1 s | yes | author | ruled 2026-09-06, Q3 (a): the vanilla death flip stays; a creature whose JSON ships a `death` clip gets the clip-replaces-flip mode, designed when the first such clip arrives (the Queen's own death clip the precedent) - a `death` delivered here plays under the flip until then |
| `display` | true | base | the blink timer (Peacock.java:40-42, :74-76, ticked at :78-92): on for 25-125 ticks, off for 50-350, rolled on each side from its own random; the model opens the fan and raises the crest while it is on (ModelPeacock.java:135-151) — a state loop when the rig lands, its transport the SPEC's (the timer is not synched) | tailf1, tailf2, tailf3, tailf4, tailf5, tailf6, tailf7, hf1, hf2, hf3, body, neck, head1, head2 | 1 s | no | author | the fan held open with a shimmer of the feathers and a strut of the body; the crest up — also offered from `reference_display`: the code's display pose (`getBlink` 1) |
| `lay_egg` | false | overlay | customServerAiStep (Peacock.java:152-162): one server tick in 5000 drops one to three Spawn Peacock eggs a block up and a block around; no packet today — a named site (transport 3) when the rig lands | body, lleg, rleg, tailf1, tailf2, tailf3, tailf4, tailf5, tailf6, tailf7 | 0.8 s | no | author | a squat and a shake on the egg |

Loop values are the JSON `loop` field exactly: `true` for cycles, `false` for one-shots, `hold_on_last_frame` only where written. Every name in this table is fixed (contract §2.4); `idle_alt_1`, `idle_alt_2`, ... may be added freely. A sheet may carry up to four extra clips of this creature's own beyond the contract's names (§2.3; ruled 2026-09-06, Q6 (a)) — more needs the owner's ruling; the extras rows above, if any, are those.

### 5.1 Wishlist (AUTHORED — only what `check` accepts today; anything else is marked)
> **DRAFT** — DRAFT — authored by a seed lane from the entity code, 2026-09-13; the owner's edit pending, landed on its hook by the sixth Tier-2 slice (T2f, 2026-09-15). Nothing in this section is ruled: it was drafted from the entity code and the model, every claim citing its line, for the owner's edit.


- in `display`: the code snaps the fan open and shut (ModelPeacock.java:135-169) — an opening sweep of the seven feathers from the centre outward and a shimmer while held sells the 25-125-tick state (Peacock.java:78-92)
- in `walk`: a bird's head-bob — the neck and head are static in the code (:74-84); the legs alone move (:132-134)
- in `attack`: a neck jab at the termite (Peacock.java:144-146); the rig has no head motion today
- in `idle`: a look-around of the head and a settle of the flat tail; the crest folds to -61 degrees when the fan is closed (:153-155) — let it twitch

## 6. Trigger inventory (generated from the entity's AI goals and state flags)

Source: `src/main/java/danger/orespawn/entity/Peacock.java` (Animal, PeacockPose)

| selector | prio | goal | guard | category | what it does | bears on |
|---|---|---|---|---|---|---|
| goalSelector | 0 | `FloatGoal` | - | locomotion | bobs up to the surface in water (vanilla) | swim state (client reads isInWater) |
| goalSelector | 1 | `BreedGoal` | - | social | walks to a mate and breeds (vanilla) | walk |
| goalSelector | 2 | `AvoidEntityGoal` | - | flee | runs away from a class of entities (vanilla) | walk (fast) |
| goalSelector | 3 | `AvoidEntityGoal` | - | flee | runs away from a class of entities (vanilla) | walk (fast) |
| goalSelector | 4 | `PanicGoal` | - | flee | runs in a panic after taking damage (vanilla) | walk (fast) |
| goalSelector | 5 | `WaterAvoidingRandomStrollGoal` | - | locomotion | strolls to random spots, avoiding water (vanilla) | walk |
| goalSelector | 6 | `RandomLookAroundGoal` | - | look | looks around idly (vanilla; head yaw/pitch only) | none: head look, not a clip |

A `[modern: key]` guard means the goal is registered only under the modern config key named (read once, at construction); `UNPARSED` means the registration's shape is one the generator does not read (a local variable or a computed priority) — the owner reads that line.

**Synched state flags** (what the client can see): none

**Locomotion facts:** has a baby form (the renderer halves the scale). Overrides: tick, customServerAiStep, removeWhenFarAway.

**Strike and launch sites:**

- melee (a direct hurt on the victim) `prey.hurt(this.damageSources().mobAttack(this), 6.0f)` — Peacock.java:147 in `customServerAiStep`, guard: if (this.distanceToSqr(prey) < 4.0) within if (prey != null) within if (this.random.nextInt(10) == 1)

**What fires each contract clip:**

| clip | signal | verdict |
|---|---|---|
| `walk` | limbSwingAmount from AnimationState (the seam's input; 0 at rest, 1 at full stride) | the gait group's weight and speed scale (P3) |
| `idle` | w_idle = (1 - w_move)(1 - w_swim)(1 - w_fly) | standing still |
| `swim` | Entity.isInWater() (client-evaluated); FloatGoal keeps it at the surface | falls back to walk then idle when absent (§2.4) |
| `aggro_idle / calm_idle` | no synched attacking flag | calm_idle only until this creature's SPEC adds a synched byte mirroring getTarget() != null (ruled 2026-09-06, Q12 (a): aggro_idle waits for that byte) |
| `attack` | melee: LivingDamageEvent.Post -> triggerAnim packet (transport 1); ranged: named launch sites (transport 3) | the transport per species by the trigger inventory (ruled 2026-09-06, Q11 (a)): no attacking flag here, so the signal column's transport applies — the server LivingDamageEvent.Post -> triggerAnim packet for melee, a named launch site for ranged |
| `hurt` | rising edge of LivingEntity.hurtTime (0 -> 10), client-observed | always available; the red overlay stays (ruled 2026-09-06, Q4 (a)) |
| `death` | LivingEntity.deathTime > 0, client-observed | hold_on_last_frame; the vanilla death flip stays and the pilot ships no death clip (ruled 2026-09-06, Q3 (a)); a creature whose JSON ships a `death` clip gets the clip-replaces-flip mode, designed when the first such clip arrives (the Queen's own death clip the precedent) — until then a delivered `death` plays under the flip |
| `idle_alt_N` | a roll at each idle loop boundary (p = 0.15) while w_idle > 0.9 | optional; one roll per idle loop boundary, p = 0.15, a uniform choice, only while the idle weight is above 0.9 and no triggered clip plays, keyed on the clip-clock cycle index (ruled 2026-09-06, Q5 (a)) |

## 7. Hitbox bones that must keep their names

No MultiHitboxLib profile: no locked bones. Every bone name is still immutable (the code poses bones by name).

## 8. Textures

| canonical file (edit this one) | canvas | also shipped as (fan-out on import) | referenced by the code as | note |
|---|---|---|---|---|
| `peacock.png` | 128x128 | peacocktexture.png | peacock.png |  |

## 9. Reference screenshots

`reference/` holds the slots listed in `reference/SLOTS.md`; the owner fills them from the dev client (fixed camera, light and state per slot). Empty until then.

## 10. Effort and priority

- Artist scope: full contract (Tier 2, the state-branching class: geckolib_migration_design.md section 5); ON THE HOOK until idle and walk are delivered (Amendment 2): in-game through the seam behind the dev switch since the sixth Tier-2 slice (T2f, 2026-09-15).
- Estimated effort: 16.2 h (generator: 4 h + 0.2 h/bone x 16 + 1 h/clip x 9 to author or improve (owner adjusts)).
- Tier 2 (state-branching — setupAnim branches on entity state).
- Density statement (the harness's, not the artist's; ruled 2026-09-06, Q9 (a) and Q10): the classic transcription is verified at 2.5e-3 rad — Beaver reference leg 15 / 13 / 8 catmullrom keys per bone with spline arguments repaired at load; wrap sample T−ε vs 0+ε included; the key counts per bone are an output of the harness, re-derived on every re-transcription.

## 11. What 'done' looks like for this entity

1. `peacock.geo.json` returned UNCHANGED (or not at all): every bone name, parent, pivot, rotation and cube as listed in §3 and the shipped file (a `locked` bone renamed, re-parented or deleted is refused by name).
2. `peacock.animation.json` — that exact file name, one file — (format 1.8.0) holding the clips of §5 with the loop values shown; keys on `locked` bones: Keying a locked bone is allowed; the checker warns, and the hitbox part follows the bone in-game (the consequence, so keep such keys deliberate). Renaming, re-parenting or deleting a locked bone is refused.
3. Textures at their canvas sizes under the canonical names of §8 (aliases are fanned out on import).
4. Optionally the `.bbmodel` working file. `tools/artist_package.py check <your folder>` must pass before hand-in. It REJECTS: a missing or empty folder, or a missing manifest; no `.animation.json`, one under the wrong name (the sheet names the file), or a second one; a clip not in the sheet (renamed or added; `idle_alt_N` only where the sheet allows it), or a clip delivered for a Tier-3 creature; a wrong `loop` value; a missing `idle` or `walk` (they open the game's switch only TOGETHER — one without the other leaves the creature on its code-driven motion); a missing clip the game already triggers by name; a key on a bone the rig does not have (renamed), or a channel other than rotation / position / scale; a Molang string, a non-numeric or non-finite value, a custom-instruction (`timeline`) key; an event keyframe (`sound_effects` / `particle_effects`) on a LOOPING clip of a phase-locked species — no event keyframes on loops; code-fired events come from the trigger inventory (one-shot clips may carry them; a creature whose controllers are its own GeckoLib controllers — the SPEC's controller kind `native`, the Queen — is exempt: native controllers are not phase-locked, event keys fire per loop); |rotation| > 3600 degrees or |position| > 1024 units; a key later than the clip's `animation_length` (a declared length shorter than the last key); a returned `.geo.json` whose bones, parents, pivots, rotations, cubes, UVs or canvas differ from the shipped rig; a `locked` bone renamed, re-parented or deleted in a returned `.geo.json` (the finding names the bone); a texture that is not this creature's, or whose canvas size changed; a `*_reference_<state>.animation.json` returned under its own name that is not the package's own untouched copy (the reference-only clips the package carries beside the sheet: the classic code sampled at fixed inputs, never shipped — the finding names the file; the untouched copy coming back is warned, not a delivery; copying a reference clip's KEYS into `idle` / `walk` / `aggro_idle` / `fly` / `swim` or into an offered extra is a delivery and passes); a returned `.bbmodel` that lists fewer clips than the folder's animation files hold, or whose embedded keys leave the round-trip tolerance (key times within 5e-5 s, values within 1e-6) of those files (the files beside the sheet are the pinned source; the `.bbmodel` must open with every clip listed — owner 2026-09-14). It WARNS on: a key on a `locked` bone (the clip and the bones are named) — Keying a locked bone is allowed; the checker warns, and the hitbox part follows the bone in-game (the consequence, so keep such keys deliberate). Renaming, re-parenting or deleting a locked bone is refused; a `_preview` file delivered (a Blockbench-only aid, never in the jar — ruled 2026-09-06, Q15 (a); leave it out of the delivery); a returned `.geo.json` at all (the shipped rig is used regardless — do not re-export it); a clip length far from the one the sheet states (rule 5: a phase-locked creature's loops at 1.0 s — ruled 2026-09-06, Q14 (a); a native creature's clips at their shipped length; a one-shot at the length its row states); an optional clip not delivered; an alias-named texture (the canonical name is preferred); a `format_version` other than 1.8.0; a number written as a string; an unknown keyframe key; a clip without `animation_length`; an unreadable `.bbmodel`.

