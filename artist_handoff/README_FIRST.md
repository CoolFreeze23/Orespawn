# READ ME FIRST — OreSpawn animation handoff

_Generated by `tools/artist_package.py` 0.2.6 (the pilot package, 2026-09-13: artist_handoff/ writable after the mirror drop; the Queen's pilot scope - idle and attack - marked in her seed and sheet). Sections marked PROVISIONAL follow decisions the mod's owner has not made yet; they will be confirmed before your gig starts._

## What this is

OreSpawn is a large Minecraft creature mod from 2013–2015, being rebuilt for Minecraft 1.21.1. Its monsters were animated in code: a handful of sine-wave formulas per creature, written once and never revisited. We are hiring you to replace those baked animations with hand-made ones — idle, walk, attack, hurt and so on — and, where a creature's sheet lists a wishlist, to add new ones. The models, bone names and textures are FIXED; your work is the motion (and, if asked, the textures).

## Toolchain

- **Blockbench** (free, blockbench.net). Each entity folder holds a `.bbmodel` you can open directly, and the raw `.geo.json` (the rig) with its texture(s) beside it; if you open the `.geo.json` instead, choose a **Bedrock Entity** project.
- Deliver one **`.animation.json`** per entity (Bedrock animation format 1.8.0 — Blockbench's default export for a Bedrock Entity project), and optionally the `.bbmodel` working file. Do NOT re-export the `.geo.json`: a re-export silently drops data the game needs; the rig you were given is the rig that ships.

## Hard rules (the game breaks if these are broken)

1. **Never rename, delete or re-parent a bone.** Code and hitboxes find bones by name. The glossary in each `SPEC.md` gives readable labels beside the fixed names; use the labels to understand, the names to work.
2. **Never rename a clip.** Every clip name in a `SPEC.md` table is fixed; the ones marked code-triggered are fired by the game by that exact name. You may ADD clips only where the sheet says so (`idle_alt_1`, `idle_alt_2`, ...).
3. **Set each clip's loop mode exactly as its table says**: `true` for cycles (idle, walk), `false` for one-shot actions (attack, hurt), `hold_on_last_frame` only where written (death). **Deliver `idle` and `walk` together**: the game switches a creature to your animations only when BOTH are in the file — one without the other leaves the creature on its old code-driven motion, and `check` says so.
4. **Keep texture canvas sizes.** A 64x32 texture stays 64x32.
5. **Keep each clip's length near the period the sheet states** unless the sheet says the length is free. (In-game, loops play at the creature's own tempo whatever their length — the sheet's tempo table explains; PROVISIONAL, see below.)
6. **A bone the sheet marks `locked` carries or parents a hitbox part (contract §8.1).** Keying a locked bone is allowed; the checker warns, and the hitbox part follows the bone in-game (the consequence, so keep such keys deliberate). Renaming, re-parenting or deleting a locked bone is refused. A reject mode for keys on locked bones stays available for the day the server-side hitbox evaluator lands (`check --lock-mode reject`, or `lock_mode: reject` in the manifest); it is not today's policy.
7. Rotation / position / scale keys, linear or Catmull-Rom curves (and GeckoLib easings) are fine; sound and particle keys only on one-shot clips (attack, hurt, death). **No event keyframes on loops** (idle, walk and the other cycles) — code-fired events come from the trigger inventory in each sheet, not from keys on a cycle (a loop plays under a phase lock that would fire such a key once, ever). Exception: a creature whose controllers are its own GeckoLib controllers (its sheet says controller kind `native` — the Queen) is not phase-locked, so its loops may carry event keys; they fire once per loop, and `check` notes each one (owner 2026-09-13). No Molang expressions, no custom-instruction keys, no `_preview` files in a delivery (a Blockbench-only aid — the checker warns; PROVISIONAL, open question 15). The `<registry>_reference.animation.json` beside each sheet is REFERENCE-ONLY: the creature's classic code sampled at fixed inputs so you can see today's motion in Blockbench — never edit it into a delivery, never return it (the checker REJECTS it by name) and never ship it (the game's jar never carries one).

## What is in each entity folder

```
entities/<registry_name>/
  <name>.geo.json          the rig (do not edit, do not re-export)
  <name>.animation.json    the current clips (empty for creatures that ship none yet)
  <registry_name>_reference.animation.json
                           REFERENCE-ONLY: the creature's classic code sampled at fixed inputs, to look at beside the rig
                           (SPEC §4.3); never edit, return or ship it — the checker rejects it by name
  <registry_name>.bbmodel  a Blockbench project of the same rig, textures embedded
  textures/                the texture(s) to edit, one canonical copy each
  SPEC.md                  the creature's sheet: what it is, size, bone glossary, current motion in plain English,
                           what to improve / leave, loop-mode table, what fires each clip, locked bones, wishlist
  spec.manifest.json       the machine-readable contract the checker reads (do not edit)
  reference/               screenshot slots (filled by the owner from the game)
```

## What 'done' looks like and how files come back

Return a folder per entity, mirroring `entities/` (same folder name, same file names): the `.animation.json` under the exact name the sheet gives, any edited textures under `textures/` with their original names, and optionally your `.bbmodel`. Before sending, run `python tools/artist_package.py check <your entity folder>` (or ask the owner to): it reports every rule above as PASS / WARN / REJECT and ends with a summary line. A folder with no REJECT is done.

**What `check` REJECTS** (the folder is not done):

- a missing or empty folder, or a missing manifest
- no `.animation.json`, one under the wrong name (the sheet names the file), or a second one
- a clip not in the sheet (renamed or added; `idle_alt_N` only where the sheet allows it), or a clip delivered for a Tier-3 creature
- a wrong `loop` value; a missing `idle` or `walk` (they open the game's switch only TOGETHER — one without the other leaves the creature on its code-driven motion); a missing clip the game already triggers by name
- a key on a bone the rig does not have (renamed), or a channel other than rotation / position / scale
- a Molang string, a non-numeric or non-finite value, a custom-instruction (`timeline`) key
- an event keyframe (`sound_effects` / `particle_effects`) on a LOOPING clip of a phase-locked species — no event keyframes on loops; code-fired events come from the trigger inventory (one-shot clips may carry them; a creature whose controllers are its own GeckoLib controllers — the SPEC's controller kind `native`, the Queen — is exempt: native controllers are not phase-locked, event keys fire per loop)
- |rotation| > 3600 degrees or |position| > 1024 units
- a key later than the clip's `animation_length` (a declared length shorter than the last key)
- a returned `.geo.json` whose bones, parents, pivots, rotations, cubes, UVs or canvas differ from the shipped rig
- a `locked` bone renamed, re-parented or deleted in a returned `.geo.json` (the finding names the bone)
- a texture that is not this creature's, or whose canvas size changed
- a `*_reference.animation.json` returned that is not the package's own untouched copy (the reference-only clip the package carries beside the sheet: the classic code sampled at fixed inputs, never edited, never delivered, never shipped — the finding names the file; the untouched copy coming back is warned, not a delivery)

**What `check` WARNS about** (delivered, but read the line):

- a key on a `locked` bone (the clip and the bones are named) — Keying a locked bone is allowed; the checker warns, and the hitbox part follows the bone in-game (the consequence, so keep such keys deliberate). Renaming, re-parenting or deleting a locked bone is refused.
- a `_preview` file delivered (a Blockbench-only aid; whether the package ships one is open question 15) — PROVISIONAL
- a returned `.geo.json` at all (the shipped rig is used regardless — do not re-export it)
- a clip length far from the stated one (PROVISIONAL rule); an optional clip not delivered
- an alias-named texture (the canonical name is preferred); a `format_version` other than 1.8.0
- a number written as a string; an unknown keyframe key; a clip without `animation_length`; an unreadable `.bbmodel`

## Priority order and effort

Bosses first (they carry hitboxes and need the most care), then the ordinary creatures, then nothing for the code-driven props (no animation work: they are listed so their textures and sheets exist).

| folder | creature | tier | bones | clips to deliver | estimated effort |
|---|---|---|---|---|---|
| the_queen | The Queen | Tier 1 (boss; the design's 'done' row) | 110 | idle, idle_to_attack, attack, bite, tail_whip_right, tail_whip_left, roar, death | 27.5 h |
| ant | EntityAnt | Tier 2 | 0 | none (no animation work) | 0 h |
| beaver | Beaver | Tier 2 | 9 | idle, idle_teeth, idle_tail, walk, walk_teeth, walk_tail, swim, calm_idle, hurt, death, chop | 12.8 h |
| brutalfly | EntityBrutalfly | Tier 2 | 0 | none (no animation work) | 0 h |
| cliff_racer | EntityCliffRacer | Tier 2 | 0 | none (no animation work) | 0 h |
| cockateil | Cockateil | Tier 2 | 0 | none (no animation work) | 0 h |
| dragonfly | EntityDragonfly | Tier 2 | 0 | none (no animation work) | 0 h |
| firefly | Firefly | Tier 2 | 0 | none (no animation work) | 0 h |
| gold_fish | GoldFish | Tier 2 | 0 | none (no animation work) | 0 h |
| mosquito | EntityMosquito | Tier 2 | 0 | none (no animation work) | 0 h |
| rainbow_ant | EntityRainbowAnt | Tier 2 | 0 | none (no animation work) | 0 h |
| red_ant | EntityRedAnt | Tier 2 | 0 | none (no animation work) | 0 h |
| ruby_bird | RubyBird | Tier 2 | 0 | none (no animation work) | 0 h |
| termite | EntityTermite | Tier 2 | 0 | none (no animation work) | 0 h |
| tshirt | EntityTshirt | Tier 2 | 0 | none (no animation work) | 0 h |
| unstable_ant | EntityUnstableAnt | Tier 2 | 0 | none (no animation work) | 0 h |
| coin | Coin | Tier 3 | 0 | none (no animation work) | 0 h |
| elevator | Elevator | Tier 3 | 0 | none (no animation work) | 0 h |
| island | Island | Tier 3 | 0 | none (no animation work) | 0 h |
| island_too | IslandToo | Tier 3 | 0 | none (no animation work) | 0 h |
| purple_power | PurplePower | Tier 3 | 0 | none (no animation work) | 0 h |
| robot_1 | Robot1 | Tier 3 | 0 | none (no animation work) | 0 h |
| robot_2 | Robot2 | Tier 3 | 0 | none (no animation work) | 0 h |
| robot_3 | Robot3 | Tier 3 | 0 | none (no animation work) | 0 h |
| robot_4 | Robot4 | Tier 3 | 0 | none (no animation work) | 0 h |
| robot_5 | Robot5 | Tier 3 | 0 | none (no animation work) | 0 h |
| rock_base | RockBase | Tier 3 | 0 | none (no animation work) | 0 h |
| rotator | EntityRotator | Tier 3 | 0 | none (no animation work) | 0 h |
| vortex | EntityVortex | Tier 3 | 0 | none (no animation work) | 0 h |

Effort figures are the generator's estimate (bosses 8 h + 0.15 h per bone + 1.5 h per clip to author or improve; others 4 h + 0.2 h per bone + 1 h per clip; a clip marked 'leave' or 'covered by idle' is not counted) unless the sheet states an owner-set figure. 'Tier 1 (boss; the design's 'done' row)' is a creature the migration design lists as done (a native rig) and the animation contract treats as its Tier-1 boss — both are true.

## PROVISIONAL items (decided before a gig starts)

- The authoring-length convention for loops (normalise every loop to 1.0 s, or author at the natural period).
- Whether a `death` clip replaces the vanilla death flip, and whether a `hurt` clip suppresses the red flash.
- The `attack` trigger transport per creature, and `aggro_idle` for creatures without a synced attacking flag.
- The optional `idle_alt_N` cadence and the cap on extra clips per creature.
- Which boss is the pilot (the Queen is the candidate).

