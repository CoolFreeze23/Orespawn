feat(queen): expand to 12-part dynamic hitbox system synced with client animations

- Expand sub-part array from 7 to 12 OreSpawnPartEntity hitboxes to fully
  map The Queen's massive geometry: body, 3 necks, 3 heads, 2 wings, and
  a 3-segment tail (base/mid/tip) that curves with the whip animation
- Replace all static hardcoded offsets in tick() with trigonometric math
  that mirrors the client-side ModelTheQueen.setupAnim frequencies, so
  every hitbox tracks the visual model frame-by-frame
- Compute head/neck positions using arc kinematics: sin(headlr) * reach
  for lateral swing and (1 - cos(headlr)) * reach for Z arc retraction,
  matching the organic sway of the hierarchical neck chain
- Implement a 7-link tail chain walk with phase-offset cosine waves
  (PI/4 per segment) to produce a rippling whip effect on the hitboxes
- Drive wing hitbox Y and X from the flap angle so they rise and compress
  laterally in sync with the client zRot animation
- Update hurtFromPart damage multipliers for all 12 parts: heads at 1.0x
  (precision reward), body at 0.5x (armored core), necks/wings/tail
  segments at 0.25x + 1 flat damage
- Scale up hitbox dimensions to match 3x model scale: body 8x8, heads
  4x4, necks 3.5x3.5, wings 14x2.5, tail base 5x5 tapering to tip 3x2
