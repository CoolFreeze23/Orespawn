fix(queen): correct hitbox coordinate desync by inverting Z axis and applying proper model-to-world scaling

- Fix 180-degree Z-axis inversion: the renderer's rotateY(180-bodyYaw)
  negates model Z in world space, so all Z offsets were placing hitboxes
  on the opposite side of the entity from the visual model (~74 block gap)
- Apply the exact conversion formula (modelCoord / 16.0) * 3.0 to every
  base offset, replacing hardcoded approximations with traceable values
  expressed as modelUnits * S where S = 3.0 / 16.0
- Recalculate neckReach (123 model units), wingCenterDist (124 units),
  and all 7 tail segment lengths from their true PartPose offsets
- Reverse tail chain walk direction (tailZ -= cos * len) so the tail
  hitboxes trail behind the entity instead of projecting forward
- Correct leg Z offsets to place them behind the body center, matching
  the model's thigh pivot at model Z = +78 from root
