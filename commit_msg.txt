fix(queen): enlarge root AABB, add leg hitboxes, scale up parts, and invert wing Y-axis

- Override getDefaultDimensions(Pose) to return EntityDimensions.fixed(16, 12)
  so the root bounding box fully encapsulates The Queen's massive core
- Add legLeft and legRight sub-hitboxes (5x12) positioned at her thigh
  columns with a subtle sine-wave walk sway in tick()
- Scale up all existing sub-part dimensions to eliminate dead zones:
  body 8x8 → 12x12, necks/heads 3.5-4 → 6x6, wings 14x2.5 → 18x4,
  tail segments proportionally increased
- Invert wing flap Y-axis math (22.7 + wingDY → 22.7 - wingDY) to
  correct model-space vs world-space Y inversion so hitboxes track the
  visual wing downstroke accurately
- Route legL/legR hits through hurtFromPart at the 0.5x armored
  multiplier, matching the body's damage reduction tier
